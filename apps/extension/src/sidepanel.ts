/**
 * Side panel placeholder (T062).
 *
 * BROWSER BOUNDARY: runs in Chrome. T063 replaces this with the real capture
 * panel (current selection + priority chips + reason + recent captures), reusing
 * the SAME loopback capture path as the popup. For now it is a minimal valid page
 * so the manifest's `side_panel.default_path` resolves.
 */

export {};
