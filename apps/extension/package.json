{
  "name": "@interleave/extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Interleave browser extension (MV3) — local-first page/selection capture into the desktop app.",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs --watch",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"@interleave/extension: linted from repo root via biome\"",
    "test": "echo \"@interleave/extension: covered by capture-contract + capture-handler unit tests + the Electron loopback integration test (a real Chrome extension cannot be Playwright-driven — see README)\""
  },
  "dependencies": {
    "@interleave/capture-contract": "workspace:*"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "esbuild": "^0.28.1"
  }
}
