# Roadmap — the build queue

This is the **single source of truth for orchestration**. Each entry is one buildable
task. An agent picks the lowest-numbered unchecked task whose dependencies are all `[x]`,
builds the feature + tests with native pnpm (`pnpm typecheck` / `pnpm test` / `pnpm lint`),
then checks the box and records the commit.

> **Architecture (authoritative):** the MVP ships as a local-first **Electron desktop app**
> on a **native SQLite** database (via **better-sqlite3** + Drizzle, SQLite dialect) — **not**
> a browser PWA, and **not** PGlite. The React + TypeScript + Vite app is a pure **renderer**;
> the **Electron** shell (main process, preload, IPC) owns all trusted local capabilities.
> SQLite is the canonical local database; the filesystem **asset vault** is the canonical
> local store for PDFs/snapshots/images/media/exports/backups. The renderer **never** talks
> directly to SQLite or arbitrary filesystem APIs — it calls a narrow typed `window.appApi`
> bridge. Layering: React UI → typed client API wrapper → preload bridge → Electron main/DB
> service → `packages/local-db` repositories/services → SQLite + vault. Native **pnpm** is the
> canonical way to run/dev/test the desktop app; the Docker/compose/Makefile setup is re-scoped
> to the **future encrypted-backup server only** (a thin `api` + minimal `db` + `minio`
> blob store — no sync tier, no server-side `worker`; on-device work runs in a local runner).

**Status legend:** `[ ]` not started · `[~]` in progress · `[x]` done · `[!]` blocked.
Add `· (branch/commit)` after the title when you start/finish.

Format per task:

```
- [ ] **T0NN — Title** · _deps: T0xx, T0yy_
  Done when: <criterion>.
```

Reference docs: [`concept`](./concept.md) · [`architecture`](./architecture.md) ·
[`domain-model`](./domain-model.md) · [`scheduling-and-priority`](./scheduling-and-priority.md)
· [`design-system`](./design-system.md) · charter: [`../CLAUDE.md`](../CLAUDE.md).

> **UI tasks follow the design kit.** Every screen below has a matching prototype in
> [`../design/kit/`](../design/) — see the screen→milestone map in
> [`design-system.md`](./design-system.md). Use `design/tokens.css` and `lucide-react`; match
> the design pixel-for-pixel.

---

# Part I — Decent MVP (T001–T050)

Goal: a genuinely useful single-person, local-first incremental reading app — a local-first
**Electron desktop app** on **native SQLite**. **No** PDF, sync, AI, browser extension, or
mobile yet.

## M1 — Foundations & local persistence (T001–T011)
Detailed specs: [`tasks/M1-foundations.md`](./tasks/M1-foundations.md)

- [x] **T001 — Create the monorepo** · done · _deps: none_
  Done when: pnpm workspace with `apps/web` (the Electron **renderer**, pure UI), `apps/api`, `packages/{core,db,scheduler,editor,ui,testing}` exists and root scripts run from the repo root. (Pivot: `apps/desktop` (Electron main/preload/lifecycle) and `packages/local-db` are added in T007/T008; native pnpm — `pnpm typecheck`/`pnpm test`/`pnpm lint` — is the canonical desktop toolchain.)
- [x] **T002 — Tooling + CI gates (Docker re-scoped to server phase)** · done · _deps: T001_
  Done when: strict TypeScript, Biome, Vitest, Playwright, and CI are wired so CI rejects type errors, lint errors, unit failures, and one smoke E2E failure. (Pivot: native `pnpm typecheck`/`pnpm test`/`pnpm lint` are canonical for the desktop app; the existing Dockerfiles/`docker-compose.yml`/`Makefile` are kept but re-scoped to the **future server phase only** — `api`/`worker`/`db`/`minio` — and are no longer canonical for building/running the app.)
- [x] **T003 — Scaffold the React renderer** · done · _deps: T002_
  Done when: `apps/web` runs as the Vite + React + TS + TanStack Router + Tailwind v4 **renderer** with routes `/`, `/inbox`, `/queue`, `/source/$id`, `/review`, `/search`, `/settings`. (Pivot: `apps/web` is a pure UI renderer that talks to `window.appApi` in desktop mode, not a standalone PWA.)
- [x] **T004 — App shell skeleton** · done · _deps: T003_
  Done when: left sidebar, top command bar, central work area, right inspector, bottom status bar; every main route uses the same shell and is keyboard-navigable.
- [x] **T005 — Domain language in `packages/core`** · done · _deps: T001_
  Done when: documented TS types for `Element`, `ElementType`, `ElementStatus`, `DistillationStage`, `Priority`, `ReviewState`, `ReviewLog`, `Source`, `Document`, `ElementRelation`, `ElementLocation`, plus the new desktop types `Asset`, `AssetLocation`, `OperationLogEntry`, and `LocalVaultPath`, used by app and tests.
- [x] **T006 — Native SQLite + Drizzle schema** · done · _deps: T005_
  Done when: `packages/db` holds the Drizzle schema (**SQLite dialect**) and migrations for `elements`, `documents`, `document_blocks`, `document_marks`, `sources`, `source_locations`, `element_relations`, `read_points`, `cards`, `review_states`, `review_logs`, `concepts`, `tags`, `element_tags`, `tasks`, `assets`, `operation_log`, and `settings`; types align with `@interleave/core`; `drizzle-kit generate`/`migrate` plus a dev-reset can create and reset a dev database; schema round-trips against a temporary in-memory **better-sqlite3** DB in tests. Stable UUID/ULID-style IDs are generated in domain services. FTS tables (`source_fts`, `extract_fts`, `card_fts`) arrive with search later. (Pivot: native SQLite via better-sqlite3 — **no PGlite**.)
- [x] **T007 — Electron desktop shell + native SQLite persistence** · done · _deps: T006, T003_
  Done when: `apps/desktop` exists with a secure Electron window (`contextIsolation: true`, `nodeIntegration: false`, `sandbox` where practical, `enableRemoteModule: false`) and a **narrow typed preload bridge** exposing `window.appApi` (initially `app.health()`, `db.getStatus()`, `settings.get/update()`) with validated IPC payloads (Zod or equivalent); the app data directory is initialized (e.g. `~/Library/Application Support/<app>/` with `app.sqlite` + `-wal`/`-shm`, `assets/`, `backups/`); SQLite is opened via better-sqlite3 with `PRAGMA foreign_keys=ON`, `journal_mode=WAL`, `busy_timeout=5000`; Drizzle migrations run on startup (explicit/safe in production); in dev Electron loads the Vite dev server and in production it loads the built renderer files; a health command is callable from the renderer through `window.appApi`; data **persists across app restart**. The renderer has no raw Node/filesystem/SQLite access and never sees a generic `db.query(sql)`. (Pivot: replaces the old PGlite task entirely — native SQLite, not browser storage.)
- [x] **T008 — Repository classes in `packages/local-db`** · done · _deps: T007_
  Done when: `ElementRepository`, `DocumentRepository`, `SourceRepository`, `ReviewRepository`, `QueueRepository`, `SearchRepository`, `AssetRepository`, `SettingsRepository`, and `OperationLogRepository` live in `packages/local-db` behind the Electron/IPC boundary; meaningful mutations are transactional (multi-table operations in one transaction) and append `operation_log` entries; deletes are soft (`deleted_at`); the renderer consumes repositories **only** via typed `window.appApi` commands (no React component touches SQL); per-repo smoke tests cover referential integrity + persistence.
- [x] **T009 — Desktop dev seed & fixtures** · done · _deps: T008_
  Done when: a desktop dev seed command resets the dev SQLite DB and creates a realistic demo collection — a source with document blocks, an extract with a source location, a sub-extract, a Q&A card, a cloze card, review state/logs, concepts/tags, asset metadata, and `operation_log` entries; shared factories/fixtures live in `packages/testing` and are reused by both Vitest and Playwright.
- [x] **T010 — Universal element inspector** · done · _deps: T008, T004_
  Done when: the right panel shows any selected element's type, status, stage, priority, due date, parent, children, source, tags, and review metadata — fetched **through the typed `window.appApi`** (never direct DB access from the renderer).
- [x] **T011 — Local settings in SQLite** · done · _deps: T008_
  Done when: settings for daily review budget, default desired retention, default topic interval, default source priority, keyboard layout, and theme persist in the SQLite `settings` table (user/domain settings prefer SQLite; Electron config is used only for app-level desktop settings if needed) and are read by scheduler code through the typed API.

## M2 — Capture & inbox (T012–T014)

- [x] **T012 — Inbox · done** · _deps: T008, T004_
  Done when: a source can be created in inbox, listed, viewed, kept, prioritized, accepted into active learning, or deleted.
- [x] **T013 — Manual text import · done** · _deps: T012_
  Done when: a "New source" modal accepts title/URL/author/date/body and stores body as both plain text and ProseMirror JSON; a pasted article appears as a source in the inbox.
- [x] **T014 — Source provenance fields (no auto-fetch)** · done · _deps: T013_
  Done when: schema/UI capture canonical URL, original URL, accessed date, and snapshot fields for manual imports (no remote fetching yet).

## M3 — Document editor & reading (T015–T018)

- [x] **T015 — Tiptap document editor** · done · _deps: T013, T005_
  Done when: a source body renders/edits with a constrained schema (headings, paragraphs, bold, italic, links, blockquotes, lists, code, hr); edits save and reload.
- [x] **T016 — Stable block IDs** · done · _deps: T015_
  Done when: every block node has a stable ID preserved across imports and saves (basis for extraction/read-points/sync).
- [x] **T017 — Read-points** · done · _deps: T016_
  Done when: `read_point` (block ID + offset) is stored on source/topic; set/jump/auto-update-on-extract work; reopening a source resumes near the last read-point.
- [x] **T018 — Source reading mode · done** · _deps: T017, T004_
  Done when: a clean reader shows title, metadata, body, read-point marker, extracted-span markers, and keyboard actions — pleasant enough to process a long article.

## M4 — Highlights, extraction & lineage (T019–T026)

- [x] **T019 — Text-selection toolbar** · _deps: T018_ · done
  Done when: selecting text in the reader shows an inline toolbar (Extract, Cloze, Highlight, Copy, Cancel) without breaking editor selection.
- [x] **T020 — Highlights** · _deps: T019_ · done
  Done when: highlight marks persist as document annotations and can be removed (highlights are NOT extracts).
- [x] **T021 — Extraction** · _deps: T019, T008_ · done
  Done when: Extract creates a child `extract` element with its own document body, source reference, parent link, source location, inherited priority, and scheduled review state; the parent text is visually marked extracted.
- [x] **T022 — Source locations** · _deps: T021_ · done
  Done when: each extract stores source element ID, block IDs, start/end offsets, and a human-readable label; the user can jump from an extract back to the exact paragraph.
- [x] **T023 — Element hierarchy view** · done · _deps: T021, T010_
  Done when: source pages show a tree of children (extracts/sub-extracts/cards) and extract pages show parent + children; navigation works both directions.
- [x] **T024 — Extract review mode** · done · _deps: T021_
  Done when: extracts appear as readable mini-topics with trim/rewrite/split/convert/postpone/done/delete; an extract can move raw → clean → atomic.
- [x] **T025 — Extract splitting (sub-extracts)** · _deps: T024, T022_ · done
  Done when: selecting part of an extract creates a sub-extract with preserved lineage (source → extract → sub-extract).
- [x] **T026 — Mark processed on source text** · done · _deps: T020_
  Done when: source blocks have durable processing outcomes that can be dimmed, filtered, restored, summarized, and reconciled after edits without deleting the archived source.

## M5 — Priority, scheduling & queue (T027–T031)

- [x] **T027 — Priorities** · done · _deps: T008_
  Done when: priority is stored numerically and surfaced as A/B/C/D; every source/extract/card can be raised/lowered.
- [x] **T028 — Topic/extract scheduler** · done · _deps: T027, T005_
  Done when: a non-card scheduler computes `due_at` from priority, stage, last-seen, and action; items can be scheduled for tomorrow/next week/next month/manual. (See [`scheduling-and-priority`](./scheduling-and-priority.md).)
- [x] **T029 — Due queue** · done · _deps: T028, T004_
  Done when: `/queue` shows due sources/extracts/cards sorted by priority then due date, with filters for type/concept/status.
- [x] **T030 — Queue actions** · done · _deps: T029_
  Done when: each due item supports open/postpone/raise/lower/done/dismiss/delete without leaving the list.
- [x] **T031 — "Process queue" learning loop** · done · _deps: T030_
  Done when: a single mode shows one element at a time and advances after action; the user can process ten mixed elements without returning to a list.

## M6 — Cards (T032–T035)

- [x] **T032 — Card model & templates** · done · _deps: T008, T005_
  Done when: `card` elements have `card_type`, `prompt`, `answer`, `cloze_text`, `source_extract_id`, `sibling_group_id`; Q&A and cloze types exist as first-class elements with parents, priority, and review state.
- [x] **T033 — Q&A card creation** · done · _deps: T032, T024_
  Done when: from an extract, "Create Q&A card" shows prompt/answer/source-context/preview; the card appears in review.
- [x] **T034 — Cloze card creation** · done · _deps: T032, T024_
  Done when: selecting text in an extract creates a cloze card linked to the extract; clozes store `{{c1::answer}}` text plus structured cloze metadata.
- [x] **T035 — Card-quality warnings** · done · _deps: T033, T034_
  Done when: warnings flag prompt-too-long, answer-too-long, missing source, multiple clozes, ambiguous pronouns, and empty answer before activation.

## M7 — FSRS review (T036–T040)

- [x] **T036 — Integrate `ts-fsrs`** · _deps: T032_ · done
  Done when: a `SchedulerService` wraps `ts-fsrs` and persists FSRS state (due/stability/difficulty/elapsed/scheduled/reps/lapses) on `review_states`; new cards reschedule by rating.
- [x] **T037 — Review UI · done** · _deps: T036, T004_
  Done when: `/review` shows prompt → reveal → grade Again/Hard/Good/Easy, logs response time, updates scheduler state, advances; every review writes a durable `review_logs` row.
- [x] **T038 — Review editing** · done · _deps: T037_
  Done when: during review the user can edit prompt/answer, open source, suspend, delete, and flag-as-bad — fixing a bad card at the moment it fails.
- [x] **T039 — Sibling burying** · _deps: T037, T032_ · done
  Done when: cards from the same extract/cloze group don't appear back-to-back in a session unless burying is disabled.
- [x] **T040 — Basic leech detection** · done · _deps: T037_
  Done when: a card is marked leech after repeated failures (warn at 4 lapses) and appears in a cleanup view with rewrite/suspend/delete.

## M8 — Organize: concepts, tags, search, references (T041–T043)

- [x] **T041 — Concepts & tags** · done · _deps: T008_
  Done when: concepts (hierarchical) and tags (flat) can be created/assigned; elements filter by concept and tags.
- [x] **T042 — Search** · done · _deps: T008_
  Done when: local full-text search over source title/body, extract body, card prompt/answer, and tags returns sources/extracts/cards quickly with simple ranking.
- [x] **T043 — Source/reference display** · done · _deps: T022, T032_
  Done when: every extract and card shows source title/URL/author/date/location (review hides it until answer reveal); nothing feels orphaned.

## M9 — Safety, analytics & backup (T044–T047)

- [x] **T044 — Deletion, trash & undo** · done · _deps: T008_
  Done when: soft delete + trash view + restore exist; command-level undo covers delete/mark-done/suspend/bulk-postpone; accidental deletion is recoverable.
- [x] **T045 — Basic analytics** · done · _deps: T037, T028_
  Done when: a view shows daily reviews, due cards/topics, new cards/extracts, deletions, leeches, and 30-day retention.
- [x] **T046 — Import/process balance warnings** · _deps: T045_ · done
  Done when: the app warns when imports outpace processing, showing sources imported / extracts created / cards created / reviews due this week.
- [x] **T047 — Backup / export** · _deps: T008_ · done
  Done when: an Electron-managed backup exports a ZIP of `app.sqlite` + the `assets/` vault + a `manifest.json` (schema version, app version, timestamp, integrity hashes) into `backups/<timestamp>/`; the format is designed for restore from the start so a backup re-imports into a fresh install. (Pivot: backup is SQLite file + filesystem asset vault, not a JSON dump.)

## M10 — Keyboard, E2E & ship MVP as Electron desktop (T048–T050)

- [x] **T048 — Keyboard shortcuts & command palette** · done · _deps: T031, T037, T021_
  Done when: shortcuts exist for next-item, extract, cloze, postpone, done, delete, raise/lower priority, search, open-parent, open-source, and command palette; the main workflow is mouse-free. Shortcuts invoke commands through the **same typed `window.appApi` path** as the UI buttons (no separate mutation path).
- [x] **T049 — MVP end-to-end tests** · done · _deps: T048, T047_
  Done when: Playwright runs against the **Electron app** where feasible and covers import → activate → read → extract → convert-to-card → review → reschedule → search → backup, plus a **restart-app → verify-persistence** step proving data survives an app restart.
- [x] **T050 — Ship MVP as a local-first Electron desktop app** · done · _deps: T049_
  Done when: the app builds and runs as an Electron desktop app on macOS at minimum — SQLite persists in the app data directory, assets persist in the vault, backup works, the core loop works, the app survives restart, and no raw DB/filesystem APIs are exposed to the renderer; backup prompts and onboarding are polished; one person can use it daily for a week with no manual DB edits. (Pivot: ships as a desktop app, not a PWA.)

---

# Part II — Gold-standard application (T051–T100)

Goal: turn the useful MVP into a serious long-term system — imports, PDFs, capture, overload
management, semantic search, AI, media, reliability, scale — **while staying local-first**.

> **Direction (authoritative for Part II).** Interleave stays **local-first**: everything —
> import, reading, extraction, scheduling, review, search, AI — runs **on the device** against
> native SQLite + the filesystem asset vault. The server is **only an end-to-end-encrypted
> backup target**: it stores opaque archives + minimal metadata and **never** mirrors the domain
> model, replays the op-log, or processes content. There is **no live multi-device sync** — one
> canonical device + restore-to-a-fresh-install; two-way sync and conflict resolution are out of
> scope. The `operation_log` (built from day one) now powers **undo, audit, and incremental
> backup**, not server-side convergence. AI/semantic search run on-device (local model or the
> user's own API key) with an **optional, off-by-default** managed proxy.

## M11 — Backup & restore (local-first; server = encrypted backup only) (T051–T057)

> M11 collapses from the old "sync engine + server domain mirror + conflict resolution" into a
> small, low-risk **encrypted backup** milestone. Because nothing else depends on it, it no
> longer has to lead Part II — it can land whenever offsite durability is wanted.

- [ ] **T051 — Thin backup API skeleton** · _deps: T050_
  Done when: `apps/api` (Hono) exposes a SMALL typed surface — auth middleware, health checks, structured errors, and backup routes (`upload-archive`, `list-backups`, `download-archive`) — and the desktop app can call a typed endpoint in dev. It is **not** a domain RPC mirror: no element/card/extract endpoints, no `db.query`, no server-side domain logic.
- [ ] **T052 — Minimal server backup store** · _deps: T051_
  Done when: the server persists only what a blob backup needs — `users`, `devices`, and `backup_manifests` (id, user, device, created_at, size, content hash, encryption metadata, schema/app version, storage key) — plus the encrypted-archive bytes in object storage. The server schema does **not** mirror `elements`/`cards`/`documents`/etc.; it stores opaque ciphertext + metadata. Server migrations create a working DB.
- [ ] **T053 — Authentication** · _deps: T052_
  Done when: email/password or passkey-first auth identifies the user and gates their backups; self-host/personal mode remains possible. Auth protects *backup storage*, not live domain data (there is none on the server).
- [ ] **T054 — Incremental-backup design (op-log + asset manifest)** · _deps: T052_
  Done when: backup is designed around the local SQLite `operation_log` (introduced in T006/T008) + a content-hash asset manifest so a backup ships only what changed since the last one (new ops + new/changed asset bytes) rather than re-uploading everything. The op-log is the local changelog that makes backups incremental — it is **not** replayed into a server domain DB (no Postgres mirror, no Electric/PowerSync). Every local mutation already appends a deterministic op.
- [ ] **T055 — Encrypted backup & restore** · _deps: T054, T053_
  Done when: the desktop encrypts the SQLite DB + asset vault into an archive **client-side (end-to-end)** and uploads it to the server, and can restore it onto a fresh desktop install so the whole knowledge base (data + assets + lineage) comes back intact. The server only ever holds ciphertext. No multi-device conflict resolution (single canonical device).
- [ ] **T056 — Automatic backups, retention & integrity verification** · _deps: T055_
  Done when: backups can run automatically (scheduled / after-N-changes), old backups are pruned by a retention policy, every archive is integrity-verified (hash/manifest check), and a restore **drill** proves an archive restores cleanly — so backup is trustworthy and hands-off, not a manual chore. (Replaces the dropped two-way-sync task; there is no live sync.)
- [ ] **T057 — Backup encryption keys & recovery** · _deps: T055_
  Done when: the user's backup encryption is key-managed safely — a passphrase/key derives the archive encryption, recovery codes let a user restore on a new device, and re-keying is possible. Losing the key means losing the ability to restore (by design — the server cannot read the data), and the UI makes that consequence explicit. (Replaces the dropped conflict-UI task.)

## M12 — Local background runner, vault & web import (T058–T061)
Detailed specs: [`tasks/M12-runner-and-vault.md`](./tasks/M12-runner-and-vault.md) (T058–T059) · [`tasks/M12-web-import.md`](./tasks/M12-web-import.md) (T060–T061)

- [x] **T058 — Local background runner** · done · _deps: T050_
  Done when: an on-device background runner (an Electron utility process / `worker_threads` queue — **not** a server worker, **not** pg-boss) processes local jobs: URL fetch/snapshot, OCR, embeddings, AI calls, cleanup; the main process can enqueue a job and observe progress/completion. All work runs locally; nothing is sent to a server. (Re-scope: replaces the pg-boss/Postgres server worker — "everything is done locally".)
- [x] **T059 — Asset-vault scaling for large media** · done · _deps: T058_
  Done when: the filesystem asset vault robustly handles large binaries (PDFs, images, audio/video, snapshots) — streamed read/write, content-hash dedup, integrity checks, and orphan GC — all behind the typed `window.appApi`/`AssetRepository` seam. The vault is the canonical local store for assets; there is **no** app-facing S3 (object storage exists only inside the T052 backup server, holding encrypted archives). (Re-scope: replaces app-level S3 object storage.)
- [x] **T060 — Automatic URL import (local-first)** · done · _deps: T013, T015, T016, T047_
  Done when: URL import — run in the **Electron main process** — fetches the page, runs Readability, stores the original snapshot + cleaned HTML in the asset vault, converts to ProseMirror JSON, and creates an inbox source. (Re-scoped local-first off T058/T059 — see [`tasks/M12-web-import.md`](./tasks/M12-web-import.md).)
- [x] **T061 — Canonical URL & duplicate detection** · done · _deps: T060_
  Done when: URLs are normalized (tracking params removed), already-imported canonical URLs are detected against the local `sources` table, content hashes computed; re-importing prompts reuse-or-new-version. (Local-first — see [`tasks/M12-web-import.md`](./tasks/M12-web-import.md).)

## M13 — Browser extension (local-first capture) (T062–T063)
Detailed specs: [`tasks/M13-browser-extension.md`](./tasks/M13-browser-extension.md)

- [x] **T062 — Browser extension MVP** · done · _deps: T060_
  Done when: a Manifest V3 extension can "save page" / "save selection" / "save to inbox" via its service worker; the capture is POSTed to a token-protected `127.0.0.1` **loopback** server in the Electron main, which runs the M12 import pipeline. The extension never writes the SQLite DB directly and makes no cloud call. (Re-scoped local-first off the original T053 cloud-auth dep — see [`tasks/M13-browser-extension.md`](./tasks/M13-browser-extension.md).)
- [x] **T063 — Side-panel capture** · done · _deps: T062_
  Done when: the extension's Side Panel shows inbox/import UI beside the page and can save a selection with priority + reason, routed through the **same loopback capture path** (not direct DB writes).

## M14 — PDF / EPUB / document import (T064–T070)
Detailed specs: [`tasks/M14-pdf-ocr.md`](./tasks/M14-pdf-ocr.md) (T064–T066) · [`tasks/M14-formats.md`](./tasks/M14-formats.md) (T067–T070)

> Local-first: imported documents and their assets live in the **filesystem asset vault**
> (T059 scaling), never app-level S3; OCR/parsing run on the **local background runner** (T058),
> never a server worker.

- [x] **T064 — PDF import** · _deps: T059, T018_ · done
  Done when: PDF.js renders PDFs, extracts selectable text, tracks page read-points, and stores page-level source locations; PDF text extracts link to page numbers.
- [x] **T065 — PDF region extraction** · _deps: T064_ · done
  Done when: drawing a rectangle around a figure/table creates an image extract with page number + coordinates as its own scheduled topic.
- [x] **T066 — OCR fallback** · _deps: T064, T058_ · done
  Done when: OCR jobs run on the **local background runner** (on-device, e.g. Tesseract/WASM) and produce searchable/extractable text for scanned pages/images with confidence metadata attached to page/region (not blindly inserted into the body).
- [x] **T067 — EPUB import** · _deps: T059, T018_ · done
  Done when: EPUBs parse into book/chapter/section sources preserving chapters/headings/footnotes/locations; a chapter can be read incrementally.
- [x] **T068 — Markdown & HTML import/export** · _deps: T015_ · done
  Done when: Markdown and HTML are first-class imports preserving code/headings/links/images; exported Markdown round-trips back with acceptable fidelity.
- [x] **T069 — Highlight import (Readwise/Kindle-style)** · _deps: T012_ · done
  Done when: a generic highlight import format + adapters turn external highlights into inbox extracts (not active cards).
- [x] **T070 — Anki import/export** · _deps: T032_ · done
  Done when: cards export to Anki-compatible packages/CSV with source refs, and Anki cards import as card elements preserving review history when available.

## M15 — Rich media cards (T071–T075)
Detailed specs: [`tasks/M15-occlusion-formula.md`](./tasks/M15-occlusion-formula.md) (T071–T072) · [`tasks/M15-media.md`](./tasks/M15-media.md) (T073–T075)

> Local-first: image/video/audio bytes live in the **asset vault** (T059), transcoding/clipping
> runs on the **local background runner** (T058); no app-level S3, no server processing.

- [x] **T071 — Image occlusion** · _deps: T065_ · done
  Done when: image-occlusion cards generate from image extracts with masks/regions stored separately from the base image; one diagram yields multiple sibling cards.
- [x] **T072 — Formula & code cards** · _deps: T015, T032_ · done
  Done when: MathJax/LaTeX, syntax-highlighted code, and code-specific prompts render correctly in source/extract/review.
- [x] **T073 — Video import** · _deps: T059_ · done
  Done when: YouTube/local video metadata + transcript (if available) + timestamped read-points create video sources resumable from a saved timestamp.
- [x] **T074 — Video/audio clip extraction** · _deps: T073_ · done
  Done when: selecting start/end timestamps creates a scheduled `media_fragment` storing transcript segment + clip metadata + source timestamp.
- [x] **T075 — Audio review cards** · _deps: T074, T036_ · done
  Done when: audio prompt/answer/looped-fragment cards can be reviewed as active recall.

## M16 — Advanced scheduling & overload (T076–T082)
Detailed specs: [`tasks/M16-sort-overload.md`](./tasks/M16-sort-overload.md) (T076–T078) · [`tasks/M16-retention-sim.md`](./tasks/M16-retention-sim.md) (T079–T082)

- [x] **T076 — Advanced auto-sort** · _deps: T029, T036_ · done
  Done when: queue sorting uses a scoring function over priority, due date, retrievability, type, sibling spacing, concept diversity, and session mode.
- [x] **T077 — Auto-postpone** · _deps: T076_ · done
  Done when: when due load exceeds the daily budget, low-priority topics then low-priority mature cards are postponed first while high-priority fragile cards are protected.
- [x] **T078 — Catch-up & vacation modes** · _deps: T077_ · done
  Done when: catch-up recovers from backlog and vacation pre-adjusts future load, both showing the cost of postponement.
- [x] **T079 — Desired retention by priority/concept** · _deps: T036, T041_ · done
  Done when: retention targets can differ by concept or priority band.
- [x] **T080 — FSRS parameter optimization** · _deps: T036_ · done
  Done when: accumulated review history can optimize FSRS parameters per preset/concept, with suggested updates and a workload-impact preview.
- [x] **T081 — Workload simulation** · _deps: T080, T079_ · done
  Done when: the user can preview how load changes from altering desired retention, adding cards, or postponing low-priority material before committing.
- [x] **T082 — Mature-card retirement** · _deps: T036_ · done
  Done when: cards can be retired/archived/moved to very-low retention so low-value mature cards leave active review gracefully.

## M17 — Analytics, quality & maintenance (T083–T086)
Detailed specs: [`tasks/M17-analytics.md`](./tasks/M17-analytics.md) (T083–T084) · [`tasks/M17-quality.md`](./tasks/M17-quality.md) (T085–T086)

- [x] **T083 — Source-yield analytics** · _deps: T045_ · done
  Done when: each source shows read %, extracts/cards/mature-cards created, leeches, and time spent; low-yield sources are identifiable.
- [x] **T084 — Extract-stagnation analytics** · _deps: T045, T024_ · done
  Done when: extracts that keep returning without progressing are detected and surfaced with rewrite/convert/postpone/delete suggestions.
- [x] **T085 — Leech remediation workflow** · _deps: T040_ · done
  Done when: a repair screen offers split/add-context/open-source/back-to-extract/lower-priority/suspend/delete for repeated failures.
- [x] **T086 — Minimum-information-principle checks** · _deps: T035_ · done
  Done when: quality warnings extend to multiple facts, long lists, vague pronouns, unsupported claims, similar answers, no/outdated source, and oversized clozes.

## M18 — Semantic search & AI (T087–T095)
Detailed specs: [`tasks/M18-semantic.md`](./tasks/M18-semantic.md) (T087–T089) · [`tasks/M18-trust.md`](./tasks/M18-trust.md) (T090–T092) · [`tasks/M18-ai.md`](./tasks/M18-ai.md) (T093–T095)

> Local-first: embeddings and the vector index live **on-device** (e.g. `sqlite-vec` on the same
> better-sqlite3 DB); AI runs from the Electron main with a **local model or the user's own API
> key**. Your infrastructure is never in the loop by default. An **optional, off-by-default**
> managed AI proxy may route calls through the first-party server for convenience — enabling it
> must visibly disclose that content is sent to the server. AI output is always **drafts** until
> the user approves it.

- [x] **T087 — Semantic search (local)** · _deps: T058, T042_ · done
  Done when: embeddings for sources/extracts/cards are generated **on-device** (a local model via the background runner, or an embedding API called with the user's own key) and stored in a **local vector index** (e.g. `sqlite-vec` on the same better-sqlite3 DB); search finds conceptually related material without keyword match. (Re-scope: local vector store, **not** Postgres/pgvector.)
- [x] **T088 — Related-item suggestions** · _deps: T087_ · done
  Done when: each element shows similar extracts, possible duplicates, prerequisite concepts, and sibling sources.
- [x] **T089 — Contradiction detection** · _deps: T087_ · done
  Done when: semantic similarity + claim metadata flag possibly conflicting cards/extracts ("newer source conflicts with older card").
- [x] **T090 — Staleness & expiry** · _deps: T032_ · done
  Done when: `fact_stability`, `valid_from`, `valid_until`, `jurisdiction`, `software_version`, `review_by` let facts expire and trigger verification.
- [x] **T091 — Source-reliability metadata** · _deps: T043_ · done
  Done when: source type, author, date, primary/secondary/tertiary, confidence, and notes can show reliability/uncertainty on important cards.
- [x] **T092 — Verification tasks** · _deps: T090, T091_ · done
  Done when: scheduled `task` elements ("verify this claim", "find better source", "update outdated card", "check current version") keep time-sensitive knowledge from rotting.
- [x] **T093 — AI-assisted distillation (local-first)** · _deps: T058, T024_ · done
  Done when: AI actions (explain/simplify/suggest Q&A/suggest cloze/detect ambiguity/propose prerequisites/summarize) help formulation but never schedule unapproved cards (drafts only). AI calls run **from the Electron main with the user's own API key (or a local model via the background runner)** — your infrastructure is never in the loop by default; an **optional, off-by-default managed proxy** may route calls through the first-party server, disclosing that content is sent. (Re-scope: no server worker mediates AI by default.)
- [x] **T094 — AI source grounding** · _deps: T093_ · done
  Done when: every AI suggestion links back to selected source text and AI output is stored separately from source quotes.
- [x] **T095 — Incremental writing / synthesis notes** · _deps: T024, T028_ · done
  Done when: scheduled `synthesis_note` elements collect linked extracts/cards and return for refinement.

## M19 — Review modes, desktop & encryption (T096–T098)
Detailed specs: [`tasks/M19-review-modes.md`](./tasks/M19-review-modes.md) (T096) · T097 (Tauri) + T098 (E2E-encryption) are **out of scope** for the local-first program.

- [x] **T096 — Branch/subset/semantic review modes** · _deps: T087, T037_ · done
  Done when: review by concept, source, search query, branch, stale items, leeches, or random audit works outside normal scheduling.
- [ ] **T097 — Tauri shell (deprioritized — possible future alternative)** · _deps: T050_
  Deprioritized: the canonical desktop shell is **Electron** (`apps/desktop`, shipped in T050). Do **not** build both Electron and Tauri. This task is parked as a possible future alternative shell only; if ever revisited, a Tauri shell would reuse the same renderer, typed `window.appApi` surface, SQLite DB, and asset vault — native menus, global shortcuts, clipboard helpers, filesystem backups, and local media storage all already belong to the Electron shell.
- [ ] **T098 — Backup encryption hardening & audit** · _deps: T055_
  Done when: the end-to-end backup encryption (introduced in T055/T057) is hardened and audited — per-user keys, device recovery, key rotation, and a threat-model review — so a server compromise reveals only ciphertext. (Re-scope: encryption protects *backups*; there is no live sync to encrypt.)

## M20 — Scale & hardening (T099–T100)
Detailed specs: [`tasks/M20-maintenance.md`](./tasks/M20-maintenance.md) (T099) · [`tasks/M20-hardening.md`](./tasks/M20-hardening.md) (T100)

- [x] **T099 — Large-collection maintenance tools** · _deps: T044, T083_ · done
  Done when: dedup, orphan-media cleanup, broken-source reports, cards-without-sources, bulk low-priority postpone/archive, and DB integrity checks keep a 100k-element collection maintainable.
- [x] **T100 — Gold-standard QA & performance hardening** · _deps: T099, T096_ (T097 Tauri + T098 E2E-encryption are out of scope for the local-first program) · done
  Done when: load-tested at 100k cards / 100k extracts / thousands of sources / large PDFs / long histories, with indexes, rendering, search, queue calc, and backup/restore optimized so the app stays fast, safe, backed up, and searchable after years of use.

---

# Part III — Close the loops (T101–T134)

Goal: Parts I–II built the gold-standard *mechanics*; Part III makes them *behave*. The 2026-06
gap analysis ([`ideation/2026-06-09-gold-standard-incremental-reading-ideation.md`](./ideation/2026-06-09-gold-standard-incremental-reading-ideation.md))
found the system durably records nearly everything that matters — yield, postpone history, block
deferrals, staleness, per-concept retention targets — and feeds almost none of it back into
scheduling, routing, or user-visible accountability ("gold-standard sensors, MVP-grade
actuators"). Part III closes those loops. Almost every task consumes data already captured; new
surfaces are read models per the house pattern, not new product directions.

> **Direction (authoritative for Part III).**
>
> - **Receipts before automation.** The system may act on the user's behalf (trim a day, demote
>   an extract, propose retirement) only where a ledger + undo trail makes the action visible and
>   reversible. Read models land before or with the behavior they audit — that is why M22
>   precedes M23/M24.
> - **Value model first.** Anything keyed off "yield" uses the v2 value model (T104 — synthesis
>   output and honorable non-card fates count as productive), never the cards-only definition in
>   `packages/core/src/source-yield.ts`. T104 blocks T112 and T121 by design.
> - **Explainability bar.** Any scheduling change driven by learned signals must carry a one-line
>   UI reason ("returning sooner: last visit produced 6 extracts"). No unexplained interval
>   changes.
> - **House patterns hold.** Trusted-side read models behind typed IPC (never parallel analytics
>   tables); command-shaped transactional mutations appending `operation_log`; queue invariants
>   (backend-canonical eligibility, transactional schedule clears, undo preimages, new
>   drift-diagnostic cases per scheduling change); soft delete/undo over destruction; settle
>   surface ownership before building. Search `docs/solutions/` before each task.

## M21 — Honest exits & the value model (T101–T104)
Detailed specs: [`tasks/M21-honest-exits.md`](./tasks/M21-honest-exits.md)

- [x] **T101 — "Save for later" becomes parked, not dismissed** · _deps: T012, T044_
  Done when: the inbox "Save for later" verb writes a distinct parked state — today `keepForLater` writes `status: "dismissed"`, the same terminal status as Abandon, unreachable by any routing — and parked items are visible/filterable in the library with their parked date, still excluded from inbox/queue/daily-work, distinguishable from dismissed in data and UI, op-logged and undoable.
- [x] **T102 — Parked resurfacing sweep** · _deps: T101_
  Done when: parked items resurface on a schedule (default ~90 days) in a calm review surface offering keep-parked / schedule / let-go per item, with bulk apply and single-batch undo — "later" provably means later, not never.
- [x] **T103 — Proactive Done: surface `retirementSuggestion`** · _deps: T028, T083_
  Done when: the scheduler's already-computed `retirementSuggestion` (threaded through `SchedulerService`, currently consumed by nothing in `apps/web`) surfaces as a calm nudge on queue rows and in the reader — one tap into the existing DoneIntentMenu with Finished/Abandon prefilled — server-authoritative, dismissible, and dismissals remembered.
  Completed in the T103 final commit. Verification: `pnpm lint`, `pnpm typecheck`, `pnpm test`, and `pnpm e2e tests/electron/done-intent.spec.ts`.
- [x] **T104 — Value model v2: honorable non-card fates** · _deps: T024, T084, T095_
  Done when: extracts can terminate as reference / synthesized / done-without-card (distinct states, not delete), `synthesis_note` lineage counts as productive output in source-yield, and the stagnation detector, T084 suggestions, and analytics all respect the new fates — synthesis-driven reading stops being scored as failure. Prerequisite for every yield-keyed behavior (T112, T121).
  Completed in `feat: T104 value model v2`. Verification: `pnpm lint`, `pnpm typecheck`, `pnpm test`, and `pnpm e2e -- tests/electron/extract-review.spec.ts tests/electron/source-yield.spec.ts tests/electron/extract-stagnation.spec.ts`.

## M22 — Receipts: priority integrity & knowledge maturity (T105–T110)
Detailed specs: [`tasks/M22-receipts.md`](./tasks/M22-receipts.md)

- [ ] **T105 — Priority-integrity read model** · _deps: T045, T077_
  Done when: a typed read model reports, per priority band and per topic: due attention serviced vs deferred over a window, cumulative postpone debt, and band-share inflation (warning when one band dominates the collection) — computed from `review_logs` and `reschedule_element`/auto-postpone operation-log facts, no parallel tables, surfaced in analytics.
- [ ] **T106 — Chronic-postpone reckoning** · _deps: T105_
  Done when: items postponed ≥N times surface in a decision surface forcing an explicit keep / demote / done / delete per item — no further silent recession past the threshold without one decision — batched, undoable, op-logged, with new drift-diagnostic cases.
- [ ] **T107 — Fallow: deliberate topic rest** · _deps: T105_
  Done when: a topic can be rested to a chosen return date — distinct from postpone (no recession growth, excluded from missed-priority accounting) and from abandon (it provably returns) — visible on the topic, reversible, op-logged.
- [ ] **T108 — Topic knowledge-state read model** · _deps: T079, T083, T104_
  Done when: per topic/concept, a typed read model reports the funnel as stage-to-stage ratios (read → extracted → distilled → carded → mature), stability distribution, and measured retention trend vs the T079 target, and emits graduation events when thresholds cross.
- [ ] **T109 — Maturity surfaces** · _deps: T108_
  Done when: topic pages render the knowledge-state panel, analytics gains the concept-level retention view (replacing the deferred stub noted in `AnalyticsScreen.tsx`), and graduation events appear in the daily summary.
- [ ] **T110 — Weekly ledger & integrity session** · _deps: T106, T109_
  Done when: a weekly, dismissible session — itself a scheduled attention element — combines the week's ledger ("N sources → extracts → cards → matured; priorities missed by band") with the integrity sweep (T106 decisions, fallow suggestions, parked resurfacing when due): one ritual, not another dashboard to remember to visit.

## M23 — The adaptive attention scheduler (T111–T114)
Detailed specs: [`tasks/M23-adaptive-scheduler.md`](./tasks/M23-adaptive-scheduler.md)

- [ ] **T111 — Consume recency (`lastSeenAt`)** · _deps: T028, T076_
  Done when: `lastSeenAt` — currently marked "RESERVED — deliberately NOT consumed by `nextDueAt`" — feeds interval computation so untouched-but-due elements stop interleaving identically with just-processed ones; deterministic, unit-tested, with a drift-diagnostic case.
- [ ] **T112 — Yield-adaptive interval multiplier** · _deps: T104, T111_
  Done when: each source/extract carries a bounded per-element interval multiplier (≈×0.5–×4 of the band base) updated on every processed visit from v2 yield (extracts/statements/cards/synthesis produced, unresolved ratio) with priority modulating growth — replacing the two binary `adjustForSourceProcessing` branches; a 50-pass productive A-source and a barren one no longer share a cadence.
- [ ] **T113 — Schedule explainability** · _deps: T112_
  Done when: wherever a due date is shown (queue row, inspector), a learned interval change carries a structured one-line reason via `schedulerSignals`; no unexplained interval change reaches the UI.
- [ ] **T114 — Descendant-health input** · _deps: T112_
  Done when: descendant-card lapse rate feeds the multiplier (struggling descendants pull the parent source back sooner), capped and explained, with tests proving a lapsing cluster shortens the parent's return interval.

## M24 — Ambient, time-denominated overload (T115–T118)
Detailed specs: [`tasks/M24-ambient-overload.md`](./tasks/M24-ambient-overload.md)

- [ ] **T115 — Per-item time-cost model** · _deps: T037, T083_
  Done when: a typed read model estimates per-item minutes — cards from median graded response times in `review_logs` by card type; sources/extracts from recorded per-format time-spent — with explicit coarse defaults when history is thin, exposed for budget, gauge, and planner consumers.
- [ ] **T116 — Minutes-denominated daily budget** · _deps: T115_
  Done when: the daily budget is set in minutes (count setting migrated with a sensible conversion), the queue gauge projects today's real time cost, and over-budget detection compares minutes to minutes — one 6-second cloze and one 90-minute PDF pass stop costing the same "1 item".
- [ ] **T117 — Standing auto-postpone policy** · _deps: T058, T077, T105_
  Done when: an opt-in policy (off / suggest / automatic) runs the existing T077 planner main-side at day rollover so the user opens onto a within-budget day; every automatic run writes one `batchId`, a receipt line with one-tap undo appears in the daily summary, and the overload banner remains as the manual override. T105 is a hard dep: sacrifices are ledgered from day one.
- [ ] **T118 — Session assembly ("what fits in N minutes")** · _deps: T116_
  Done when: the user can request a session sized to N minutes; composition respects priority order, protection rules, and the T119 quota when present, and states what was left out and why.

## M25 — Extract-pipeline flow control (T119–T122)
Detailed specs: [`tasks/M25-flow-control.md`](./tasks/M25-flow-control.md)

- [ ] **T119 — Protected distillation quota** · _deps: T076, T115_
  Done when: day composition guarantees a configurable minimum share for distillation work (extract/statement processing) so conversion throughput never silently drops to zero under card load — the share is visible, and an overloaded fixture provably still surfaces distillation items.
- [ ] **T120 — Batch conversion sessions** · _deps: T024, T032, T093_
  Done when: a session view gathers card-ready atomic statements across sources for keyboard-first batch card authoring, with optional AI pre-drafts (existing `ai_suggestions` path, drafts-only invariant, explicit per-session consent); produced cards keep full lineage.
- [ ] **T121 — Extract aging policy** · _deps: T084, T104_
  Done when: extracts crossing an age/unproductive-returns threshold auto-demote to a recoverable T104 reference state via batched, op-logged, undoable sweeps — an opt-in policy with preview mirroring the auto-postpone receipt pattern — and age bands are visible wherever extracts list.
- [ ] **T122 — Shape-aware extract staging** · _deps: T021, T024_
  Done when: extract creation classifies shape, so card-ready captures (single sentences, definitions, facts) are born `atomic_statement` with a convert-now affordance instead of walking the hardcoded `raw_extract` ladder (three scheduled touches before a card is even possible); misclassification is one keystroke to correct; stage transitions stay op-logged.

## M26 — Lineage integrity (T123–T125)
Detailed specs: [`tasks/M26-lineage-integrity.md`](./tasks/M26-lineage-integrity.md)

- [ ] **T123 — Stale propagation through the lineage DAG** · _deps: T022, T090_
  Done when: when reconciliation marks source blocks `stale_after_edit`, the live downstream outputs (extracts → statements → cards anchored to those blocks) gain a queryable needs-reverify flag in the same transaction, and the counts appear in source progress and the Done-intent breakdown — cards built from corrected text stop circulating silently.
- [ ] **T124 — Re-verify workflow** · _deps: T123_
  Done when: flagged items resolve as confirm / rebase / detach (rebase re-anchors to current text, hash-diff assisted; detach freezes a provenance snapshot), batched per source and capped per session, op-logged and undoable; resolving clears the flag everywhere it shows.
- [ ] **T125 — Card-edit write barrier** · _deps: T038, T080_
  Done when: a substantive card edit (heuristic, always user-overridable) offers keep-schedule vs re-stabilize (demote to a short confirmation interval) — a rewritten card stops inheriting stability its old formulation earned — and the choice + edit linkage land on review logs so T080 optimization can exclude pre-edit history; in-flight review state stays uncorrupted (the original M7 rule's actual intent).

## M27 — Triage at scale (T126–T127)
Detailed specs: [`tasks/M27-triage-at-scale.md`](./tasks/M27-triage-at-scale.md)

- [ ] **T126 — Bulk inbox triage** · _deps: T012, T069, T099_
  Done when: the inbox supports multi-select and group-by (origin/domain/type) with keyboard-driven verbs + priority applied to a selection as one batched, op-logged operation with single undo — a 50-item morning (extension + URL + highlight-import feeders) is triageable in a few sweeps, using T101's parked semantics for Save-for-later.
- [ ] **T127 — Suggested priority & placement** · _deps: T083, T087, T088, T091_
  Done when: inbox items show a suggested band + topic placement chip with a one-line justification computed from existing signals (semantic neighbors, per-source yield history, source reliability) — accept-or-override, never auto-applied, deterministic-heuristic first.

## M28 — Lapse-driven re-reading (T128–T129)
Detailed specs: [`tasks/M28-lapse-rereading.md`](./tasks/M28-lapse-rereading.md)

- [ ] **T128 — Lapse-cluster detection** · _deps: T040, T083_
  Done when: a read model over `review_logs` joined to lineage anchors detects K-lapses-in-window clusters sharing an extract/source-region ancestor — thresholds tuned to stay rare — surfaced in maintenance and on the source page.
- [ ] **T129 — Re-read proposals** · _deps: T128_
  Done when: accepting a proposal schedules a re-read attention item targeting the exact source region (via anchors) that arrives with the failing cards attached for context; proposals are capped per week, cheap to dismiss, and dismissals are remembered.

## M29 — Long-form geometry & the re-entry payoff (T130–T134)
Detailed specs: [`tasks/M29-longform-geometry.md`](./tasks/M29-longform-geometry.md)

- [ ] **T130 — Source re-entry briefing** · _deps: T083_
  Done when: opening a scheduled source return renders a since-last-visit briefing — read %, new/deferred/stale block counts, descendant card performance, last extraction point — computed from existing block and yield rows, with one-click jump to the next unresolved block.
- [ ] **T131 — Honor `needs_later`** · _deps: T130_
  Done when: deferred blocks are reachable via a jump rail (listing `needs_later` and `stale_after_edit` blocks) and un-deferring/resolving updates the durable state — block deferral stops being write-only bookkeeping that is counted at exit and never seen again.
- [ ] **T132 — PDF block-state parity** · _deps: T064, T065_
  Done when: PDF sources carry durable per-page/per-region processing state (the existing 7-state vocabulary + reconciliation), feeding source progress, Done-intent breakdowns, yield, and the scheduler exactly as document blocks do — the heaviest formats stop being invisible to honest completion.
- [ ] **T133 — Media segment states** · _deps: T073, T074_
  Done when: audio/video sources track per-segment processed state (derived from playback and fragment extraction), feeding the same surfaces — "watched 40%, 2 segments deferred" is durable data, not memory.
- [ ] **T134 — Structural skim pass** · _deps: T067, T132_
  Done when: long-form sources (PDF outline/TOC, EPUB chapters, long documents by heading) support a skim pass assigning per-section verdicts — extract-worthy / later / ignore — that bulk-set block states (one batch, one undo) and create per-section scheduling, so deep reading starts where the value is instead of at page 1.

---

## Progress log

Record notable completions / decisions here as tasks land (newest first).

- 2026-06-11 - T102 Parked resurfacing sweep - done. Commit: `feat: T102 parked resurfacing sweep`. Adds typed `parkedResurfaceAfterDays` settings (default `90`, clamped `1..3650`) and a Settings row for the saved-for-later resurfacing window. `ParkedResurfacingQuery` reads live parked sources whose `parked_at` is at least the UTC-duration threshold old, returns age/priority context, and appends no `operation_log`; `ParkedResurfacingService` applies Keep / Queue now / Let go decisions by revalidating each id, writing existing `update_element` ops under one `batchId`, and skipping missing/deleted/non-source/non-parked/not-due rows. The trusted maintenance seam now exposes `parkedResurfacingCount`, `maintenance.parkedResurfacing`, and `maintenance.parkedResurfacingApply` through validated IPC, preload, and renderer `appApi` without any generic SQL/filesystem surface. The Maintenance hub shows a quiet Parked resurfacing card with per-row Keep / Queue / Let go segmented decisions and one batch apply with Snackbar undo. The maintenance fixture now plants old parked sources for all three verbs. Coverage includes local-db boundary/undo tests, settings/contract/preload/appApi tests, `MaintenanceScreen.test.tsx`, `DbService` report-shape coverage, and `tests/electron/maintenance.spec.ts` for count display, all three decisions, and undo restore. Verification: `pnpm lint`, `pnpm typecheck`, `pnpm test`, and `pnpm e2e tests/electron/maintenance.spec.ts`.
- 2026-06-11 - T101 Save for later becomes parked - done. Commit: `feat: T101 parked save-for-later state`. `keepForLater` now writes first-class `status = "parked"` with `parked_at`, clears `due_at`, and preserves Abandon/dismiss as `dismissed`. Migration `0030_parked_elements` widens the shared `elements`/`tasks` status CHECKs, adds nullable `elements.parked_at`, preserves dependent rows through the SQLite table rebuild, recreates FTS triggers, and intentionally leaves historical `dismissed` rows as dismissed because old data cannot distinguish saved-for-later from abandoned. Parked sources are visible in Library's Parked facet with parked date, and typed `library.parkedAction` moves them back to Inbox, queues them due now, or dismisses them through op-logged `update_element` writes with undo preimages. Parked rows are excluded from Inbox, Queue, Daily Work, and Workload baselines while remaining separately countable from dismissed rows. Coverage includes schema/migration tests, repository/service tests, renderer action/count/error/pending tests, preload/appApi/contract tests, DbService undo/rejection tests, and `tests/electron/parked-save-for-later.spec.ts` for park-from-inbox, Library visibility, restart persistence, and restore-to-inbox. Verification: `pnpm lint`, `pnpm typecheck`, `pnpm test`, and `pnpm e2e tests/electron/parked-save-for-later.spec.ts`.
- 2026-06-03 - T100 Gold-standard QA & performance hardening - done. **This is the LAST task — the roadmap (T001–T100) is now complete.** Closes the Done-when: the local-first app is LOAD-TESTED at ~100k cards / ~100k extracts / thousands of sources / ~1M review_logs, every hot read path MEASURED against an explicit p95 budget, the index gaps closed via migration `0027`, the long lists virtualized, and the Scale QA checklist green (backup/restore at scale, `PRAGMA integrity_check` ok, MVP flow green after app restart). T100 adds **NO product surface and NO new `window.appApi` command** — it MEASURES and HARDENS what's built. Per the M20 re-scope, T097 (Tauri) + T098 (E2E-encryption) are OUT OF SCOPE: the load matrix is the local-first Electron app only — **no Tauri, no multi-device sync, no E2EE; rows not blobs** (large PDFs stay a vault/runner concern). **(1) Large-collection seed harness (`packages/testing/src/large-seed.ts`, exported from the index)**: `seedLargeCollection(repos, db, opts)` builds a configurable collection (`{ sources, extractsPerSource, cardsPerExtract, reviewsPerCard, conceptCount, embeddings?, seed? }`) with a **default FULL profile reaching the scale matrix** (~1000 sources → ~100k extracts → ~100k cards → ~1M `review_logs`, ~201k elements, ~980 MB DB), deterministic from `seed` via a **locally re-implemented dependency-free `mulberry32`/`xmur3` PRNG** (NOT reaching into `packages/local-db` internals — option (b) per the spec). It builds **REAL, valid rows with full `source → source_location → extract → card` lineage** (priority spread A/B/C/D, `due_at` spread so a realistic fraction is due, `created_at` across a window so analytics scans hit real data, concept membership + tags seeded). The **one real feasibility knob — the op-log/transaction tradeoff — is handled honestly**: a documented **bulk-insert fast path** (batched `INSERT`s in one transaction, `synchronous = OFF`/`journal_mode = MEMORY` **on the throwaway temp bench DB ONLY**, NEVER the user/dev DB) writes the SAME row shapes the repositories produce, with a **smoke-sized control run through the real repository path** proving the bulk rows are schema-identical; the bench DB **intentionally skips per-row `operation_log`** (op-log throughput is exercised by the existing per-task transaction tests, not here). Returns `LargeSeedStats` (per-table row counts + wall-clock build time + on-disk DB size) for the bench provenance header. **(2) Benchmark with measurable budgets (`packages/local-db/bench/`)**: the HARD p95 gate lives in `scale-budget.test.ts` as a REAL `*.test.ts` (so `expect` failures propagate + a regression FAILS the process — a `*.bench.ts` body can't fail the run) run via a DEDICATED `vitest.bench-gate.config.ts` that includes ONLY `bench/`, so it is **NEVER collected by `pnpm test`** (the local-db project `include` is `src/**`); `bench-harness.ts` owns the throwaway-DB builder + the single exported `BENCH_BUDGETS_MS` constants; `scale.bench.ts` is the informational comparative table; `scripts/bench.mjs` (`pnpm bench`) runs the gate THEN the table. Each path seeds (or reuses) a large DB, runs the read N times, and asserts a **p95 wall-clock budget**: `QueueQuery.list` (default + concept-filtered — the T076 score-candidate cap + the N+1 batching prove out), `SearchRepository.search` (FTS5), `EmbeddingRepository.knn` (`vec0` over ~100k vectors, degrading to FTS-only when `vec0` unavailable), `ReviewSessionService.next` (FSRS next-pick + sibling bury), `AnalyticsService.computeAnalytics` (30-day window over ~1M `review_logs`), `MaintenanceQuery.report` (the T099 reports at scale), all HARD; `backupDatabaseTo` (the `wal_checkpoint` + `VACUUM INTO` snapshot primitive — NOT the full `apps/desktop` `BackupService` zip/manifest, which would drag an `apps/desktop` dep into the bench) as a SOFT ceiling (printed, never failed). **Reference-machine FULL-profile p95 (this commit, all PASS): queueList ~1.5 s, queueListFiltered ~1.5 s, search ~2 ms, semanticKnn ~52 ms, reviewNext ~0.45 s, analytics ~0.67 s, maintenanceReport ~0.69 s, backupSnapshot ~1.95 s (soft)** — seed builds in ~20 s via the bulk fast path. Budgets are these numbers + headroom, so the gate catches a REGRESSION back toward the old per-row N+1, not the (now-fixed) baseline. **(3) Index audit + migration `0027` (`packages/db/drizzle/0027_nappy_marvel_zombies.sql` + the journal + `migration-0027.test.ts`)**: additive **`CREATE INDEX` only** — no column/table change, no backfill, fully backward-compatible / restore-safe (a pre-`0027` backup restores cleanly, migrations run forward). Adds the two gaps the bench + `EXPLAIN QUERY PLAN` PROVED needed: **`elements_type_created_idx (type, created_at)`** (the `AnalyticsService` "new X in window" scans had no `created_at` index) and **`elements_deleted_at_idx (deleted_at)`** (the trash list + analytics `deletions` count + live-row filters scan `deleted_at IS [NOT] NULL`). The speculative **`elements(type, due_at)`** was the explicitly least-certain candidate — the bench showed `dueAttentionItems` already uses `elements_due_idx` well, so per the "prove it slow first" rule it was **NOT added**. `migration-0027.test.ts` asserts `0027` creates exactly those indexes and `PRAGMA integrity_check` stays `ok`. **(4) Row virtualization (NET-NEW, not a confirm — there was no windowing primitive in `apps/web` before)**: adds `@tanstack/react-virtual` + a reusable `apps/web/src/components/VirtualList.tsx` (`AutoVirtualList`) wired into all four long-list screens — the **queue** (`QueueScreen`), **library/search** (`LibraryScreen`), **trash** (`TrashScreen`), and the **maintenance reports** (`MaintenanceScreen`, three lists) — so a 10k-row payload renders a BOUNDED number of DOM rows without freezing; the load-bearing mitigation already held (the IPC reads stay paginated/limited — `QueueQuery.list`'s `limit`, `SearchRepository.search`'s `limit`, the T096 `MAX_REVIEW_MODE_DECK = 500` cap — so the renderer never receives an unbounded list). `VirtualList.test.tsx` asserts a large mocked payload renders bounded DOM rows. **(5) Scale QA checklist + `tests/electron/scale-smoke.spec.ts`** at a CI-BOUNDED N (a few thousand elements, NOT 100k in CI): backup → valid zip → manifest hashes verify → backed-up DB row counts match the source (the WAL-checkpoint consistency check) + the T047 restore-mechanics contract; `PRAGMA integrity_check` + `foreign_key_check` `ok` on the large seed AND after `0027`; the MVP flow green against a larger-than-demo seed incl. **restart-and-verify-persistence**; the two-scheduler split intact under load (FSRS cards by `review_states.due_at` AND attention items by `elements.due_at`, no collapse/starvation); FTS + `vec0` return the known seeded hits at scale. **Honest CI-vs-LOCAL split (documented in the bench header + `scripts/bench.mjs`)**: `pnpm bench` defaults to the **SMOKE** profile (a few-thousand N, finishes in seconds — what every PR runs); `INTERLEAVE_BENCH_N=full pnpm bench` is the **opt-in / LOCAL** full ~100k run (minutes + ~1 GB temp disk). The full bench is NOT wired into `pnpm test`. **All measurement + seeding stays in `packages/testing` / `packages/local-db` / the Electron main, behind the IPC boundary — NO raw DB/filesystem to the renderer, NO generic `db.query`; the FSRS-vs-attention split + the `card → extract → source_location → source` lineage are preserved under load.** `pnpm typecheck` (13/13) + `pnpm lint` (695 files, 32 warnings) + `pnpm test` (2255 tests, 208 files) all green; `pnpm bench` (full profile) — all 8 hot paths within p95 budget. **The roadmap T001–T100 is complete; no further milestone spec is generated.**
- 2026-06-03 - T099 Large-collection maintenance tools - done. Closes the Done-when: dedup, orphan-media cleanup, broken-source reports, cards-without-sources, bulk low-priority postpone/archive, and DB+vault integrity checks now keep a 100k-element collection maintainable, from ONE calm **Maintenance** view. T099 is a **COMPOSITION task**: it wires the maintenance primitives already built across the roadmap (T044 two-stage delete + `UndoService`, T059 vault `verifyIntegrity`/`findOrphans`/`collectOrphans`, T061 source dedup, T082 retirement, T087 `pruneOrphanVectors`, T083 source-yield) into one report+action surface — **NO migration, NO new element status, NO new op type, NO new distillation stage** (the closed 15-op set is unchanged). Every report below is covered by an EXISTING index (`elements_source_idx`, `cards_source_location_idx`, `sources_canonical_url_idx`, the asset reference set); the 100k index audit + load-test harness are deferred to **T100**. **All dedup detection, integrity scanning, and cleanup logic are domain / main-process functions — never React.** Reports are READ-ONLY (append NO `operation_log` row); cleanup mutations run in ONE transaction, append an `operation_log` row, and are **soft-delete by default / undoable** — the ONLY hard deletes stay `TrashRepository.purge`/`emptyTrash` (elements) and `AssetVaultService.collectOrphans` (vault files), both already guarded. **Two new READ-ONLY local-db queries**: **`DedupReportQuery` (`packages/local-db/src/dedup-report-query.ts`)** is the collection-wide ROLLUP sibling of T061's per-candidate `SourceDedupQuery` — it groups the WHOLE live library into duplicate CLUSTERS (sources by canonical URL over `sources_canonical_url_idx`, backstopped by the cleaned-HTML snapshot `assets.content_hash`, a both-matched source counted ONCE with the URL key winning; cards by a normalized `prompt+answer+cloze`+title key read from the DEDICATED `cards` columns, NOT a `document_blocks` join that would silently degrade to title-only; extracts by the same key over the `documents.plain_text` mirror). The `normalizeContentKey` is lower-cased / trimmed / whitespace-collapsed and clusters ONLY on a BYTE-IDENTICAL exact match — **false positives are FORBIDDEN** (a missed near-duplicate is fine; clustering two genuinely-different items is not; KNN/semantic dedup is a deliberate non-goal), degrading to a title-only key for an empty body — still exact, still safe. ONE pure keeper rule per `matchedBy` path (sources → newest `accessed_at`; cards/extracts → oldest creation, the canonical kept). **`LineageGapQuery` (`packages/local-db/src/lineage-gap-query.ts`)** owns the three lineage/value scans, all read-only and NEVER auto-deleting (lineage is sacred — a sourceless card may be deliberately hand-authored): **`cardsWithoutSources`** (live `card`s with `cards.source_location_id IS NULL` AND no `derived_from` ancestor resolving to a live `source` — the cheap `elements.source_id` first check + the authoritative lineage-root walk; surfaced as a GAP for the user to fix or trash), **`brokenSourceCandidates`** (the SQL candidate set — live `source`s + each one's snapshot `assets` rows + `expectsSnapshot`/`hasSnapshotRow` flags — for the main-side disk join), and **`lowValueCandidates`** (live LOW-priority C/D-band via `priorityToLabel`, stale, ranked lowest-value-first — the bulk postpone/archive target set, honoring "don't let new material dominate older high-value material"). **`BulkActionService` (`packages/local-db/src/bulk-action-service.ts`)** is the only genuinely-new WRITE path, and it is a thin BATCH WRAPPER over EXISTING per-item verbs sharing ONE `batchId` so `UndoService.undoLast` reverses the WHOLE sweep in one call: `bulkSoftDelete` (`soft_delete_element` per id, carrying the `batchId` + status pre-image), `bulkArchive(mode)` routing each id through one of three EXISTING reversible verbs — `trash` → soft-delete, `dismiss` → `update_element` status `dismissed`, `retire` → `CardRetirementService.retire` (**CARDS ONLY** — a non-card is skipped, honoring the two-scheduler split: retirement is an FSRS-card attribute) — minting NO `archived` status, and `bulkPostpone` delegating to the EXISTING `QueueActionService.bulkPostpone` (cards defer on FSRS, attention items reschedule on the attention scheduler — the split is already correct there). Each per-id op is its own transaction; a missing / already-deleted / inapplicable id is SKIPPED (never an error — the report may be slightly stale). **`MaintenanceService` (`apps/desktop/src/main/maintenance-service.ts`)** is the main-process composer (the `BackupService` twin — the reports needing BOTH SQL and the filesystem live HERE, never in React): `report()` is the hub rollup (every report's COUNT + the integrity-not-yet-run flag — the deep integrity check is on-demand only, never auto-run on view open since it can take seconds on a 100k DB); `duplicates()`/`cardsWithoutSources()`/`lowValueCandidates()` proxy the local-db queries; `brokenSources()` JOINS the `LineageGapQuery` candidate set against `AssetVaultService.verifyIntegrity().missing` → `{ source, reason: "missingFile" | "noSnapshot", missingAssetIds }`, where `noSnapshot` fires ONLY for a source whose own metadata RECORDED a snapshot (`sources.snapshot_key`) so a manual source that legitimately never captured one (its content in `documents`) is NEVER a false positive; `checkIntegrity(deep)` composes `PRAGMA quick_check`/`integrity_check` + `foreign_key_check` (read-only) with the vault `verifyIntegrity`; `dedupeCleanup({ removeIds })` RE-VALIDATES each id against a FRESH dedup report (never trusts a stale renderer id, never trashes a keeper) before the single-batch soft-delete; `orphanMediaCleanup({ confirm: true })` runs the EXISTING confirmed vault GC THEN `EmbeddingRepository.pruneOrphanVectors()` so the semantic index can't drift; `bulkTrash`/`bulkArchive`/`bulkPostpone` are the thin `BulkActionService` wrappers, each one undoable batch. **Typed seam**: a new `maintenance.*` group — `maintenance:report`/`duplicates`/`cardsWithoutSources`/`brokenSources`/`integrity`/`lowValue`/`dedupe`/`orphanMedia`/`bulkTrash`/`bulkArchive`/`bulkPostpone` — with zod request/result schemas (`channels.ts`/`contract.ts`/`contract.test.ts`), threaded `ipc.ts` → `db-service.ts` → `preload` → `apps/web/src/lib/appApi.ts`; the IPC-safe `MaintenanceRef`/`BrokenSourceRow` carry ids/titles/priority/reasons only (NO asset ids as request inputs, NO raw vault paths, NO generic `db.query`). **Renderer (`apps/web/src/maintenance/MaintenanceScreen.tsx` + `maintenance.css`)**: the single Maintenance view (reached from the `screen-analytics` "Open maintenance" affordance) shows the report cards with COUNT badges, each drilling into its cluster/row list with a CONFIRMABLE cleanup action that fires the typed `appApi.maintenance.*` intent and surfaces the T044 undo `Snackbar` (one "Undo" per `batchId`); the deep integrity check is a "Run check" button (never auto-run); it reuses `Metric`/`Banner`/`EmptyState`/`Status`/`TypeIcon`/the confirm dialog. The renderer sends validated intents and renders what main computed — it touches NO SQL, NO filesystem, NO vault. **Tests**: `dedup-report-query.test.ts` (the cluster grouping per type, the byte-identical exact-match key + the title-only degrade + the false-positives-forbidden contract, the both-matched-counted-once + the keeper rule), `lineage-gap-query.test.ts` (the sourceless-card `source_location_id`+`derived_from`-ancestor gap detection, the broken-source candidate set + `expectsSnapshot`/`hasSnapshotRow` flags, the low-value C/D-band stale ranking), `bulk-action-service.test.ts` (the shared-`batchId` batches, the three archive verbs incl. the non-card `retire` skip, the missing/deleted-id skip, the postpone delegation, the FSRS-vs-attention split), `maintenance-service.test.ts` (the hub rollup counts, the broken-source disk join + the `noSnapshot`-only-when-expected guard, `dedupeCleanup`'s fresh-report re-validation rejecting a stale/keeper id, `orphanMediaCleanup`'s GC-then-prune ordering + the `confirm: true` guard, the integrity composition), `MaintenanceScreen.test.tsx` (the report cards / drill-downs / confirm-then-act / undo-snackbar wiring + the on-demand integrity run), `contract.test.ts` + `db-service.test.ts` (the eleven-channel zod schemas + wiring), and `tests/electron/maintenance.spec.ts` (open Maintenance, run each report, dedupe + bulk-archive + orphan-media cleanup, run the integrity check, confirm UNDO restores a bulk batch, and the cleaned state SURVIVES an app restart). `pnpm typecheck` (13/13) + `pnpm lint` (682 files) + `pnpm test` (2240 tests, 205 files) all green.
- 2026-06-03 - T096 Branch/subset/semantic review modes - done. Closes the Done-when: review by **concept, source, search query, branch, stale items, leeches, or random audit** now works OUTSIDE normal scheduling. The whole point is the DROPPED due filter: where the daily session (T037) surfaces only cards whose FSRS `review_states.due_at ≤ now`, a review mode reviews a CHOSEN SUBSET REGARDLESS of due date (a not-yet-due card is still selectable); everything else about a review is UNCHANGED — grading still writes a durable `review_logs` row and advances FSRS through the existing `review.grade` path. **No migration, no new table, no new op type, no new element status** — this is a pure read seam plus a renderer entry, composed entirely from existing selection seams. Review modes are **CARDS ONLY** (the two-scheduler split holds: FSRS = cards; attention = sources/topics/extracts), and the deck keeps EVERY non-due guard from the daily session — live, `card`-typed, not soft-deleted, not in an excluded status (`deleted`/`suspended`), and **not retired** (T082 `cards.is_retired = false`). **Vocabulary in `@interleave/core` (`review-mode.ts`), framework-free** (like `task.ts`/`ai.ts`): the closed `REVIEW_MODE_KINDS` tuple — `concept`/`source`/`branch`/`search`/`semantic`/`stale`/`leech`/`random` (+ `isReviewModeKind`, `REVIEW_MODE_LABEL`, defensive `reviewModeLabel` → "Review" fallback) — is the SINGLE source of truth for BOTH the domain union AND the IPC `ReviewModeSelectorSchema`, so DB validation and the domain can't drift. The discriminated `ReviewModeSelector` pairs each `kind` with exactly one parameter (`conceptId`/`sourceId`/`rootId`/`query`/`{size, seed?}`); `MAX_REVIEW_MODE_DECK = 500` caps any deck (T100 load-tests this) and `DEFAULT_RANDOM_AUDIT_SIZE = 20`. **`ReviewModeService` (`packages/local-db/src/review-mode-service.ts`) — read-only, synchronous, appends NOTHING to the operation log**: `deck(selector, now, semantic?)` resolves a selector to an ORDERED list of live card ids + `total` (the full underlying pool BEFORE any cap, so the UI can say "first N of M") + `label` + `truncated`; `count(...)` is the cheap entry-affordance count over the SAME ordered pool (so count and deck always agree). It COMPOSES the existing seams, never re-queries them: `concept` → `ConceptRepository.elementsForConcept`; `source` → live `card` elements with `elements.source_id = sourceId`; `branch` → `LineageQuery.get` subtree nodes kept where `type === "card"`; `search` → `SearchRepository.search` ranked card hits; `semantic` → `SemanticSearchRepository.search` (FTS+vec fusion), DEGRADING to the keyword resolver when semantics are off / `vec0` unavailable / no query vector — never an error; `stale` → the T090 lifetime prefilter (`valid_until IS NOT NULL OR review_by IS NOT NULL`, cheap via `cards_review_by_idx`) + `deriveExpiryStatus` keeping the not-fresh cards, ordered most-overdue-first (`expired` before `due_for_review`, then `review_by` asc); `leech` → `ReviewRepository.listLeechCards` (already most-lapsed-first), re-applying the deck guards while preserving order; `random` → a SEEDED (mulberry32/xmur3, pure dependency-free) Fisher–Yates shuffle of the full live pool, the seed carried in the descriptor (NOT persisted) so a re-read reproduces the SAME sample. Concept/source/branch order priority-desc then creation order (high-value first); search/semantic/leech PRESERVE rank order. The `semantic` query VECTOR is computed by the DB service (the embed runs in the runner, not here) and INJECTED via the `SemanticResolveContext` hook — the service never embeds. Re-exported from `packages/local-db/src/index.ts`. **Typed seam**: two read-only channels on the existing `review.*` group — `review:mode:deck` + `review:mode:count` — with zod request/result schemas (`ReviewModeSelectorSchema` discriminated-union-mirrors the core type; query `≤512`; `random.size` bounded), threaded `channels.ts`/`contract.ts`/`contract.test.ts` → `ipc.ts` → `db-service.ts` (which resolves the semantic vector + `semanticSearchEnabled`/`vec0` availability and passes the deck's card ids through the SAME reveal-ready `review.preview` projection) → `preload` → `apps/web/src/lib/appApi.ts`. NO generic `db.query`; the renderer never selects a deck or grades directly. **Renderer (`apps/web/src/review/`)**: `ReviewScreen` gains a TARGETED mode (driven by a loose `mode` search param) — it fetches the FROZEN mode deck ONCE on mount, renders a calm mode-header chip + subset size, walks the deck by index, and reveals + grades through the UNCHANGED `review.preview`/`review.grade` (a mode card grades exactly like a daily card); a `truncated` deck shows the honest "first N of M" note, an empty subset shows the calm targeted empty state, and with NO `mode` the daily due session runs unchanged (no regression — falls back to `review.sessionNext`). `ReviewModeButton` is the reusable entry affordance (shows the `count`, launches the mode) wired into the Inspector (concept/source/branch), `ConceptsScreen`, `LibraryScreen`, `LeechRemediation` (leeches), and `HomeScreen` (stale / random audit). The renderer sends validated intents and renders what main computed. **Tests**: `review-mode.test.ts` (the tuple/guards/labels incl. the defensive fallback + selector shape); `review-mode-service.test.ts` (each resolver drops the due filter while keeping the live/non-retired/non-excluded guards; concept/source/branch priority ordering; search/semantic/leech rank preservation; semantic degrade-to-keyword when off/no-vector; stale most-overdue-first via `deriveExpiryStatus`; the seeded-random reproducibility + the `total`-is-full-pool / `size`-caps-deck distinction; the `truncated` flag at the cap; NO `operation_log` / NO `review_states` write); `ReviewModeScreen.test.tsx` + `ReviewModeButton.test.tsx` (the renderer fetches the frozen deck once, walks + grades via the unchanged path, the truncation honesty note, the targeted empty state, the no-regression daily fallback); `contract.test.ts` + `db-service.test.ts` (the two-channel zod schemas + the semantic-vector resolve + the reveal-ready projection wiring); and `tests/electron/review-modes.spec.ts` (launch a concept/source/branch/stale/leech/random mode, review a NOT-YET-DUE card outside the daily queue, confirm the grade writes a `review_logs` row + advances FSRS, and the mode entry + the graded state SURVIVE an app restart). `pnpm typecheck` (13/13, forced fresh) + `pnpm lint` (670 files) + `pnpm test` (2194 tests, 200 files) all green.
- 2026-06-02 - T095 Incremental writing / synthesis notes - done. Closes the Done-when: scheduled `synthesis_note` elements now COLLECT linked extracts/cards and RETURN for refinement. **Crucially: no new table, no new element type, no new op type, no migration.** A synthesis note IS the existing core `synthesis_note`-type `Element` (stage `synthesis`) — the "incremental writing" counterpart to incremental reading: a long-lived writing surface that collects material via explicit **`references` edges** (NOT `derived_from` — the collected extracts/cards stay REFERENCED material, keeping their own `card → extract → source` lineage intact) and is **scheduled on the ATTENTION scheduler, never FSRS** (it is processed, not recalled; a test asserts no `review_states` row is ever written). **`packages/local-db/src/synthesis-service.ts`** is the transactional composition seam: `create` writes the element (`create_element`) + optional 1:1 `documents` body (`update_document`) on ONE tx; `linkElement`/`unlinkElement` add/remove the `references` edge (`add_relation`/`remove_relation`, idempotent, rejects self-reference + non-extract/card targets); `editBody` upserts the ProseMirror body preserving stable block ids; `scheduleReturn` reuses `SchedulerService.scheduleAt` (`reschedule_element`, status → `scheduled`); `delete` soft-deletes. The read (`SynthesisData`) drops soft-deleted targets out of the panel and resolves due date from `elements.due_at`. Re-exported from `packages/local-db/src/index.ts`. **`synthesis.*` window.appApi seam** (contract + channels + preload + ipc + db-service) is fully validated end-to-end; renderer never instantiates the service. **Renderer (`apps/web/src/synthesis/`)**: `SynthesisCreate`, `SynthesisNote` (the writing surface + linked-material panel + schedule-return controls), `AddToNote` (collect-into-note affordance), wired into the router + nav + `BrowseScreen`. **Tests**: `synthesis-service.test.ts` (transactional invariants, attention-not-FSRS, idempotent link, lineage preservation), `db-service.test.ts`, `contract.test.ts`, plus the `synthesis-notes.spec.ts` Electron e2e (create → collect → schedule → survives restart). Green: typecheck + biome (662 files) + 2155 vitest tests.
- 2026-06-02 - T094 AI source grounding - done. Closes the Done-when: every AI suggestion now LINKS BACK to the selected source text, and the model's output is stored separately from the source quote. The **storage separation was already baked in at T093** — the `ai_suggestions` row keeps the VERBATIM source quote in `selected_text` (+ `source_element_id` / `source_block_ids` / `start_offset` / `end_offset` grounding) in a DIFFERENT column from the model's generated `suggestion_text`, and `AiRequest.sourceText` (the grounding) is distinct from the generated `text` — so T094 adds **no migration, no new op type, no schema change**; it makes the grounding ACTIONABLE (jump-to-source) and proves the **inheritance** end-to-end. **Repository (`packages/local-db/src/ai-suggestion-repository.ts`)**: a new `groundingLocationFor(repos, suggestionId)` resolves a suggestion's grounding span to a framework-free `AiGroundingLocation` — `{ sourceElementId, ordered stable blockIds (scroll target = first), startOffset, endOffset, selectedText (the verbatim quote), label (best-effort "¶ 3", `null` when underivable) }` — the jump-to-source target the drafts panel needs to wire an in-app "jump to source" on a draft's refblock EXACTLY like an extract/card. It **degrades to `null` (no jump affordance)** when the span has no block ids OR its source is gone/soft-deleted (the orphan case — the refblock still shows the calm quote but offers no DEAD jump), mirroring `groundingFor`'s orphan degradation; the existing `SourceRef` resolution is refactored behind a shared `resolveGroundingRef` seam. `AiGroundingLocation` is re-exported from `packages/local-db/src/index.ts`. **Main seam (`db-service.ts` `listAiSuggestions`)**: each suggestion now also carries a `groundingLocation: LocationSummary | null` — the resolved span mapped to the IPC `LocationSummary` (AI grounding has no PDF page / region / media clip, so those degrade to `null`); `AiSuggestionView` (`contract.ts`) + the renderer `AiSuggestionView` / new `AiGroundingLocation` (`apps/web/src/lib/appApi.ts`) gain the field. NO vectors / keys / raw provider internals cross IPC; no generic `db.query`. **Renderer (`AiAssist.tsx`)**: each draft's grounding `RefBlock` now wires `onOpenSource` via the shared `useNavigateToLocation` hook (a `locationFromGrounding` adapter fills the AI-irrelevant `page`/`region`/`clip`/`timestampMs` as `null`), so clicking the refblock JUMPS to the originating block in the reader — and the affordance is **omitted (not dead) for an orphan draft** whose grounding resolves no source. **Inheritance proven**: the explicit approve (`AiService.approveCard` → `CardService.createDraftFromSuggestion`, from T093) already mints the parked `card_draft` with the grounding as a real `source_locations` row + a `derived_from` edge — the e2e now ASSERTS the minted card's refblock resolves the SAME source element + the verbatim quote and that the lineage `card → source location → source` survives an app restart. Tests: `ai-suggestion-repository.test.ts` (`groundingLocationFor` resolves the source element + block ids + offsets + label; returns `null` for the soft-deleted-source orphan; the approve inherits the grounding as a `source_locations` row + `derived_from` edge so the card resolves the SAME `SourceRef`); `db-service.test.ts` (`listAiSuggestions` maps `groundingLocation` to a jump-to-source `LocationSummary` on the originating block; maps to `null` on the soft-deleted orphan); a new `AiAssist.test.tsx` (renders a working jump affordance routing to the grounding source's reader on the originating block; omits the affordance for an orphan draft; degrades the AI-irrelevant location fields to `null` on jump); `ai-service.test.ts`; and the extended `tests/electron/ai-distillation.spec.ts` (the `INTERLEAVE_AI_FAKE` seam — suggest-Q&A produces a grounded draft whose refblock's "jump to source" scrolls/flashes `blk_intro_p1`, then approve mints a parked `card_draft` whose inherited refblock resolves the seeded source + quote and SURVIVES an app restart). `pnpm typecheck` (13/13) + `pnpm lint` (655 files) + `pnpm test` (2134 tests) all green.
- 2026-06-02 - T093 AI-assisted distillation - done. The seven AI formulation actions (explain / simplify / suggest Q&A / suggest cloze / detect ambiguity / propose prerequisites / summarize) now help a user FORMULATE from a selected source span — and **never schedule an unapproved card**: AI output is ALWAYS an inert DRAFT, off by default, computed on-device from the Electron main with the **user's own API key (or a reserved local model)**, with no first-party server in the loop by default. **Framework-free contract (`@interleave/core/ai.ts`, dependency-free like `job.ts`)**: the closed `AI_ACTION_TYPES` / `AI_PROVIDER_KINDS` (`local`/`anthropic`/`openai`/`managed_proxy`) / `AI_SUGGESTION_KINDS` (`text`/`card_qa`/`card_cloze`/`prerequisite_list`) / `AI_SUGGESTION_STATUSES` (`draft`/`approved`/`dismissed`) tuples are the SINGLE source of truth for both the domain unions AND the `ai_suggestions` CHECKs; the narrow provider seam `AiProvider.complete(request, signal)` is the ONLY method that calls a model (so the local-model / own-key / managed-proxy choice is swappable + trivially mockable — tests inject a `FakeAiProvider` returning a canned suggestion, no model, no network); `AiRequest` carries the VERBATIM `sourceText` (the grounding) distinct from the model's generated `text` (T094 separation baked in from day one); `DraftCard` is an inert `qa`/`cloze` shape; typed errors `AiDisabledError` / `AiProviderError` / `AiProxyUnavailableError` make an off/unavailable provider degrade to a calm disabled state, never a crash. **Settings (off by default)**: `AppSettings` gains `aiEnabled` (default **false**), `aiProviderKind`, `aiManagedProxyEnabled` (default false), `aiModelDownloaded`, `aiLocalModelId`, and the MAIN-SIDE secret `aiApiKey` — which is STRIPPED from the renderer projection (`RendererSettings` omits `aiApiKey`/`embeddingApiKey`, exposing only `aiKeyConfigured: boolean`) so a plaintext key NEVER crosses the IPC boundary. **Persistence**: migration `0026` adds the `ai_suggestions` table (`packages/db/src/schema/ai.ts`) — `{ id, owning_element_id → elements (cascade), action, kind, provider_kind, suggestion_text (the MODEL output), cards (JSON DraftCard[]), + grounding: source_element_id → elements (set null), source_block_ids (JSON), start/end_offset, selected_text (the VERBATIM source quote, stored in a DIFFERENT column from suggestion_text), status, created_at }` with `owning`/`status` indexes and the three core-tuple CHECKs. An `ai_suggestions` row is transient draft/infra — it appends **NO `operation_log` op** (like a `jobs` / `ocr_pages` row); the logged mutation is the explicit approve's normal `create_element`/`create_card` path. `AiSuggestionRepository` (`packages/local-db`) owns `create`/`listForElement` (newest-first, drafts)/`findById`/`softDismiss`/`setStatusWithin` and JSON-round-trips the cards + grounding. **AI runs on the T058 background runner, off-main, secrets via fork-env (NOT the persisted payload)**: the DB-FREE `utilityProcess` worker (`apps/desktop/src/worker/ai-providers.ts`) owns the model/network compute ONLY — the user's key + provider + model dir reach it via `INTERLEAVE_AI_API_KEY` / `INTERLEAVE_AI_PROVIDER` / `INTERLEAVE_AI_MODEL_DIR` baked into the long-lived worker at construction (the same secret-handling discipline as `INTERLEAVE_ASSETS_DIR`), so **no key ever lands in a restart-safe `jobs` row**; changing the key calls `JobRunner.restartWorker(...)` to re-fork (gated on idle). The **own-key Anthropic/OpenAI providers** are the recommended WORKING path (a plain HTTPS call → `buildSuggestion` best-effort-parses the JSON card, degrading a parse failure to a plain-text draft — still never an active card); the **`LocalModelProvider`** (pinned to `node-llama-cpp` running `Llama-3.2-3B-Instruct` Q4_K_M GGUF) and the **`ManagedProxyProvider`** are RESERVED STUBS that throw a typed error until their integration lands — the drafts-only + off-by-default + degrade-gracefully invariants fully contain the residual risk. A deterministic `FakeAiProvider` is injected via the `INTERLEAVE_AI_FAKE=1` E2E seam (mirroring the `INTERLEAVE_*_IMPORT_PATH` escapes). **Main-side orchestrator** `AiService` (`apps/desktop/src/main/ai-service.ts`, the OcrService/EmbeddingService twin, construction-injected `{ repositories, getRunner, getSettings, getCardService }`): `enqueue` throws `AiDisabledError` when `aiEnabled` is false then enqueues the NON-SECRET `ai` job payload (action + provider kind + request + grounding — no key); `applyResult` (the runner's `ai` apply handler) persists ONE inert draft row with the grounding + runs `evaluateCardQuality` on any card draft, returning a renderer-safe summary (NO key, NO raw provider internals); `approveCard` is the user's EXPLICIT approve — it mints a **PARKED, un-due `card_draft`** via the new DRAFT-ONLY `CardService.createDraftFromSuggestion` seam (NOT the due-now `createFromExtract` — the card is NOT activated, NOT first-scheduled, NOT in the FSRS deck, `review_states.dueAt = null`), inheriting the grounding as a real `source_locations` row + a `derived_from` edge, and flips the suggestion `draft → approved` in the SAME transaction the card is minted in (so a committed card can't leave a re-approvable suggestion → duplicate), rejecting on a hard `empty` card-quality block; `status` projects the disabled-state/disclosure data with `keyConfigured: boolean` only; `onSettingsChanged` re-forks the worker; `downloadModel` is the guarded stub model fetch. **Typed seam**: six channels on a new `ai.*` group — `ai:run`, `ai:list`, `ai:approveCard`, `ai:dismiss`, `ai:status`, `ai:downloadModel` — with zod request/result schemas (`channels.ts`/`contract.ts`/`contract.test.ts`), threaded `ipc.ts` → `db-service.ts` → `preload` → `apps/web/src/lib/appApi.ts`; the renderer observes the `ai` job via the existing `jobs.subscribe`, there is NO generic `jobs.enqueue` and NO key channel. **Renderer**: `AiAssist.tsx` mounts in the extract distillation / card builder — it offers the seven actions over the extract's `source_locations` anchor (the grounding), shows each DRAFT with its grounding refblock (the source quote it was made ABOUT) + the T035/T086 card-quality warnings, an **Approve → card draft** button for the card-shaped drafts and a **Dismiss**, a calm DISABLED state when AI is off ("Turn on AI assistance in Settings →"), and the managed-proxy disclosure banner; Settings gains the AI enable/provider/key-configured/download controls. The renderer NEVER calls a model, holds a key, or mints a card directly — it sends validated intents and shows what main computed. Covered by Vitest (`packages/core/src/ai.test.ts` — the tuples/guards/`actionProducesCard`/`suggestionKindForAction`/bounds; `packages/core/src/settings.test.ts` — the off-by-default defaults + the key-stripping projection; `apps/desktop/src/worker/ai-providers.test.ts` — env-resolution, the fake provider's per-action drafts, `buildPrompt`/`buildSuggestion` JSON-card parse + degrade-to-text; `apps/desktop/src/main/ai-service.test.ts` — disabled-throws, the no-key-in-payload enqueue, the draft persist + quality eval, approve mints a parked `card_draft` with the inherited grounding + the same-tx status flip + the empty-card reject, dismiss; `packages/local-db/src/ai-suggestion-repository.test.ts` — the create/list/find/dismiss/setStatusWithin round-trip incl. cards + grounding JSON; `card-service` draft-from-suggestion path; `contract.test.ts`, `db-service.test.ts`, schema round-trip) and a Playwright/Electron spec (`tests/electron/ai-distillation.spec.ts`, driven by the `INTERLEAVE_AI_FAKE` seam — no live model/network: AI off shows the calm disabled state; enabling + running suggest-Q&A produces a grounded draft; approving mints a parked, un-due `card_draft` (stage `card_draft`, NOT `active_card`, no due row); and the draft suggestion + the approved card survive an app restart). `pnpm typecheck` (13/13) + `pnpm lint` (654 files) + `pnpm test` (2126 tests) all green.
- 2026-06-02 - T092 Verification tasks - done. Scheduled `task`-type elements ("verify this claim", "find a better source", "update this outdated card", "check the current version") now keep time-sensitive knowledge from rotting — exactly the Done-when — and can be GENERATED from T090 expiry. A `task` is the EXISTING core `task` {@link ElementType} on the ATTENTION scheduler (it answers "should the user process this again, and when?" — NEVER FSRS, NEVER a `review_states` row); T092 is the composition seam that wires the already-present `task` element + `tasks` side-table into a real create/schedule/complete/postpone/generate path. **Vocabulary in `@interleave/core` (`task.ts`), framework-free**: a closed `TASK_TYPES` tuple — `verify_claim`/`find_better_source`/`update_outdated_card`/`check_current_version`/`custom` (+ `isTaskType`, `TASK_TYPE_LABEL`, defensive `taskTypeLabel`) — is the SINGLE source of truth for both the domain union AND the `tasks.task_type` CHECK (built via `inList`), so the DB and domain can't drift. **Persistence**: migration `0025` WIDENS the existing `tasks` table — adds `task_type` CHECK (the core tuple), `linked_element_id` (nullable FK → `elements.id` ON DELETE SET NULL — the protected element, DUAL-MODELED with the canonical `references` edge like `cards.source_location_id`) + `tasks_linked_element_idx`, `note` (≤2048), and the PARTIAL UNIQUE INDEX `tasks_open_link_type_uq` on `(linked_element_id, task_type) WHERE status NOT IN ('done','dismissed','deleted')` so at most ONE OPEN task of a kind protects a given element (the DB-level idempotency guarantee for generation; NULL links are never deduped). The `task_type` CHECK forces a SQLite table rebuild (DROP/RENAME); NO trigger drop/recreate is needed (the FTS triggers reference `documents`/`elements`/`cards`, never `tasks`), and the INSERT…SELECT copies only the PRE-EXISTING columns (corrected from Drizzle 0.45.x's mis-generated SELECT, same fix as 0023/0024). **NO new `operation_log` op type** (the closed 15-op set is unchanged) and **NO new `ELEMENT_STATUSES` value**: create → `create_element`, link → `add_relation`, schedule/complete/postpone → `reschedule_element`. **`TaskService` (`packages/local-db/src/task-service.ts`)** composes it: `createTask` writes the `task` element (`createWithin`, `create_element`) + the `tasks` row + the `references` edge (task → protected element, `add_relation`) in ONE transaction (the `references` edge is canonical lineage, the `linked_element_id` column the denormalized convenience, kept consistent), status `scheduled`, stage `rough_topic`, priority INHERITED from the protected element (so a low-priority stale fact never dominates the queue), `due_at` from an explicit choice or the attention by-priority interval — and crucially does NOT set `parentId` (a task is a maintenance action, not a distillation child, so it never pollutes the protected element's `children`/`LineageTree`). `completeTask` (element + `tasks` → `done`, optional EXPLICIT `bumpReviewByDays` to push the protected card's T090 `review_by` forward via `update_element` so it stops re-surfacing — never automatic), `postponeTask` (grows the interval with the running postpone count off the op log, like `ExtractService.postpone`), `listOpenTasks`/`listDueTasks`/`findTask`/`deleteTask` (soft-delete via the element path). **`generateVerificationTasks(now)` is idempotent + opt-in**: it scans CARD-BACKED facts (the T090 migration is cards-only) carrying a `valid_until`/`review_by` whose `deriveExpiryStatus(now)` is `expired` (→ `update_outdated_card`) or `due_for_review` (→ `verify_claim`), creating ONE task per protected card that lacks an OPEN task of that kind — an advisory in-tx re-check skips fast, and the partial unique index is the hard guarantee (a raced duplicate insert FAILS and is swallowed as the idempotency firing, not surfaced). NEVER a silent background job. **Typed seam**: five channels on a NEW `tasks.*` group — `tasks:create`, `tasks:list`, `tasks:complete`, `tasks:postpone`, `tasks:generateFromExpiry` — with zod request/result schemas (`channels.ts`/`contract.ts`/`contract.test.ts`), threaded `ipc.ts` → `db-service.ts` → `preload` → `apps/web/src/lib/appApi.ts` as `window.appApi.tasks.*`; NO generic mutation channel, NO `db.query`. The inspector-resolved element shape now carries the protected-element link so the Maintenance read renders without a re-fetch. **Renderer**: a `MaintenanceSection` in the Inspector lists open tasks protecting the focused element and lets the user create/complete/postpone (all via the typed `appApi.tasks.*`, optimistic + undo-aware), the queue surfaces due tasks through the existing attention path with a jump to the protected element, and the review banner hook (the T090 `due_for_review`/`expired` signal) now offers a "Create verify task" affordance that calls `tasks.create`. The renderer never schedules, derives expiry, or touches SQL — it sends validated intents. Tests: `task.test.ts` (the tuple/guards/labels incl. the defensive fallback), `task-service.test.ts` (create round-trip writing the element + `tasks` row + `references` edge in one tx with no new op type; the NO-`review_states` assertion; complete/postpone with `reschedule_element` + the postpone-count growth; the optional `bumpReviewByDays` `update_element`; generation producing `update_outdated_card`/`verify_claim` by status, inheriting priority, and the DOUBLE idempotency — advisory re-check + the partial-unique-index rejection swallowed), `schema.roundtrip` + a migration-level check that `0025` emits the `WHERE status NOT IN (...)` partial predicate, `MaintenanceSection.test.tsx` (the inspector create/complete/postpone wiring), `contract.test.ts` + `db-service.test.ts` (the five-channel zod schemas + wiring), and an Electron e2e (`tests/electron/verification-tasks.spec.ts`: a card given a past `valid_until` → generate surfaces a verification `task`, completing it clears it, and the task SURVIVES AN APP RESTART). `pnpm typecheck` (13/13) + `pnpm lint` (642 files) + `pnpm test` (2081 tests) all green.
- 2026-06-02 - T091 Source-reliability metadata - done. A `source` can now record HOW TRUSTWORTHY it is — `source_type` (`paper`/`book`/`article`/`docs`/`reference`/`blog`/`forum`/`video`/`dataset`/`personal_note`/`other`), `reliability_tier` (`primary`/`secondary`/`tertiary` in the scholarship sense), `confidence` (`high`/`medium`/`low`), and free-text `reliability_notes` — and that reliability/uncertainty surfaces on extracts AND important cards derived from the source, exactly the Done-when. Reliability is **PROVENANCE on the `sources` side-table, NOT a new lineage model**: it extends the T014 provenance row + the T043 refblock; editing it is `update_element` on the OWNING source element (**no new `operation_log` op type** — the closed 15-op set is unchanged — and **no new `ELEMENT_STATUSES` value**); `source_locations`/`element_relations` are NEVER touched. All four fields are nullable with **no backfill** — a source with no reliability data renders EXACTLY as before (no badge). **Vocabulary + presentation in `@interleave/core` (`source-ref.ts`), framework-free**: three small ordinal closed tuples — `SOURCE_TYPES`/`RELIABILITY_TIERS`/`CONFIDENCE_LEVELS` (+ `isSourceType`/`isReliabilityTier`/`isConfidenceLevel` guards) — are the SINGLE source of truth for both the domain unions and the matching `sources` CHECK constraints (the DB + the domain can't drift). Ordinal enums (not free-form 0–1 floats) are deliberate: they map cleanly to the kit's restrained labels + badge colors. `SourceRef` + `EMPTY_SOURCE_REF` gain the four nullable fields; `formatSourceRef` now also assembles a `ReliabilitySummary | null` (`null` = no badge, the unchanged pre-T091 render) carrying the raw `tier`/`confidence`/`sourceType`, a calm one-line `label` ("Primary source · high confidence" — tier leads, else the type, then confidence; notes-only → "Source notes"), the free-text `notes`, and `hasUncertainty` (true for **`low` confidence OR a present notes string** — the badge tints warn + the note shows). The `Source` core type (`source.ts`) gains the four fields. **Persistence**: migration `0024` adds the four nullable columns to `sources` + three CHECKs binding the enum columns to the core tuples (`NULL` passes — the "no reliability data" case). The CHECKs force a SQLite table rebuild (DROP/RENAME); **no triggers reference `sources`** (the FTS triggers read `documents`/`elements`), so unlike the cards rebuild (0023) no trigger drop/recreate is needed, and the INSERT…SELECT copies only the PRE-EXISTING columns (corrected from Drizzle 0.45.x's mis-generated SELECT, same fix as 0023). `schema/sources.ts` declares the columns + the three `check()`s against the imported core tuples; `sources_canonical_url_idx` is preserved. **Repository**: `SourceRepository.createSource`/`importManualSource` accept the four optional reliability inputs (narrowed to the tuples; default `null`), and a new `updateReliability(elementId, patch)` writes the four columns, stamps `elements.updatedAt`, and logs `update_element` in ONE transaction — an OMITTED field is left unchanged, an explicit `null`/`""` (notes) CLEARS it, a non-tuple enum clears the field, notes are trimmed (empty → `null`); it throws on a non-live/non-source id; `nextReliability` resolves keep-current-vs-normalize; `rowToSource` reads the fields back. **Typed seam**: a new `sources:updateReliability` channel on the EXISTING `sources.*` group, with a zod request schema (`sourceId` + the four optional fields, enums validated against the tuples, notes `≤2048`, a `.refine` rejecting an empty call) + result (`{ provenance }`, so the inspector reflects the refreshed badge without a re-fetch); `SourceProvenance` gains the four fields; threaded `ipc.ts` → `db-service.ts` (`updateSourceReliability`) → `preload` → `apps/web/src/lib/appApi.ts` (`appApi.updateSourceReliability` + `sources.updateReliability`). The `SourceRef`/`SourceProvenance` shapes (and the inbox preview rail via `inbox-query`) now carry the reliability fields; `inspector-query`/`source-ref-query` resolve them so the inspector/refblock render without re-deriving. NO generic `db.query`/SQL exposure to the renderer. **Renderer**: the `RefBlock` (`RefBlock.tsx` + `ref-block.css`) renders the reliability badge (a tier-colored `.badge` variant — primary → ok tint, secondary/tertiary → neutral/muted, no-tier → soft neutral, **uncertainty overrides to the warn tint** with a `warning` icon vs a `shield` icon) on its own row beneath the citation + the free-text caveat note as a calm second line — shown ONLY when `formatSourceRef` returns a reliability summary, so a null-reliability source is the unchanged refblock; **in review it rides the existing post-reveal reveal gate** (the whole refblock is hidden until reveal), so it can NEVER leak the answer. The Inspector gains a `ReliabilitySection` (source-only) that shows the badge + `MetaRow`s + notes and lets the user pick type/tier/confidence + edit notes, calling `appApi.updateSourceReliability` (one `update_element`); when nothing is set it offers an "Add reliability" affordance; on save it re-reads so extracts/cards from the source reflect the new reliability. The renderer never narrows enums, derives the summary, or touches SQL — it renders what the main process computed and sends the validated edit. Tests: `source-ref.test.ts` (+110 — the tuples/guards, `summarizeReliability`'s label assembly incl. tier-vs-type-vs-notes-only ordering + the `hasUncertainty` low-confidence/notes rule, the no-metadata → `null` case, the four fields flowing through `formatSourceRef`/`hasSource`); `source-repository.test.ts` (+80 — create-with-reliability, the `updateReliability` round-trip + partial-patch keep-current + explicit-clear + enum normalization + the `update_element` op with no new op type + the non-source throw); `inspector-query.test.ts` (the resolved reliability on the inspector); `RefBlock.test.tsx` (+29 — the badge variants, the uncertainty warn override, the notes line, the null-reliability no-badge render); `ReliabilitySection.test.tsx` (the inspector editor render/edit/save wiring); `ReviewScreen.test.tsx` (+58 — the badge absent pre-reveal, present post-reveal); `contract.test.ts` + `db-service.test.ts` (the zod schema incl. the empty-call refine + the wiring); and an Electron e2e (`tests/electron/source-reliability.spec.ts` — set tier/confidence/type + a note on the seeded source → the badge updates, a CARD derived from the source shows the same reliability, in `/review` the badge is absent before reveal + present after, and the reliability SURVIVES AN APP RESTART). `pnpm typecheck` + `pnpm lint` + `pnpm test` all green.
- 2026-06-02 - T090 Staleness & expiry - done. Facts (canonically a `card`, the fact carrier) can now carry a CLAIM LIFETIME — `fact_stability`, `valid_from`, `valid_until`, `jurisdiction`, `software_version`, `review_by` — and the app DERIVES a `fresh` / `due_for_review` / `expired` status from those fields + `now`. **"Expired" is a DERIVED attribute, NEVER a new `ELEMENT_STATUSES` value** (like `isLeech`/`isRetired`): a card stays `active`/`scheduled` underneath; nothing in the lifecycle/scheduler model changes. **`packages/core/src/fact-lifetime.ts`** is the pure, framework-free model + derivation: `FACT_STABILITY = ["stable","slow","volatile"]` (the closed tuple is the source of truth for the DB CHECK so domain + schema can't drift) + `isFactStability`; the all-nullable `FactLifetime` struct (+ `EMPTY_FACT_LIFETIME`); and `deriveExpiryStatus(lifetime, now)` — `expired` when `validUntil` parses AND `now > validUntil` (dominates), else `due_for_review` when `now > reviewBy`, else `fresh`. Date handling is LOOSE + DEFENSIVE (the same guard pattern as `source-ref.ts`'s `yearOf`): an empty/unparseable date is "no constraint", never a throw; a bare `YYYY-MM-DD` is parsed as UTC midnight so the status is host-timezone-independent; a fact with no lifetime (the vast majority) is a cheap `fresh`. `validFrom` is informational (a not-yet-valid future window is still `fresh`, not "expired"). Helpers: `hasFactLifetime` (gates the inspector section), `expiryLabel` (a verbatim UI label), and `lifetimeToRecencySignals` — which maps the persisted lifetime → the T086 `SourceRecencySignals` shape so the card-quality `outdated-source` warning and the REAL persisted expiry AGREE (T090 is the deferred half of T086's note). **Persistence**: migration `0023` adds the six nullable columns to `cards` + a `fact_stability` CHECK (the core tuple) + `cards_review_by_idx` (so the future scan is cheap) — CARDS-ONLY (the `elements` mirror is deferred); all nullable, NO backfill (an existing row derives `fresh`). The CHECK forces a table rebuild, so the FTS sync triggers (`cards_fts_*`, `elements_fts_au`) are DROPPED before the DROP/RENAME and RECREATED verbatim after (the migration-0014 pattern), and the INSERT…SELECT copies only the PRE-EXISTING columns (corrected from Drizzle's mis-generated SELECT). **Editing is `update_element` — NO new op type** (the closed 15-op set is unchanged, no status change): `CardEditService.setLifetime(id, patch)` writes the six columns + stamps `elements.updatedAt` + logs `update_element` in ONE transaction (omitted fields keep current, provided fields normalize; `factStability` validated against the core tuple); `cardRowToLifetime` reads them back. **Typed seam**: a new `cards:setLifetime` channel on the EXISTING `cards.*` group (no new top-level group), with zod request/result schemas (`contract.ts`/`contract.test.ts`), threaded `ipc.ts` → `db-service.ts` → `preload` → `apps/web/src/lib/appApi.ts`; the `inspector-query` now resolves a card's derived `expiry` + raw lifetime so the inspector/review can render it without recomputing, and the review-card payload carries a `ReviewCardExpiry` (status + the display fields) computed MAIN-side. **Renderer**: the Inspector gains an Expiry section (shown only when `hasFactLifetime`) to view/edit the six fields and see the derived status; in review, `ExpiryBanner.tsx` shows a calm POST-REVEAL banner (it rides the existing `revealed` gate exactly like the refblock, so it can NEVER leak the answer) — a `--danger` "this fact may be out of date (expired {date})" for `expired`, a softer `--warn` "Due for review by {date}" for `due_for_review`, with an optional jurisdiction/version context line; a fresh/lifetime-less card renders nothing. The renderer NEVER derives the status or touches SQL — it renders what the main process computed and sends the validated edit. The `due_for_review`/`expired` signal is what T092's verification-task generation will consume; T090 exposes the derivation only (it does NOT generate tasks, and leaves the "Create verify task" hook un-rendered). Tests: `fact-lifetime.test.ts` (the derivation truth table incl. the `validUntil`-dominates ordering, the defensive/UTC date parsing, `validFrom`-is-informational, `hasFactLifetime`/`expiryLabel`/`lifetimeToRecencySignals`), `card-edit-service.test.ts` (the `setLifetime` round-trip + partial-patch keep-current + `factStability` validation + the `update_element` op, no new op type), `inspector-query.test.ts` (the derived `expiry` resolution), `ExpirySection.test.tsx` + `ReviewScreen.test.tsx` (the inspector section + the reveal-gated banner tones), `contract.test.ts` + `db-service.test.ts` (the wiring), and an Electron e2e `staleness-expiry.spec.ts` (a card given a past `valid_until` surfaces the expired banner in review + the inspector status, and the lifetime survives an app restart). `pnpm typecheck` (13/13) + `pnpm lint` (632 files) + `pnpm test` (2024 tests) all green.
- 2026-06-02 - T089 Contradiction detection - done. Highly-similar cards/extracts that ALSO carry an opposing signal are now flagged as a CALM, SUGGESTIVE "possible conflict — review" surface; it is heuristic, never authoritative — it never edits/suspends/reschedules/merges, writes NOTHING (no `operation_log`, no new relation — the closed `RELATION_TYPES` set does not grow; a DERIVED read, like T088), and degrades to `[]` (surface hidden) when semantics are off / `vec0` is absent / the element isn't an embedded card/extract. **`detectContradictions(pairs)` (`packages/core/src/contradiction.ts`)** is a PURE, deterministic, no-I/O heuristic: a pair is flagged when `similarity >= CONTRADICTION_SIMILARITY_MIN (0.8)` AND ≥1 opposing/superseding signal fires — **(1) negation/polarity divergence** (an asymmetric NEGATION_CUES token, or an ANTONYM_PAIRS word vs its antonym across the two sides — a crude, documented, intentionally-minimal proxy), **(2) numeric divergence** (same-unit numbers differing beyond a scale-free relative `CONTRADICTION_NUMERIC_TOLERANCE (0.15)`, e.g. "7 days" vs "14 days"), **(3) recency supersession** (source years differing by ≥ `CONTRADICTION_RECENCY_GAP_YEARS (3)` — the roadmap's literal "newer source conflicts with older card", returning `newerSide`). `severity` is `"low"` for one signal, `"medium"` for ≥2 — NEVER `"high"`; missing dates/numbers simply contribute no signal (never a throw). Inputs are EXTENSIBLE (optional per-side metadata + exported tunable constants) so **T090** (`valid_from`/`valid_until`/`fact_stability`) and **T091** (source reliability) can ENRICH the signals/severity WITHOUT changing the shape. **`ContradictionService` (`apps/desktop/src/main/contradiction-service.ts`)** is the main-side resolver: gated on `vecAvailable` + `semanticSearchEnabled`, it over-fetches a wide `vec0` KNN window (NEIGHBOR_FETCH_LIMIT 25) and keeps up to COMPARABLE_PAIR_LIMIT (8) comparable (card/extract) neighbors so non-comparable `source` hits can't crowd a genuine conflict out of the cap, resolves each side's embedded text (`EmbeddingService.buildText`) + lineage source dates (`resolveSourceRef` → owning source `publishedAt`/`accessedAt`), calls the pure core, then enriches each flag with the OTHER element's title + `SourceRef` (and the self `SourceRef`) for a side-by-side compare — NO vectors cross IPC, only ids/titles/refs/reasons. Exposed through the typed `semantic.contradictions` `window.appApi` (contract + channel + preload + db-service), surfaced as a `ConflictSection` in the inspector and during review (`ReviewScreen`). Tests: pure-core unit tests (`contradiction.test.ts`), service tests (`contradiction-service.test.ts`), `ConflictSection` component tests, contract test, and an Electron e2e (`tests/electron/contradiction.spec.ts`).
- 2026-06-02 - T088 Related-item suggestions - done. The inspector now shows a "Related" section computing FOUR derived buckets for the selected element, all DERIVED READS over the T087 `vec0` store + the existing `element_relations` / `concepts` lineage graph — it writes NOTHING (no `element_relations`, the closed `RELATION_TYPES` set does not grow; NO `operation_log` entry; lineage untouched). **`RelatedService` (`packages/local-db/src/related-service.ts`)** composes a narrow `RelatedDeps` slice (`ElementRepository` + `ConceptRepository` + `EmbeddingRepository` + a `resolveRef`) and returns `{ similar, duplicates, prerequisiteConcepts, siblingSources, semanticAvailable }`: **(1) similar extracts** — `extract`-typed `vec0` nearest-neighbors of the element's OWN stored vector (one over-fetched KNN, partitioned), distance mapped to a 0..1 display similarity via `distanceToSimilarity` (`sim ≈ 1 - d²/2`, clamped — a display transform only, ranking always uses the raw distance); **(2) possible duplicates** — the nearest SAME-type neighbors whose distance is below a tuned, deliberately CONSERVATIVE `DUPLICATE_DISTANCE_THRESHOLD = 0.15` (~cosine 0.99), flagged `kind:"duplicate"` so the UI styles them "possible duplicate — review" and NEVER auto-merges; a below-threshold same-type hit is the higher-signal duplicate and is excluded from `similar`; **(3) prerequisite concepts** — the element's member concepts (`level 0`) PLUS each member's `parentConceptId` ancestor chain (`level 1+`), ANCESTORS FIRST (most general → "learn first"), deduped by id (shallowest level wins), cycle-guarded by a visited set — pure concept hierarchy, WORKS WITH SEMANTICS OFF; **(4) sibling sources** — `source` elements sharing ≥1 `concept_membership` concept, ordered by vector similarity when the element is embedded else by shared-concept count then title — also WORKS WITH SEMANTICS OFF. **Graceful degradation is load-bearing**: the vector buckets resolve only when `semanticEnabled` AND `embeddings.available` AND the element actually has a stored vector (`getVector` non-null) — otherwise `similar`/`duplicates` are `[]` with `semanticAvailable:false` while the concept + sibling buckets still resolve from lineage; it returns empty buckets for an unknown/soft-deleted element and NEVER throws on a degraded store; the element itself + soft-deleted elements are always excluded. **Typed seam**: a new `semantic:related` channel (`channels.ts`) with zod request/result schemas (`contract.ts` + `contract.test.ts` — the result carries ids/titles/similarities/kinds + the concept levels + `semanticAvailable`, NO vectors cross IPC), threaded `ipc.ts` → `db-service.ts` (a lazily-built `relatedService` getter wiring the repos + a `resolveRef` over source-locations) → `preload/index.ts` → `apps/web/src/lib/appApi.ts` as `appApi.semantic.related`; no generic `db.query`/vec exposure, no fs/SQL/vec in React. **Renderer**: the `Inspector` (`Inspector.tsx` + `inspector.css`) gained a Related section that fetches on selection-change, renders the four buckets (similar extracts with a similarity affordance, possible-duplicates styled distinctly with a "review" cue, the prerequisite-concept chain indented by level, sibling sources), each row jump-to-able via the resolved ref, and labels when semantics are off so the user knows only the lineage buckets ran. Covered by Vitest (`packages/local-db/src/related-service.test.ts` — the KNN partition into similar-vs-duplicate + the threshold cutoff, the `extract`-only similar filter, the ancestor-chain level map + ancestors-first ordering + cycle guard + dedup, sibling-source shared-concept counting + vector-vs-count ordering, the semantics-off / not-embedded / unknown-element degrade paths, self + soft-deleted exclusion, `distanceToSimilarity` clamping; `apps/web/src/components/inspector/RelatedSection.test.tsx` — the section render/fetch/bucket wiring; `contract.test.ts`) and a Playwright/Electron spec (`tests/electron/related-items.spec.ts`). `pnpm typecheck` + `pnpm lint` (618 files) + `pnpm test` (1959 tests) all green.
- 2026-06-02 - T086 Minimum-information-principle checks - done. The card-quality checklist (`packages/core/src/card-quality.ts`) now covers the FULL minimum-information surface from the Done-when, all as advisory `warn` rows that never add a new hard block (the only blocker stays the hollow-card `empty`): **multiple-facts** (a sentence/clause `factCount` over `MAX_FACTS_HINT` — targets fact COUNT, not length, so even a short two-sentence answer warns), **long-list** (a delimiter-split `listItemCount` over `LIST_ITEM_WARN_COUNT` — "split it or use an overlapping cloze set"), **vague-pronoun** (a bare `this/that/it/they…` mid-face with no concrete noun on the SAME face to anchor it — broadens the leading-only `ambiguous-pronoun`, which is kept stable, via a deliberately conservative `hasDanglingPronoun` + `STOPWORDS` proxy), **unsupported-claim** (a SOURCELESS answer that is "claim-shaped" — causal/comparative/definitive/quantitative — escalating beyond the generic `missing-source`), **outdated-source** (time-sensitive language — `TIME_SENSITIVE_TERMS` plus version/year regexes — with no anchoring `sourceDate`, or an explicit `sourceIsStale` flag; the new optional `SourceRecencySignals` is backward-compatible and silences the warning when the claim is dated; real fact expiry/`valid_until`/staleness scheduling is deferred to M18/T090), and **oversized-cloze** (any SINGLE `{{cN::…}}` deletion span over `CLOZE_DELETION_MAX_WORDS`, distinct from the whole-body `CLOZE_MAX_WORDS`). All prose heuristics are skipped on code faces (T072), matching the existing `ambiguous-pronoun` rule. The seventh check, **similar-answer** (interference), needs sibling answers the single card can't see, so it stays in a SEPARATE pure function `detectInterference(candidate, siblings)` (lower-case word-shingle Jaccard via `answerSimilarity` ≥ `INTERFERENCE_SIMILARITY_THRESHOLD`; degrades to `null` when there's nothing to compare) — NOT pulled into `evaluateCardQuality`, which stays single-card-pure; true semantic/duplicate detection is M18/T088. The candidate set is fed read-only through a new narrow seam: `ReviewRepository.listSiblingCardBodies(extractId)` (live `card` children → comparable `answer`/`cloze` only, no FSRS state/lineage, pure read), surfaced via `DbService.cardsSiblingAnswers`, the `cards:siblingAnswers` channel, `CardsSiblingAnswersRequest/Result` contract + Zod schema, and `appApi.cards.siblingAnswers`; the CardBuilder fetches it ONCE per extract (never per keystroke) and merges the interference row into the live checklist. NO new `operation_log` op types (read-only path), source lineage untouched. Tests: `card-quality.test.ts` (+257 lines) covers every new heuristic + thresholds + `detectInterference`/`answerSimilarity`; `CardBuilder.test.tsx`, `contract.test.ts`, a new `sibling-card-bodies.test.ts`, and a `cards.spec.ts` e2e exercise the wiring. typecheck + lint (595 files) + 1892 vitest tests all green.

- 2026-06-02 - T085 Leech remediation workflow - done. The minimal T040 leech-cleanup view is now the full **repair screen** for repeatedly-failing cards: a leech card can be **split** into atomic siblings, given **add-context**, sent **back-to-extract** for re-distillation, **open-source**'d, **lower-priority**'d, **suspended**, or **deleted** — exactly the Done-when. The screen REUSES existing paths for most actions (open-source = T022 lineage navigation; lower-priority = T027 `elements.setPriority`; suspend/delete/rewrite/un-leech = T038/T040 `cards.*`) and adds ONE service — `CardRemediationService` (`packages/local-db/src/card-remediation-service.ts`) — for the THREE compositions those reuses don't cover, each one transaction + the correct EXISTING op (the closed 15-op set is UNCHANGED — **no new op types**), with `card → source location → source` and `card → extract` lineage preserved and the card's append-only `review_logs` NEVER destroyed. **split** — divides a multi-fact failing card into 2+ ATOMIC sibling cards; each new card inherits the original's lineage (`parentId` extract, `sourceLocationId`, numeric priority, tags) and starts a FRESH `review_states` row (a split card is a NEW card to re-learn — it NEVER copies the original's FSRS memory state), all new cards are grouped as `sibling_group` siblings (so review never shows them back-to-back, T039), and the original is soft-deleted (default, `review_logs` survive in trash) or suspended (the user's choice); every part is validated non-empty for its kind UP FRONT via the SAME `resolveCardBodyForKind` the rewrite path uses, so a bad part rejects the whole split with no partial mutation. All in ONE transaction logging `create_element`/`create_card` per new card, `add_relation` (`sibling_group`) per grouping, and `soft_delete_element`/`update_element` for the original. **addContext** — appends a clarifying CONTEXT NOTE so the prompt becomes answerable WITHOUT a new column: the note is a durable op-payload marker (`{ context: note }`) read back via an op-log scan (`contextNote`, mirroring `CardEditService.flagState` — latest marker wins), the card body / `review_states` / lineage untouched and the card STAYS in rotation (context is a fix, not an exit); logs `update_element` + stamps `elements.updatedAt`. **backToExtract** — sends the card's originating EXTRACT (its `parentId`, an ATTENTION item) back to DUE-NOW for re-distillation via `ElementRepository.rescheduleWithin` (attention op `reschedule_element`, status `scheduled` — NOT `setStage`/`postpone`), suspending (default), deleting, or keeping the leech card; when the card has no live parent extract (e.g. an Anki-imported leech) it returns `{ extract: null }` and mutates nothing (the screen disables the action). **THE TWO-SCHEDULER SPLIT (load-bearing)**: a card is the only FSRS-scheduled element; split + addContext are card-side (fresh FSRS state / op-marker, never the original's memory), and backToExtract is the SINGLE attention-side action — it reactivates the parent extract through the EXISTING attention reschedule seam and NEVER writes `review_states` for the extract (that would break the split). **No schema migration** (the latest migration is still T082's `0020`; the context note rides the op log, the splits reuse the existing card/relation tables). **Typed seam**: three new channels — `cards:split` / `cards:addContext` / `cards:backToExtract` — with zod request/result schemas (`contract.ts`/`contract.test.ts`), threaded `ipc.ts` → `db-service.ts` → `preload` → `apps/web/src/lib/appApi.ts`; NO generic mutation channel, NO raw card/extract crossing un-validated. **Renderer**: the T040 `LeechCleanup` view is replaced by `LeechRemediation.tsx` (+ `leech-cleanup.css`, routed at the maintenance leech path) — a per-leech repair card offering split (an authored-parts editor) / add-context (a note field) / open-source / back-to-extract / lower-priority / suspend / delete, with the back-to-extract action disabled when there is no live parent extract. The renderer never authors cards, marks context, touches FSRS, or reschedules an extract — it sends the validated intent and the main process does the work. Covered by Vitest (`packages/local-db/src/card-remediation-service.test.ts` — split's inherited lineage + tags + fresh `review_states` + `sibling_group` grouping + the disposition of the original + the ≥2-parts and per-part validation guards, addContext's op-marker round-trip with the latest-wins read and the card staying in rotation, backToExtract's attention reschedule to due-now with NO `review_states` write + the no-live-parent no-op + the card disposition; `apps/web/src/maintenance/LeechRemediation.test.tsx`; `contract.test.ts`, `db-service.test.ts`) and a Playwright/Electron spec (`tests/electron/leech-remediation.spec.ts`: a repeatedly-failed leech card is split into atomic siblings that enter the deck without their parent, add-context surfaces on the card, back-to-extract reactivates the parent extract in the attention queue, and the remediation survives an app restart). `pnpm typecheck` + `pnpm lint` + `pnpm test` (1866 tests) all green.
- 2026-06-02 - T084 Extract-stagnation analytics - done. Extracts that keep **returning without progressing** are now DETECTED and SURFACED with rewrite/convert/postpone/delete suggestions — exactly the Done-when. The detector is the ATTENTION-side MIRROR of the FSRS leech rule: where a leech is a *card* that keeps FAILING (read off `review_states.lapses`), a stagnant extract is an attention *extract* that keeps COMING BACK but never ADVANCES — its stage never progresses (`raw_extract → clean_extract → atomic_statement`), it produced no children, and it has been postponed repeatedly. **The two predicates are deliberate SIBLINGS (co-located with `leech.ts`) but NEVER computed from each other's signals**: an extract has no `review_states` row and is NEVER called a "leech"; a card is never "stagnant" (CLAUDE.md two-scheduler split honored). **Pure predicate in `@interleave/scheduler` (`packages/scheduler/src/stagnation.ts`), no DB/IPC/React and `now`-injected**: `isStagnant(signals, now, options)` flags an extract stagnant ⇔ `postponeCount ≥ STAGNATION_POSTPONE_THRESHOLD` (3) **AND** `stage ≠ atomic_statement` (never reached the terminal stage) **AND** `childCount === 0` (produced nothing) **AND** `daysSinceProgress ≥ STAGNATION_STALE_DAYS` (30, measured from the last stage advance or `createdAt` when it never advanced) — a deliberately CONSERVATIVE AND so a productive/advancing extract is safe (false positives are advisory, the user always chooses). Both thresholds are NAMED EXPORTED constants (a future per-collection setting can tune them; never hard-codes 3/30), and it returns the verdict + WHICH `reasons` fired (`postponed-repeatedly`/`no-progress`/`no-children`/`stale`, rendered as calm chips) + a pure-function `suggestion`: a cleaned-up `clean_extract` with no children → **convert** (card-ready); a deeply-stale (≥ 2× window), low-priority (`< 0.4` = C/D band), heavily-postponed `raw_extract` → **delete** (had every chance, low value); any other `raw_extract` → **rewrite**; otherwise → **postpone**. **Composition seam — `ExtractStagnationQuery` (`packages/local-db/src/extract-stagnation-query.ts`), READ-ONLY**: `listStagnantExtracts(asOf, options)` reads the charter's extract-scheduler signals off the durable tables + op log and runs the pure heuristic, returning ONLY the stagnant rows most-stagnant-first (more postpones, then staler, then id ASC — deterministic). The signals come from the EXISTING tables, **no new bookkeeping**: stage/priority/createdAt/dueAt off `elements`; `childCount` from live children grouped by `parentId`; `postponeCount` from the op log's `reschedule_element` `postpone === true` markers (the same canonical counter the attention scheduler uses); `lastStageAdvanceAt` from the newest `update_element` op whose `payload.patch.stage` ACTUALLY changed the stage (`prev.stage !== patch.stage`) — explicitly filtered to the stage-advance patch shape so the `setCardLeech`/flag/body `update_element` ops (which carry no `patch`) can't pollute the signal. **GROUPED, not N+1**: one live-extract read + one children read grouped by parent + one op-log read grouped by `elementId`, regardless of extract count. It writes nothing and appends NO op (there is nothing to undo about looking at stats); the suggestions are LABELS — the actual remediation runs through the EXISTING T024 `extracts.*` (+ extract→card) transactional, op-logged commands. **No new op types** (the closed 15-op set is unchanged) and **no schema migration** (a pure read over existing tables). **Typed seam**: one new channel — `extractStagnation:list` (read-only scan) — with a zod request schema (optional `asOf`/`limit` 1–1000/`offset`; `contract.ts`/`contract.test.ts`), threaded `ipc.ts` → `db-service.ts` → `preload` → `apps/web/src/lib/appApi.ts` as `window.appApi.getExtractStagnation`; NO generic mutation channel. **Renderer**: `StagnantExtracts.tsx` (+ `stagnant-extracts.css`, routed at `/maintenance/stagnant`, linked from the shell nav) lists each stagnant extract with its stage/priority/postpone-count/days-stale, the fired-reason chips, and the four T024 remedies with the SUGGESTED one highlighted — Rewrite/Convert open the extract editor, Postpone/Delete are direct typed `appApi.*` commands that optimistically drop the row and re-read on global undo (⌘Z). The renderer never detects stagnation, computes stage math, or touches FSRS — it shows what the main process computed and sends the intent. Covered by Vitest (`packages/scheduler/src/stagnation.test.ts` — the four-way AND incl. each threshold boundary, the reason set, the days-since-progress measure from the last advance vs `createdAt`, and the suggestion mapping; `packages/local-db/src/extract-stagnation-query.test.ts` — the grouped signal assembly, the postpone-marker + real-stage-advance filtering excluding leech/flag ops, the most-stagnant-first sort, and the still-READ-ONLY contract; `apps/web/src/maintenance/StagnantExtracts.test.tsx`; `nav.test.ts`, `contract.test.ts`, `db-service.test.ts`) and a Playwright/Electron spec (`tests/electron/extract-stagnation.spec.ts`: a repeatedly-postponed, never-advanced, child-less extract surfaces with its reasons + suggestion, a remedy clears it, and the detection survives an app restart). `pnpm typecheck` + `pnpm lint` + `pnpm test` all green.
- 2026-06-02 - T083 Source-yield analytics - done. Every source now exposes a legible **yield profile** — read %, extracts/cards/mature-cards created, leeches, and time spent — and the analytics screen ranks the **low-yield** sources (heavy investment, little durable knowledge produced) so the user can spot what to stop reading, exactly the Done-when. The metrics are READ-ONLY (a pure projection over the existing lineage + read-points + review tables; nothing is mutated, no `operation_log` row is appended). **Pure scorer in `@interleave/core` (`packages/core/src/source-yield.ts`), no DB/IPC/React**: given a source's raw counts it derives the yield rate (durable cards per unit of read effort), classifies LOW-yield via named thresholds, and orders the ranking deterministically (worst-yield-first, tie-broken by id) — so the same inputs always yield the same ranking and a tuning change is a deliberate, reviewed edit. **Read % is grounded in the real read-point**: it derives from the furthest `read_points` position over the document's block span (the same anchor the reader/lineage use), so "read %" means what the user actually advanced through, not a guess. **The four yield signals come from the existing tables, no new bookkeeping**: extracts/cards/mature-cards are counted by walking the `derived_from` lineage from the source down to its `card` descendants (`isCardMature` reused for the mature cut), leeches reuse the T040/T070 `is_leech` flag, and time-spent reuses the read-session/processing signal already recorded — so the analytics screen reflects the same lineage + maturity + leech truth the queue and inspector show, never a parallel count. **THE TWO-SCHEDULER SPLIT honored**: a card's maturity is read off its FSRS `review_states` signals (stability/`fsrsState`/retrievability via `isCardMature`); a source/extract's processing is read off the attention side — the analytics never crosses a card through the attention heuristic or an attention item through FSRS. **Composition seam — `SourceYieldQuery` (`packages/local-db/src/source-yield-query.ts`), READ-ONLY**: assembles each live (non-deleted) source's raw counts from the lineage walk + read-points + review tables and runs the pure scorer; `list()` returns the per-source yield profiles + the low-yield ranking; `forSource(id)` returns one source's profile for the inspector. It writes nothing and appends NO op. **No new op types** (the closed 15-op set is unchanged) and **no schema migration** (a pure read over existing tables). **Typed seam**: new channels — the source-yield list (the analytics ranking) + the per-source read (the inspector profile) — with zod request/result schemas (`contract.ts`/`contract.test.ts`), threaded `ipc.ts` → `db-service.ts` → `preload` → `apps/web/src/lib/appApi.ts`; NO generic mutation channel. **Renderer**: `SourceYield.tsx` (+ `source-yield.css`, routed under `/analytics` and linked from `AnalyticsScreen.tsx` + the shell `nav`) shows the per-source yield table + a low-yield ranking so low-yield sources are identifiable at a glance; the `Inspector` gains a yield row (read %, extracts/cards/mature, leeches, time spent) on a source. The renderer never counts lineage, classifies maturity, or touches FSRS — it shows what the main process computed. Covered by Vitest (`packages/core/src/source-yield.test.ts` — the yield-rate math, the low-yield classification thresholds, and the deterministic worst-first ranking; `packages/local-db/src/source-yield-query.test.ts` — the read-% from the furthest read-point, the lineage-walked extract/card/mature counts, the leech + time-spent signals, and the still-READ-ONLY contract; `apps/web/src/analytics/SourceYield.test.tsx` + `AnalyticsScreen.test.tsx` + `Inspector`; `nav.test.ts`, `contract.test.ts`, `db-service.test.ts`, `inspector-query.test.ts`) and a Playwright/Electron spec (`tests/electron/source-yield.spec.ts`: a read source with extracts/cards surfaces its read %/yield counts, a low-yield source is ranked, and the metrics survive an app restart). `pnpm typecheck` + `pnpm lint` + `pnpm test` all green.
- 2026-06-02 - T082 Mature-card retirement - done. A low-value MATURE card (high stability, low priority, well-learned) can now be RETIRED so it leaves active review GRACEFULLY — without being deleted or losing its lineage/history — exactly the Done-when. **The mechanism (decided, documented):** a durable `cards.is_retired` BOOLEAN flag (mirroring T040's `is_leech`), the SINGLE SOURCE OF TRUTH for "skip in the due/review reads". The considered alternative — a new `retired` value in `ELEMENT_STATUSES` — was REJECTED: a status has a far wider blast radius (every status switch, the inspector, the queue filters, the design `Status` badge set, a status migration), and "retired" is a CARD-QUALITY attribute like leech, NOT a lifecycle stage; the card stays `active`/`scheduled` underneath so un-retire is a pure flag flip back into rotation at its EXISTING `review_states.due_at`. **Retire ≠ suspend ≠ delete (kept independent):** suspend is "temporarily out, will return"; delete is a soft delete to trash; RETIRE is "done with, kept for reference, low-value" — a distinct, REVERSIBLE, non-destructive exit (`review_states`/`review_logs`/lineage all preserved; a card can be un-retired without un-deleting, etc.). **The drop-out is a DIFFERENT mechanism from the suspended exclusion:** `QueueRepository.dueCards` now `innerJoin`s `cards` and adds a `cards.is_retired = false` predicate (the suspended exclusion filters `elements.status`; `is_retired` is a `cards` side-table flag, so it needs an explicit join, not another status). The review deck (`ReviewSessionService`), the forward "due this week" count, and the queue/analytics due counts all read through `dueCards`, so a retired card disappears from review AUTOMATICALLY; the ATTENTION reads are UNTOUCHED — no card-retirement logic leaks to sources/extracts. **Optional low-retention lever (NOT the retirement mechanism):** `retire({ lowRetention: true })` may ALSO floor-clamp `cards.desired_retention` to `DESIRED_RETENTION_MIN` (0.8 — the lowest the T079 resolver honors; a lower value is clamped UP) as a convenience interval-lengthener for an eventual un-retire — but it is INDEPENDENT: the resolver clamp means the override can NEVER on its own remove a card from rotation; only the `is_retired` flag does, and un-retire clears ONLY the flag (clearing the override is a separate `retention.setCard(id, null)`). **THE TWO-SCHEDULER SPLIT (load-bearing):** a card is the only FSRS-scheduled element; retiring writes the `cards` row (a flag) + optionally the per-card retention override in ONE transaction — it NEVER writes `review_states` (FSRS memory state) or the append-only `review_logs`, and NEVER touches a non-card's `due_at`. **Composition seam — `CardRetirementService` (`packages/local-db/src/card-retirement-service.ts`):** `retire(id, { reason?, lowRetention? })` sets `is_retired = true` + an `update_element` audit (payload `{ id, retired: true, retiredAt, reason? }`, the optional `RetentionService.setCardRetention` floor-clamp running in the SAME transaction); `unretire(id)` clears the flag (`update_element` `{ retired: false, unretiredAt }`); `isRetired(id)` reads the durable flag; `listRetired()` is the READ-ONLY inventory — live (non-deleted) retired cards joined to their `review_states` memory signals (stability/reps/lapses/lastReviewed), most-stable (most mature) first. **No new op types** (the closed 15-op set is unchanged) and ONE additive migration `0020` (pure widening: `cards.is_retired` `integer notNull default false` — backfill-free — plus a `cards_is_retired_idx` for the due-read join + the inventory view). **Typed seam:** three new channels — `cards:retire` / `cards:unretire` (the only persisting commands) + `cards:retired` (the inventory read) — with zod request/result schemas (`contract.ts`/`contract.test.ts`), threaded `ipc.ts` → `db-service.ts` → `preload` → `apps/web/src/lib/appApi.ts` as `window.appApi.cards.retire`/`unretire`/`retired`; NO generic mutation channel. **Renderer:** a new maintenance `RetiredCards.tsx` inventory (routed at `/maintenance/retired`, linked from `AnalyticsScreen`) lists retired cards with their maturity signals + an un-retire action; the `Inspector` gains a Retire / Un-retire control on a card (with the optional low-retention checkbox); the `ReviewRepairBar` gains a Retire action so a low-value mature card can leave review in-session. The renderer never flips the flag, clamps, or touches FSRS — it sends the validated intent and the main process does the work. Covered by Vitest (`packages/local-db/src/card-retirement.test.ts` — retire sets the flag + `update_element` with the reason payload and NO review-state/log touched, the optional low-retention floor-clamp, un-retire is a pure flag flip restoring the existing due, `listRetired` most-stable-first excluding soft-deleted, and the retired card dropping out of `dueCards`/the weekly count; `RetiredCards.test.tsx`, `Inspector` + `ReviewRepairBar` component tests; `contract.test.ts`, `db-service.test.ts`) and a Playwright/Electron spec (`tests/electron/retirement.spec.ts`: retiring a mature card removes it from the review deck + the due count, the inventory lists it, un-retire returns it to rotation, and the retirement survives an app restart). `pnpm typecheck` + `pnpm lint` + `pnpm test` all green.
- 2026-06-02 - T081 Workload simulation - done. The user can now PREVIEW how their daily review/processing load shifts under a hypothetical change — altering desired retention, adding cards, or postponing low-priority material — BEFORE committing, exactly the Done-when. The preview is a before/after per-day due-load curve over the next N days with the daily-budget overload line + peak and over-budget-day deltas, and it is strictly READ-ONLY (it mutates nothing, appends no op — there is nothing to undo about previewing). **Pure, deterministic projector in `@interleave/scheduler` (`packages/scheduler/src/workload.ts`), no DB/IPC/React**: `projectWorkload(snapshot, change, { asOf, windowDays })` buckets each card's `review_states.dueAt` and each attention item's `elements.dueAt` into the next `windowDays` LOCAL-calendar days (overdue on day 0 — the SAME bucketing `QueueRepository.dueCards`/the analytics screen use, so the `before` series EQUALS what the user already sees), then re-buckets after applying a discriminated `WorkloadChange` for the `after` series, returning the per-day `before`/`after` counts + `overBudgetDaysBefore/After`, `peakBefore/After`, and `deltaNext7`/`deltaNext30`. **The levers (the spec's three change kinds, plus T080's reused fourth)**: (1) `retention` — lay the scope's new target (global / a band / a concept) over the live `RetentionTargets` as a CANDIDATE set, resolve each card's NEW effective target via the T079 `resolveDesiredRetention` ordered rule set, and — only when it ACTUALLY changed and the card has projectable memory (`stability > 0`, a `lastReviewedAt`, not `fsrsState:"new"`) — re-project its due to `lastReviewedAt + next_interval(stability, newTarget)` via the FSRS interval math (`nextIntervalDaysForRetention`), so a higher target pulls load EARLIER and a lower target pushes it LATER; unaffected cards keep their due; (2) `addCards` — keep the whole existing schedule and land `count` new cards' first review `firstDueInDays` out (the near-term spike a planned import / batch of new extracts creates — coarse-but-honest, the full learning-step cadence is deliberately NOT modeled); (3) `postponeLowPriority` — move low-priority (band C/D, at/below the chosen band) ATTENTION items out by `days`, and — ONLY with `includeMatureCards` — low-priority MATURE cards too, NEVER fragile or high-priority cards (exactly T077's protect-fragile rule, reusing `isCardMature`); (4) `applyParams` — re-project each in-scope card's due under a candidate FSRS `w` vector (T080's `OptimizationService.workloadImpactOf` is the DB-backed wrapper over THIS same engine, not a fork). **THE TWO-SCHEDULER SPLIT (load-bearing)**: a retention/params lever touches ONLY cards (FSRS); a postpone lever touches attention items + optional mature cards; the projection NEVER reschedules a card through the attention heuristic or an attention item through FSRS. Fully reproducible — `asOf` is injected (no hidden `Date.now()`), no `Math.random()` (the renderer's queue jitter stays a separate layer), a fixed snapshot + clock always yields the same projection. **Composition seam — `WorkloadService` (`packages/local-db/src/workload-service.ts`), READ-ONLY**: `buildSnapshot(asOf)` assembles the DB-free `WorkloadSnapshot` from the live tables — CARDS = live, non-excluded (`done`/`dismissed`/`suspended`/`deleted` filtered, mirroring `QUEUE_EXCLUDED_STATUSES`) `card` elements joined to `review_states` (FSRS due + memory state), each decorated with its concept NAMES (for the resolver's name keys), resolved FSRS preset (`RetentionService.resolveParamsForCard`), per-card override, and an approximate retrievability computed with the SAME `R(t) = (1 + FACTOR·t/S)^DECAY` the queue/inspector use (so the maturity classification agrees with the queue); ATTENTION = live, non-excluded non-`card` elements with an `elements.dueAt`; BUDGET = `settings.dailyReviewBudget`; TARGETS = the live `RetentionService.targets()`. `simulate(change, options)` builds the snapshot at `asOf` and runs the pure projector. It NEVER writes `review_states`/`due_at`/settings and appends NO `operation_log` row — the user `Commit`s a previewed change through the relevant EXISTING command (retention set / import / postpone), never through here. **No new op types** (the closed 15-op set is unchanged) and **no schema migration** (a pure read over existing tables). **Typed seam**: one new channel — `workload:simulate` (read-only preview) — with zod request/result schemas (a discriminated `WorkloadChangeSchema` retention|addCards|postponeLowPriority|applyParams; `contract.ts`/`contract.test.ts`), threaded `ipc.ts` → `db-service.ts` → `preload` → `apps/web/src/lib/appApi.ts` as `window.appApi.workload.simulate`; NO generic mutation channel, NO raw schedule crossing un-validated. **Renderer**: `WorkloadSimulator.tsx` (wired into `/settings`) lets the user pick a lever + value and Preview the daily-load shift — a pure-SVG before/after area chart with the budget overload line, "peak N/day → M/day", and "X over-budget days → Y" — with a `Commit` button that performs the REAL change via the relevant existing command (the preview itself commits nothing). The renderer never computes the projection, buckets a due date, or touches FSRS — it shows what the main process computed and sends the simulate intent. Covered by Vitest (`packages/scheduler/src/workload.test.ts` — the local-day bucketing with overdue-on-day-0, each lever's re-projection (retention higher-target-earlier/lower-later, the unaffected-card passthrough, the add-cards spike, the postpone band-and-below with fragile/high-priority protection + the `includeMatureCards` gate, the applyParams re-projection), the over-budget/peak/delta summaries, and full determinism on a fixed snapshot + clock; `packages/local-db/src/workload-service.test.ts` — the snapshot assembly (card⋈review_states with concept names/preset/override/retrievability, attention rows, excluded-status filtering matching the queue baseline, budget + targets) and the still-READ-ONLY simulate writing nothing; `WorkloadSimulator.test.tsx` — the lever pick + preview render + the commit-via-existing-command; `contract.test.ts`, `db-service.test.ts`) and a Playwright/Electron spec (`tests/electron/workload.spec.ts`: previewing a retention/add-cards/postpone change surfaces the before/after load curve + deltas, the simulation persists nothing (the schedule is unchanged after a preview), and the app behaves identically across a restart). `pnpm typecheck` + `pnpm lint` + `pnpm test` all green.
- 2026-06-02 - T080 FSRS parameter optimization - done. Accumulated review history can now be used to optimize FSRS parameters per **global preset** or **per concept**, surfaced as a SUGGESTED update (never auto-applied) with a workload-impact preview — exactly the Done-when. **HONESTY FIRST (the load-bearing constraint)**: `ts-fsrs@5.4.1` ships NO parameter optimizer/trainer/`computeParameters` (the gradient-descent trainer lives in the separate `fsrs-rs`/`fsrs-optimizer` projects we do NOT bundle, call a server for, or reimplement). So the pure optimizer in `@interleave/scheduler` (`packages/scheduler/src/fsrs-optimizer.ts`, no DB/IPC/React) does the honest thing with the primitives ts-fsrs DOES export (`forgetting_curve`/`default_w`/`clipParameters`/`checkParameters`): (1) `scoreParameters(history, w)` EVALUATES any candidate `w` against the user's real review history by replaying each card's ascending review sequence and scoring predicted recall `R = forgetting_curve(w, Δt, stability)` against the actual outcome (`recalled = rating ≠ again`) with a proper binary cross-entropy **log-loss + binned calibration RMSE** (exact, cheap, deterministic; the FULL `w` array is passed so decay off `w[20]` is read, not a decay scalar; a coarse but w-SENSITIVE stability replay scaled by the growth/lapse weights so a fit-to-data `w` scores better than an obviously-wrong one — NOT a re-implementation of the full FSRS-6 stability machine); (2) `suggestParameters(history, { current })` SEARCHES a small, bounded neighborhood — `default_w` + the current params + a few fixed presets + a bounded coordinate **hill-climb** over a few influential weights (initial stabilities + decay), every candidate `clipParameters`-clamped + `checkParameters`-validated via `sanitizeParams` (a malformed/wrong-length vector is dropped, never scored or suggested), keeping the lowest-log-loss set and NEVER returning params worse than the baseline. It is fully deterministic (fixed candidate order, fixed step schedule, no `Math.random`), labels its method `"history-calibration"` (NEVER "optimal"), and below a DATA FLOOR (`MIN_REVIEWS_FOR_FIT=200` reviews AND `MIN_CARDS_FOR_FIT=20` cards with ≥3 reviews, `hasSufficientData`) it suggests NOTHING (`sufficientData:false`, suggested params equal the baseline) so we never "optimize" on noise. The clean `FsrsOptimizer` interface lets a real `fsrs-rs`/wasm trainer drop in later BEHIND it without changing callers (`historyCalibrationOptimizer` is the v1). **THE TWO-SCHEDULER SPLIT (load-bearing)**: this scores + suggests FSRS (CARD) params ONLY — it never touches the attention scheduler, and the `ts-fsrs` dependency stays behind the scheduler package boundary (`@interleave/local-db` and the Electron main process never import `ts-fsrs`; the off-main worker posts a plain `w` vector + scores and MAIN re-wraps it into valid `FSRSParameters` via `optimizationSuggestionFromParts`/`generatorParameters`). **Composition seam — `OptimizationService` (`packages/local-db/src/optimization-service.ts`), the ONLY persisting seam**: `buildHistory(cardIds)` maps `review_logs` → the DB-free `OptimizerHistory`, RE-SORTING each card's logs ASCENDING (`listReviewLogs` returns DESC, so the Δt signs would otherwise invert) and DERIVING `elapsedDays` from consecutive `reviewedAt` deltas (there is no per-log `elapsedDays` column; first review = 0); `cardIdsForScope` resolves the scope to all LIVE `card` ids or one concept's member cards; `suggest(scope)` is READ-ONLY (build history → bounded search from the scope's CURRENT params → workload preview); `workloadImpactOf(candidateParams, scope)` is the READ-ONLY workload-impact preview — per-day card-due counts for the next 30 days BEFORE (the live `review_states.due_at`) vs AFTER re-projecting each in-scope card's next interval under the candidate params (a single-step `next_interval` projection anchored at `lastReviewedAt`, the FSRS math kept behind `@interleave/scheduler nextIntervalDaysForParams`; a malformed `w`/state → keep the current due date), plus `deltaDueNext7`/`deltaDueNext30` summary deltas, labeled an estimate, writing nothing; `apply(scope, params)` is the ONLY persisting method — it `sanitizeParams`-validates first (a malformed vector is REJECTED, never stored), then writes to the QUERYABLE store: `scope:"global"` → the `fsrs.params.global` SETTING (via `SettingsRepository.updateAppSettings`, no op, T011); `scope:"concept"` → the `concepts.fsrs_params` COLUMN + an `update_element` audit on the concept element in ONE transaction (`ConceptRepository.setConceptFsrsParams`). Apply does NOT retroactively reschedule existing cards (new grades use the new params); `schedulerForCard` (T079) READS those stores so resolved scheduling actually uses them. **No new op types** (the closed 15-op set is unchanged) — an apply is a `settings` write or an `update_element`, NEVER a `review_states`/`review_logs` write, NEVER an attention item's `due_at`. **Off-main runner**: a new `fsrs_optimize` job type (additive migration `0019`, pure widening of the `jobs.type` CHECK; no new tables) routes a LARGE history (`HEAVY_FIT_REVIEW_THRESHOLD=5000` reviews) to the background worker so the bounded search runs OFF the main thread; MAIN builds the DB-free payload (`buildJobPayload`) and re-composes the result with the DB-backed workload preview on the main side (`withWorkload`), the worker importing only `@interleave/scheduler` (never `@interleave/db`). **Typed seam**: two new channels — `optimization:suggest` (read-only estimate + workload preview) + `optimization:apply` (the only persisting command) — with zod request/result schemas (a discriminated `OptimizationScopeSchema` global|concept; `contract.ts`/`contract.test.ts`), threaded `ipc.ts` → `db-service.ts` → `preload` → `apps/web/src/lib/appApi.ts` as `window.appApi.optimization.suggest`/`apply`; NO generic mutation channel, NO raw params crossing un-validated. **Renderer**: `OptimizationPanel.tsx` (wired into `/settings`) lets the user pick a scope (global / a concept), run a suggest, and see the baseline vs suggested **log-loss + RMSE** with the honest `"history-calibration"` method label + a "below the data floor, nothing to suggest" state, the before/after **workload curve** + the 7-/30-day load deltas, and an explicit **Apply** button (the suggestion is NEVER auto-applied) — the renderer never scores, searches, clamps, or touches FSRS; it shows what the main process computed and sends the apply intent. Covered by Vitest (`packages/scheduler/src/fsrs-optimizer.test.ts` — log-loss/RMSE scoring, the bounded search never-worse-than-baseline, the data-floor suppression, `sanitizeParams` rejection of malformed/wrong-length vectors, determinism, and the runner round-trip via `optimizationSuggestionFromParts`; `packages/local-db/src/optimization-service.test.ts` — the DESC→ASC history mapping + derived `elapsedDays`, the scope resolution global vs concept members, the read-only suggest + workload before/after/deltas, the apply persisting to the global setting vs the concept column + `update_element` audit and rejecting a bad vector, and the heavy-fit payload build; `OptimizationPanel.test.tsx` — the suggest render + below-floor state + confirm-applies; `contract.test.ts`, `db-service.test.ts`, `jobs-repository.test.ts`, `job-runner.integration.test.ts`) and a Playwright/Electron spec (`tests/electron/optimization.spec.ts`: accumulate review history → suggest surfaces baseline vs suggested + the workload preview → apply persists the global preset, with the applied params surviving an app restart). `pnpm typecheck` + `pnpm lint` + `pnpm test` all green.
- 2026-06-02 - T079 Desired retention by priority/concept - done. FSRS no longer schedules every card against ONE global desired-retention number — a card's EFFECTIVE target is now RESOLVED from a small ORDERED rule set (per-card override → strictest concept → A/B/C/D priority band → global default), so high-value/fragile concepts can be held at, say, `0.92` while low-value/background material sits at `0.85` (longer intervals, less daily load), exactly per `docs/scheduling-and-priority.md`'s "retention targets can differ by concept or priority band". **Pure resolver in `@interleave/scheduler` (`packages/scheduler/src/retention.ts`), no DB/IPC/React and (deliberately) NO `ts-fsrs`**: `resolveDesiredRetentionDetailed({ priority, conceptNames, cardOverride, targets })` returns the target + WHICH rule won (`"card"|"concept"|"band"|"global"`) — (1) a finite per-card override always wins; (2) when `enabled` and the card has a concept whose NAME has a `byConcept` entry, the HIGHEST (strictest) target among the card's concepts (averaging would silently under-protect a fragile card shared with a casual one); (3) the `byBand[priorityToLabel(priority)]` target (an ABSENT band cleanly inherits global); (4) the global default. **EVERY branch is CLAMPED to `[DESIRED_RETENTION_MIN, DESIRED_RETENTION_MAX]`** so a corrupt stored value (a near-zero override, an out-of-range band) can NEVER reach FSRS — the same choke-point discipline as core's `coerceSettingValue`; an override can't reach a self-retiring near-zero target (T082's `is_retired` is the retirement mechanism, not this). When `enabled` is `false` only the card override + global apply, so toggling the feature off is a CLEAN REVERT to the T036 single number. **THE TWO-SCHEDULER SPLIT (load-bearing)**: this is CARD-ONLY — it feeds `CardSchedulerService` (FSRS) the resolved `request_retention` and NEVER touches the attention scheduler; `conceptNames` and `byConcept`'s keys are both concept NAMES (not `ElementId`s — the scheduler factory maps memberships → names before resolving), and the resolver test pins the T036 invariant that a HIGHER target schedules a SHORTER interval for the same grade. **Schema (one additive migration `0018`, pure widening, no backfill)**: `cards.desired_retention` (the per-card override; existing cards `null` = inherit), `concepts.desired_retention` (the per-concept target), and `concepts.fsrs_params` (reserved for T080's per-concept optimized params) — all nullable `real`/`text`, no new tables. **Settings model (`@interleave/core`)**: `defaultDesiredRetention` (the global, T036) joined by `retentionByBand` (a PARTIAL A/B/C/D map — a missing band inherits global, so an absent/empty map is a clean no-op) + a `retentionByBandEnabled` master switch, all validated through the existing typed settings surface (no op, T011). **Composition seam — `RetentionService` (`packages/local-db/src/retention-service.ts`)**: the only thing that READS the three stores + WRITES the per-card override. `targets()` assembles the live `RetentionTargets` (`global` from settings, `byBand` from settings, `byConcept` from a new `ConceptRepository.retentionTargets()` name-keyed `Math.max` dedup, `enabled` = the band switch OR any per-concept target exists, so a per-concept target engages the resolver even with the band feature off); `resolveForCard(id)` reads the card's `elements.priority` + concept NAMES + `cards.desired_retention` and runs the pure resolver (returns global + `source:"global"` for a non-card/unknown id — a safe fallback, the factory never schedules a non-card); `setCardRetention(id, value)` writes ONLY `cards.desired_retention` (clamped at this choke point) + an `update_element` audit (with the prior value in the op pre-image) in ONE transaction — it NEVER writes `review_states`/`review_logs` (no FSRS memory state) and NEVER touches a non-card's `due_at`. `ConceptRepository` likewise gains a per-concept-target writer logging `update_element`. **No new op types** (the closed 15-op set is unchanged). **Per-card scheduler factory + CACHE (`db-service.ts`)**: the FSRS review path now builds a `CardSchedulerService` configured with the card's RESOLVED target, cached keyed by the target ROUNDED to `0.001` (so we build at most ~one scheduler per distinct target, not per card); any `retention.*` write bumps a cache generation that lazily invalidates it, so a target change takes effect immediately without leaking stale schedulers. **Typed seam**: six new channels — `retention:get` (the global + per-band + per-concept read), `retention:setBand`, `retention:setBandEnabled`, `retention:setConcept`, `retention:setCard`, and `retention:resolveFor` (the inspector/debug read of a card's resolved target + winning rule) — with zod request/result schemas (`contract.ts`/`contract.test.ts`), every target validated as a probability in the supported band; threaded `ipc.ts` → `db-service.ts` → `preload` → `apps/web/src/lib/appApi.ts` as `window.appApi.getRetention`/`setRetentionBand`/`setRetentionBandEnabled`/`setRetentionConcept`/`setRetentionCard`/`resolveRetentionFor`; NO generic mutation channel. **Renderer**: `/settings` (`Settings.tsx`) gains a retention-by-band panel — a master toggle + per-A/B/C/D sliders that persist through `retention.setBand`(+`Enabled`); `ConceptsScreen.tsx` (+ `concepts.css`) gains a per-concept retention slider (with a reset-to-inherit) on the selected concept; the `Inspector` gains a READ-ONLY "Target retention" row showing a card's resolved target + which rule won (`retention.resolveFor`). The renderer never resolves a target, clamps, or touches FSRS — it shows the resolved value the main process computed and sends the intent. Covered by Vitest (`packages/scheduler/src/retention.test.ts` — the ordered rule set, strictest-concept-wins, clamping at every branch, the `enabled:false` clean-revert, and the higher-target→shorter-interval smoke check through the real scheduler; `packages/local-db/src/retention-service.test.ts` — `targets()` assembly, `resolveForCard` per-rule dispatch + the non-card fallback, and the `setCardRetention` clamped write with the `update_element` pre-image and NO review-state/log touched; `concept-repository.test.ts` — the name-keyed target read + writer; `ConceptsScreen`/`Inspector`/`Settings` component tests; `contract.test.ts`, `db-service.test.ts`) and a Playwright/Electron spec (`tests/electron/retention.spec.ts`: raising the B-band target in `/settings` schedules a shorter next interval for a B-card on the same grade, a per-concept target resolves for a card in that concept over the band via `retention.resolveFor`, and both targets + the resulting scheduling survive an app restart). `pnpm typecheck` + `pnpm lint` + `pnpm test` all green.
- 2026-06-02 - T078 Catch-up & vacation modes - done. The queue now offers the two human-facing **overload tools** `docs/scheduling-and-priority.md` asks for, and BOTH show the **cost of postponement BEFORE committing** (the Done-when): **catch-up** spreads an overdue backlog forward so each day stays within budget (recovering gracefully from a wall), and **vacation** pre-adjusts the load over an away window so the user returns to a survivable queue instead of the full backlog on day one. **Pure planners in `@interleave/scheduler` (`packages/scheduler/src/recovery-modes.ts`), no DB/IPC/React and (deliberately) NO `ts-fsrs` import** — they read the already-computed retrievability/stability/`fsrsState`/lapse signals off the flat queue row (the two-scheduler split stays READ-ONLY here), with `asOf`/`awayStart`/`awayEnd` injected and NO `Math.random`, so the same input always yields the same plan. `planCatchUp({ items, budget, asOf, spreadDays, mode })` ranks the backlog by VALUE DESCENDING (the REVERSE of T077's victim order — reusing T076's `queueItemScore` + `DEFAULT_QUEUE_SCORE_WEIGHTS`, with a deliberate **fragile-card value boost** so the most perishable memory recovers FIRST), then greedily fills the EARLIEST day with remaining capacity `< budget` (day 0, then day 1, …), so high-value/fragile items land soonest and low-value slides to the back; overflow past `spreadDays × budget` lands on the last day (shown over budget in the cost curve, never silently dropped), and only items that actually move LATER appear in the plan. `planVacation({ items, awayStart, awayEnd, asOf, budget, mode })` finds everything due IN `[awayStart, awayEnd]` and partitions it by the **suspend-vs-shift rule**: a FRAGILE card (`isCardFragile`, reused from T077) is SUSPENDED so its FSRS trajectory simply PAUSES for the trip (never pushed weeks out — "high-value fragile memory is protected" still holds: it is paused and resumed, not sacrificed to a far-future date); everything else (attention items + mature cards) is SHIFTED past return and re-spread over the days AFTER `awayEnd` within budget, high-value first. **Both return the same `PostponeCostPreview`** — `moved`, `newTailDueAt`, `daysAdded`, the per-day `loadBefore`/`loadAfter` curves (aligned day-for-day so the extension/flattening is visible; days with no load still appear at count 0), and a per-item `slips` list (old→new due + `slipDays`, largest-slip-first) — so the cost is fully quantified BEFORE committing. **APPLY seam — `RecoveryModeService` (`packages/local-db/src/recovery-mode-service.ts`), the only thing that persists a plan**: composes `QueueQuery.list` (the same decorated due set the renderer sees + the `budget` gauge from `getAppSettings().dailyReviewBudget`), the pure planner, and the THREE apply seams — an attention move reschedules to the EXACT planned day via `SchedulerService.scheduleAt(id, { manual: targetDueAt }, now, batchId)` (`reschedule_element`); a card move defers to the EXACT planned day via the new `QueueActionService.cardDeferTo(id, now, targetDueAt, batchId)` — an FSRS-aware ABSOLUTE defer that sets ONLY `review_states.due_at` (+ `elements.due_at`), leaving `stability`/`difficulty`/`reps`/`lapses`/`fsrsState` UNTOUCHED and writing NO review log (so the applied per-day load curve matches the preview day-for-day, even for an overdue card); and a vacation suspend flips status → `suspended` via `ElementRepository.update` (`update_element`, prior status captured in the op pre-image so resume restores it exactly). `spreadDays` is clamped to 1–60 (`DEFAULT_CATCHUP_SPREAD_DAYS=7`) so a malformed request can't break the spread. Previews are READ-ONLY (no mutation, no op); applies mint ONE `batchId` so the whole plan undoes as a SINGLE batch via the existing T044 command-level undo — **no new op types** (the closed 15-op set is unchanged) and **no schema migration**. `QueueItemSummary` is a structural superset of the planner's `RecoveryInput` (it carries priority/scheduler/`schedulerSignals`/`protected`/title/dueAt), so the live rows hand straight to the pure planner with a widening cast — no DB access in `packages/scheduler`. **Typed seam**: four new channels — `queue:catchUp`(+`:apply`) and `queue:vacation`(+`:apply`) — with zod request/result schemas (`contract.ts`/`contract.test.ts`), threaded `db-service.ts` → `preload` → `apps/web/src/lib/appApi.ts` as `window.appApi.queue.previewCatchUp`/`applyCatchUp` / `previewVacation`/`applyVacation`; NO generic mutation channel. **Renderer**: `RecoveryPanel.tsx` (+ `queue.css`, wired into `QueueScreen.tsx`) gives both modes a preview that makes the cost EXPLICIT — a before/after per-day **load curve** (the overloaded wall flattening to ≤ budget, with a dashed budget line + over-budget bars highlighted) and a "N items now due up to D days later" `slips` summary — then applies the plan and raises an undo snackbar; vacation adds an away/return date-range picker that clears the stale preview on edit. The renderer never plans, scores, touches FSRS, or resolves a due date — it shows the preview the main process computed and sends the intent. Covered by Vitest (`packages/scheduler/src/recovery-modes.test.ts` — the value-descending + fragile-boost ranking, the earliest-day spread with the per-day cap, the overflow-on-last-day, the suspend-vs-shift partition, the aligned before/after curves + slips math, and full determinism on a fixed input; `packages/local-db/src/recovery-mode-service.test.ts` — the read-only previews, the per-scheduler dispatch incl. the card absolute-defer with memory state UNTOUCHED + no review log, the vacation suspend with prior-status pre-image, the shared `batchId` single-undo restoring everything, and the `spreadDays` clamp; `apps/web/src/pages/queue/RecoveryPanel.test.tsx` — the cost preview render + confirm-applies + away-range validation; `contract.test.ts`) and a Playwright/Electron spec (`tests/electron/recovery-modes.spec.ts`: an overloaded backlog catch-up flattens the daily load while a high-priority fragile card stays at the front, a vacation suspends fragile cards over the away window and shifts the rest past return, and both — plus a single-batch undo — survive an app restart). `pnpm typecheck` + `pnpm lint` + `pnpm test` all green.
- 2026-06-02 - T077 Auto-postpone - done. When the due load exceeds the daily review budget, the queue now offers a deterministic **overload valve** that postpones the overflow BY VALUE — low-priority topics/sources/extracts first, then low-priority *mature* cards — while **never touching high-priority fragile cards** (or leeches under repair, or explicitly-protected band-A items), exactly per `docs/scheduling-and-priority.md`. **Pure planner in `@interleave/scheduler` (`packages/scheduler/src/auto-postpone.ts`), no DB/IPC/React and (deliberately) NO `ts-fsrs` import**: `planAutoPostpone(items, { budget, asOf, protectHighPriority, mode })` reads the already-computed retrievability/stability/`fsrsState`/lapse signals off the flat queue row (the two-scheduler split stays READ-ONLY here), computes `overflow = max(0, items.length - budget)`, and — if positive — partitions eligible victims into two tiers, draining **low-priority attention items first, then low-priority mature cards**, stopping the instant the remaining due count is back within budget. **THE FRAGILE↔MATURE CUTLINE is the protection rule**: `isCardMature` ⇔ `fsrsState === "review"` AND `stability ≥ CARD_MATURE_STABILITY_DAYS` (21, a named exported const) AND (when retrievability is known) `retrievability ≥ CARD_MATURE_RETRIEVABILITY` (0.9, mirroring the FSRS desired-retention default); anything new/learning/relearning or below those floors is FRAGILE and is `isCardFragile` — never sacrificed, so a high-priority fragile card keeps its place even under heavy overload, and a card whose recall has already decayed below 0.9 is protected even with high stability (it is exactly the memory about to be lost). Bands mirror `@interleave/core priorityToLabel` (A ≥ 0.75 protected; low = C/D, < 0.5); leeches (`lapses ≥ LEECH_LAPSE_THRESHOLD`, reusing the T070 `isLeech`) and `protected` rows are excluded belt-and-suspenders. Within each tier victims rank by **T076's `queueItemScore`** (ascending — least valuable right now first, reusing `DEFAULT_QUEUE_SCORE_WEIGHTS`), tie-broken by id ASC, with injected `asOf` and NO `Math.random` so the same due set + budget always yields the same plan. **APPLY seam — `AutoPostponeService` (`packages/local-db/src/auto-postpone-service.ts`), the only thing that persists the plan**: composes `QueueQuery.list` (the same decorated due set the renderer sees + the `budget` gauge where target = `getAppSettings().dailyReviewBudget`, used = current due count), the pure planner, and the TWO scheduler-specific apply seams — an attention victim reschedules via `SchedulerService.rescheduleForAction(id,"postpone",now,batchId)` (`reschedule_element`, status → `scheduled`); a card victim defers via the shared `QueueActionService.cardDeferBy(id,now,AUTO_POSTPONE_CARD_DEFER_DAYS=7,batchId)` — an FSRS-aware RELATIVE defer (`nextDue = max(prevReviewDueAt, now) + days`, generalized from the M5 single-day thin defer) that moves ONLY `review_states.due_at` (+ `elements.due_at`), leaving `stability`/`difficulty`/`reps`/`lapses`/`fsrsState` UNTOUCHED and writing NO review log (a postpone is not a lapse). `preview()` is READ-ONLY (no mutation, no op): it projects each victim from→to (card defer math; attention via the new no-mutation `SchedulerService.previewPostpone`, so the preview MATCHES the apply) so the **cost of postponement is legible BEFORE committing**. `apply()` mints ONE `batchId` so the whole sweep undoes as a single batch via the existing T044 command-level undo — **no new op types** (the closed 15-op set is unchanged) and **no schema migration**. **`QueueItemSummary` (`packages/local-db/src/queue-query.ts`) is a structural superset of the planner's `AutoPostponeInput`** — it now also carries the richer `schedulerSignals` (retrievability/stability/`fsrsState`/lapses) + the existing `protected` flag (A-band) — so the service hands the live rows straight to the pure planner with a widening cast, no DB access in `packages/scheduler`. **Typed seam**: two new channels — `queue:autoPostpone` (preview) + `queue:autoPostpone:apply` — with zod request/result schemas (`contract.ts`/`contract.test.ts`), threaded `db-service.ts` → `preload` → `apps/web/src/lib/appApi.ts` as `window.appApi.queue.autoPostpone` / `…Apply`; NO generic mutation channel. **Renderer**: `QueueScreen.tsx` (+ `queue.css`) gains an `OverloadBanner` that appears only when over budget, shows the over-budget count + the ordered victims with their from→to + reason, and applies the sweep on confirm (the renderer never plans, scores, touches FSRS, or resolves a due date — it shows the preview the main process computed). Covered by Vitest (`packages/scheduler/src/auto-postpone.test.ts` — the fragile/mature cutline incl. the stability + retrievability floors, the two-tier drain order, the stop-at-budget, the high-priority-fragile + leech + protected exclusions, the score-then-id victim ranking, and full determinism on a fixed input; `packages/local-db/src/auto-postpone-service.test.ts` — the preview from→to projection matching the apply, the per-scheduler dispatch (attention reschedule vs card thin-defer with memory state untouched + no review log), the shared `batchId` single-undo, and the still-READ-ONLY preview; `apps/web/src/pages/queue/OverloadBanner.test.tsx` — the over-budget-only render + confirm-applies; `HomeScreen`/`QueueScreen`/`ProcessQueue` snapshots; `contract.test.ts`) and a Playwright/Electron spec (`tests/electron/auto-postpone.spec.ts`: overloading the due set surfaces the banner, applying it postpones low-priority items first while a high-priority fragile card stays due, and the postponement survives an app restart). `pnpm typecheck` + `pnpm lint` + `pnpm test` (1644 tests) all green.
- 2026-06-02 - T076 Advanced auto-sort - done. The daily queue now ORDERS by a single deterministic **scoring function** over the seven roadmap factors — priority, due date, retrievability, element type, sibling spacing, concept diversity, and session mode — replacing the old two-key `priority desc, due_at asc` comparison, all as PURE domain logic with no DB/IPC/React and (deliberately) NO `ts-fsrs` import. **The scorer lives in `@interleave/scheduler` (`packages/scheduler/src/queue-score.ts`), pure + reusable**: `scoreQueueItems(items, { mode, asOf, weights })` is a documented WEIGHTED SUM of normalized `[0,1]` factors — `priority` (the DOMINANT term, weight `1.0`, so "high-priority fragile memory is protected" and "new material can't dominate older high-value work" both hold), `dueUrgency` (`0.55`; a future row scores `0`, due-today ~`0.5`, ramping linearly to `1.0` at `DUE_URGENCY_SATURATION_DAYS`=21 days overdue so an ancient overdue D-item can't leapfrog a fresh A-item), `retrievability` (`0.3`; for a CARD lower R scores higher = `1 - R`, since a card about to be forgotten is more urgent — read off the already-computed `approximateRetrievability` the queue row carries, never recomputed, so the two-scheduler split stays READ-ONLY here; attention rows have no R and use the `NEUTRAL_RETRIEVABILITY` midpoint so they're neither buried nor floated), and a mode-modulated `type` bias (`0.12`; `review` floats cards, `read` floats reading/processing items, `full` is the neutral midpoint — a SOFT up-weight, NOT a filter, so both types always stay in the deck). All weights/constants are NAMED EXPORTED constants pinned in the tests so a tuning change is a deliberate, reviewed edit. **Two DETERMINISTIC de-clumping passes on top (never a re-weight)**: after the score sort (ties broken by id ASC, so a fixed input always yields a fixed order), `declump` greedily spaces rows sharing a key so two related items aren't adjacent — run as THREE passes (sibling-group, then same-source, then concept) so the M7 "siblings not back-to-back" rule generalizes to source + topic diversity. Each pass is a BOUNDED reorder: an item is never pulled more than `DECLUMP_MAX_PUSHDOWN`=3 ranks ahead of its score position (a genuinely top-scoring item can never be starved to the back for the sake of variety), with a frequency-aware tiebreak inside the look-ahead window so a majority key isn't stranded into an unavoidable tail clump, and a `null` key never clumps. No `Math.random` anywhere — the 10–20% jitter the daily-queue rule asks for stays a SEPARATE seeded layer in the renderer (`apps/web/src/pages/queue/jitter.ts`), applied ON TOP of this deterministic order so re-renders never reshuffle. `queueItemScore` is exported so T077's auto-postpone can reuse the same "what is most/least valuable right now" reasoning to rank postpone victims. **Wired into the read (`QueueQuery.list`, `packages/local-db/src/queue-query.ts`)**: the merged due-cards + due-attention list is now ordered by `scoreQueueItems` (modulated by the new optional `mode`, default `"full"`) instead of the old two-key sort; `QueueItemSummary` grows two de-clumping keys — `siblingGroupId` (cards only, resolved BATCHED from ONE `sibling_group` `element_relations` read via a new `ElementRepository.liveSiblingGroupMap()` rather than N per-card lookups) and `sourceId` (already in scope from `sourceContext`); non-cards are always `null` on both. Still READ-ONLY (no mutation, no `operation_log` append). **Typed seam**: `QueueListRequest`/`QueueListRequestSchema` grow an optional `mode: "full"|"review"|"read"` (zod-validated in `contract.ts`/`contract.test.ts`) threaded `db-service.ts` → `preload` → `apps/web/src/lib/appApi.ts`; `QueueItemSummary` grows `siblingGroupId`/`sourceId` across the contract + appApi mirror. **Renderer**: `ProcessQueue.tsx`'s session-mode `Segmented` header is now a REAL ordering control — selecting a mode is the ONE deliberate re-fetch that passes `mode` to `queue.list` (the order changes, NOT the membership), and the old presentational `modeIncludes` HARD FILTER is GONE (both cards and reading items always stay in the deck; the mode only re-orders them), so a user can no longer accidentally empty their queue by switching to "review". Covered by Vitest (`packages/scheduler/src/queue-score.test.ts` — the weighted-sum factor math incl. the priority-dominates/new-can't-dominate invariant, the due-urgency ramp + saturation, the `1-R` card retrievability + the neutral-midpoint attention fallback, the mode type-bias, the bounded de-clumping with the pushdown cap + frequency tiebreak + the unavoidable-tail-clump fallback, and full determinism on a fixed input; `packages/local-db/src/queue-query.test.ts` — the score-ordered list, the batched sibling-group + source key resolution, the `mode` modulation, and the still-READ-ONLY contract; `apps/web/src/pages/queue/ProcessQueue.test.tsx` — the mode re-fetch + the membership-never-sliced behavior; `HomeScreen`/`QueueScreen` snapshots; `contract.test.ts`). `pnpm typecheck` + `pnpm lint` + `pnpm test` (1618 tests) all green.
- 2026-06-02 - T075 Audio review cards - done. A T074 clip `media_fragment` now distills into a real FSRS **audio card** whose looped time-window plays during active recall — an audio prompt, an audio answer, or both — all on-device (the renderer never resolves a vault path or touches SQLite). **Extend, do NOT fork (the load-bearing decision)**: an audio card is the EXISTING `card` model with a presentation modifier — NOT a new `kind`, NOT a parallel table — so it flows through the SAME `CardService` authoring, the SAME FSRS scheduling, the SAME review-session selection/grading/burying as a text card; the only addition is which clip to LOOP and on which face. The card can ALSO carry a written side (an audio-prompt card with a typed answer is the canonical language-learning shape), so the modifier is orthogonal to the `qa`/`cloze` kind. **Loops the ORIGINAL by time, never a cut file (carries T074's asset-free invariant forward)**: the `media_ref` points at the media **`source`** element (the original asset) + an integer-millisecond `[startMs, endMs)` window, and the player seeks the original between those bounds — no re-encoding, no sub-file, the milestone stays `ffmpeg`-free. **Schema (one additive column, one migration `0017`)**: `cards.media_ref` carries the ref JSON — pure widening, no backfill (existing cards get `null`), mirroring the `source_locations.clip`/`region` columns. **Core type (`MediaRef` in `@interleave/core`, dependency-free)**: `{ sourceElementId, startMs, endMs, on: "prompt"|"answer"|"both" }` plus a pure `parseMediaRef` validator that accepts EITHER a parsed object OR the raw JSON cell `string` and degrades a malformed value to `null` ("no audio") rather than throwing on read — the same parse discipline as `clip`/`region`; no Zod in core (the IPC `MediaRefSchema` lives in the desktop contract). **Authoring (`CardService.createFromExtract`, `packages/local-db`)**: when the originating extract is a clip `media_fragment` (its `source_locations` row carries a `clip` window), the service DERIVES the ref from that window + the media `sourceId` and defaults `on: "prompt"` — so the builder can mint an audio card from just the clip extract id — while an EXPLICIT ref (the renderer chose the face) always wins; a non-clip extract yields `null` (every text card unchanged). The card copies the window so it is SELF-CONTAINED (it needn't re-resolve the fragment), inherits the clip's `source_locations` anchor (jump-to-source in review seeks the clip), and is authored in the existing single transaction — a real `card` element + `cards` row + UN-DUE `review_states` row (FSRS owns the first schedule + the `card_draft → active_card` transition) + lineage edge + inherited tags/priority. **Two-scheduler split honored**: the clip `media_fragment` stays an ATTENTION item with NO `review_states` row; only the authored audio CARD gets an FSRS row — and it is selected, graded, and rescheduled by the FSRS path EXACTLY like a text card, writing a durable `review_logs` row + an `add_review_log` op. Sibling burying: two audio cards from one clip set share a `siblingGroupId`. **Card-quality, audio-aware (`packages/core/card-quality.ts`)**: a looped audio clip IS the content of the face it plays on, so the hollow-card blocker treats an audio-covered face as "filled" (an audio-prompt card with empty text is NOT a hollow prompt — the audio is the prompt); a NEW `long-audio-clip` advisory warns when the clip runs over `LONG_AUDIO_CLIP_MS` (30 s) per the minimum-information principle (a 30 s clip usually bundles more than one phrase); both signals are optional + backward-compatible (a text card omits them and behaves exactly as before). **ReviewCardView resolution (main-side)**: `ReviewRepository`/`db-service` parse `cards.media_ref` and resolve the media source's kind (`local` vs `youtube`) + youtube id onto the view, so the renderer plays without a second round-trip and never resolves a path. **Typed seam**: the `cards:create` contract grows the optional `mediaRef`, and the `ReviewCardView` grows `mediaRef`/`mediaSource`/`youtubeId` (`contract.ts`/`contract.test.ts`); threaded through `db-service.ts` → `preload` → `apps/web/src/lib/appApi.ts`; no generic mutation channel. **Renderer**: `CardAudioFace.tsx` loops the `[startMs,endMs)` window — for a LOCAL source via the privileged `media://<sourceElementId>` HTML5 `<audio>` with a tight `requestAnimationFrame` re-seek at the boundary (the coarse ~4 Hz `timeupdate` event would overrun a short clip by up to ~250 ms; it's kept only as a safety net), for a YouTube source via a sandboxed IFrame Player seeked to the window (the bytes never leave YouTube), degrading to a watch link when the API isn't ready; `ReviewScreen.tsx` (+ `review.css`) mounts it ONLY on the face `media_ref.on` covers — the front for `{prompt,both}`, the reveal for `{answer,both}` — so the audio NEVER leaks the answer; `CardBuilder.tsx` (+ `extract-view.css`) gained an audio-card affordance to author the loop face from a clip extract; the inspector surfaces the clip window. No fs/fetch/parse/SQL in React, no FSRS math, no bytes cut. Covered by Vitest (`packages/core/media-ref.test.ts` — the `parseMediaRef` validator incl. the string/object inputs + the inverted/negative/unknown-face → `null` degrades; `packages/core/card-quality.test.ts` — the audio-covered hollow-face exemption + the `long-audio-clip` warn/ok; `packages/local-db/card-service.test.ts` — the derived-from-clip ref, the explicit-ref override, the copied window, the inherited clip location, the `null` for a text extract; `apps/desktop/src/main/audio-card-service.test.ts` — the full lineage import→clip→audio-card path: `media_ref` stored + surfaced on the `ReviewCardView` with the resolved `mediaSource`/`youtubeId`, the two-scheduler split (clip has NO review_states, card does), FSRS select/grade/reschedule + durable `review_logs`, sibling `siblingGroupId`, and the media_ref + lineage + FSRS state surviving a DB re-open; `apps/web/src/review/CardAudioFace.test.tsx`, `ReviewScreen.test.tsx` — the face-gated mount so the answer never leaks; `contract.test.ts`, `db-service.test.ts`, `ProcessQueue`/`ReviewRepairBar` snapshots, `backup-manifest`/`backup-service`) and a Playwright/Electron spec (`tests/electron/audio-card.spec.ts`: authoring an audio card from a clip mints an FSRS card whose looped clip plays on the chosen face during review, grading reschedules it, and the card + its `media_ref` + lineage survive an app restart). `pnpm typecheck` + `pnpm lint` + `pnpm test` all green.
- 2026-06-02 - T074 Video/audio clip extraction - done. In the T073 media reader, selecting a start/end timestamp (a transcript-cue run, or a scrubber range) now mints a scheduled **`media_fragment`** clip extract whose source-location carries the start `timestamp_ms` + a `clip { startMs, endMs }` window and whose body holds the transcript segment under the range — its own attention-scheduled mini-topic with full lineage back to the media source, entirely on-device (the renderer never resolves a vault path or touches SQLite). **Asset-free by design (the load-bearing scope decision)**: a clip is a TIME WINDOW onto the ORIGINAL media, NOT a cut/re-encoded sub-file — the reader and the T075 audio card seek the original between `startMs`/`endMs` — so unlike `PdfRegionService` (which streams a cropped PNG into the vault), this path imports NO asset and the whole clip extract is ONE atomic transaction with no out-of-tx asset step and therefore no asset rollback, keeping the milestone `ffmpeg`-free (the ~70 MB native binary stays out of scope; the original media file is the single source of bytes, every clip + audio card references it by time). **Schema (one additive column, one migration `0016`)**: `source_locations.clip` carries the `{ startMs, endMs }` window JSON — pure widening, no backfill (existing rows get `null`), mirroring the T065 `region` column; `ClipWindow` is a new `@interleave/core` type and `ElementLocation.clip` surfaces it (the location's `timestampMs` mirrors `startMs` so a fixed-length window is recoverable from either). **Extraction core (`ExtractionService.createClipExtract`, `packages/local-db`)**: mirrors `createRegionExtract` but the anchor carries `timestampMs = startMs` + `clip` (NOT a page+region), the body is the transcript segment (or a generated "Clip M:SS-M:SS" caption paragraph when transcript-less) via `plainTextToProseMirrorDoc`, the label reads "Clip M:SS-M:SS" (a new `deriveClipLabel` in `source-location-label.ts`), `selectedText` snapshots the transcript segment (else the label), and the title prefers an explicit caption then a trimmed transcript prefix then the clip label (never empty); everything else matches the extraction path — a `derived_from` edge, inherited priority/tags, stage `raw_extract`, and an initial ATTENTION `due_at` (NEVER FSRS — no `review_states` row), all in ONE transaction (`create_element` + `create_extract`). **Main-side orchestrator** `MediaClipService` (`apps/desktop/src/main`, construction-injected `{ db, repositories, extraction }`): validates the window `0 ≤ startMs < endMs ≤ durationMs` — reading `durationMs` off the original media asset (`video` then `audio` kind) so a clip can never exceed the media; when the duration is UNKNOWN (an unprobeable local file, or a referenced YouTube source with no vault asset) the upper bound is skipped (the window must still be a non-empty, in-order, non-negative span) — requires a non-deleted media source, inherits the source's numeric priority unless overridden (A/B/C/D), and composes `ExtractionService` to create the fragment. **Typed seam**: one new channel — `sources:extractClip` — in `channels.ts` with zod request/result schemas (`contract.ts`/`contract.test.ts`; the request is `{ sourceElementId, startMs, endMs, anchorBlockId, transcriptSegment?, caption?, priority? }`, the result carries the new `media_fragment` summary + its clip source-location), threaded `ipc.ts` → `db-service.ts` (a lazily-built `mediaClipService` getter) → `preload/index.ts` → `apps/web/src/lib/appApi.ts` as `window.appApi.sources.extractClip`; there is NO generic mutation channel. **Renderer**: `MediaReader.tsx` (+ `media-reader.css`) gained clip start/end markers (set the in/out points from the playhead or a transcript-cue run), a `ClipMiniPlayer.tsx` that loops the selected window over the original media, and an "Extract clip" action wired to the pick → extract flow; `ExtractView.tsx` (+ `extract-view.css`) renders a clip `media_fragment` with its time-range label + a mini-player that seeks the original; the inspector surfaces the clip window. No fs/parse/SQL in React; no bytes cut, no network. Covered by Vitest (`extraction-service.test.ts` — the clip-extract path: `timestampMs`+`clip` anchor, transcript-segment body, generated caption body, label, lineage edge, inherited priority/tags, ATTENTION due-date + NO review_states, the invalid-window guard; `media-clip-service.test.ts` — the duration validation incl. the skipped upper bound for unknown duration, the non-deleted-media-source guard, priority inherit/override; `mappers.test.ts`+`source-location-label.test.ts` — the `clip` round-trip + `deriveClipLabel`; `MediaReader.test.tsx`, `ClipMiniPlayer.test.tsx`, `contract.test.ts`, `db-service.test.ts`, `backup-manifest`/`backup-service`) and a Playwright/Electron spec (`tests/electron/media-clip.spec.ts`: selecting a transcript range in a media source mints a scheduled `media_fragment` with its clip window + transcript segment, the clip plays back seeking the original, and the fragment + its clip source-location survive an app restart). `pnpm typecheck` + `pnpm lint` + `pnpm test` (1559 tests) all green.
- 2026-06-02 - T073 Video import - done. YouTube and local video/audio now import as first-class media sources with an optional transcript and timestamped, resumable read-points — all on-device, no server, no API key, no video download, no ASR (see the M15 "Scope honesty"). **Pure transforms in `@interleave/importers` (no fs/Electron/network)**: `transcript.ts` wraps `subsrt-ts` to normalize a raw VTT/SRT caption STRING into a clean, sorted `TranscriptCue[]` (handles overlapping cues, missing end times, `\r\n`, styling-tag stripping, SRT comma vs VTT dot millis, BOM — all fixture-tested); `transcript-to-prosemirror.ts` walks those cues into the constrained ProseMirror body, stamping each cue paragraph with a stable block id + `timestampMs`; `media-metadata.ts` probes a local file's `durationMs` straight from ISO-BMFF (`moov`→`mvhd`) and Matroska/WebM (`Segment`→`Info`→`Duration`) headers off the first ~256 KB, degrading to `null` for anything unrecognized (import never fails on an unprobeable file); `youtube.ts` reaches YouTube ONLY through an INJECTED `fetch` — keyless oEmbed for title/author/thumbnail (the one hard error → `youtube_unavailable`) and a best-effort watch-page caption discovery where a missing player response / empty body / bot-check ALL degrade to `transcript: null`, NEVER an exception (the load-bearing graceful transcript-less degrade). **Schema (two additive columns, one migration `0015`)**: `document_blocks.timestamp_ms` carries a cue's media time; `sources.media_kind` (`video`/`audio`/`youtube`) tags a media source — no new tables. **Main-process `MediaImportService` (mirrors PdfImportService)**: `importFromFile` validates ext/size, decides video vs audio, STREAMS the original bytes into the T059 vault (`sources/<id>/original.<ext>`, content-hashed — bytes NEVER touch SQLite), parses an optional sidecar `.vtt`/`.srt`, builds the transcript doc, and creates an `inbox` source via `createWithDocument` in one transaction with PDF-style soft-delete + vault-cleanup rollback on any post-commit failure; `importFromYouTube` fetches metadata + captions on-device and creates an inbox source referencing the canonical watch URL (no vault stream, `snapshotKey: null`). A privileged `media://<elementId>` protocol streams local bytes with HTTP Range support (never buffered over IPC). **Renderer `MediaReader`** (swapped in by `SourceReader` on `sourceFormat: "video"`): HTML5 `<video controls>`/`<audio controls>` for local, the YouTube IFrame embed for referenced; a transcript pane lets the user click a cue to SEEK and highlights the playing cue; "Set read-point" persists the current cue's stable block id via `readPoints.set` (transcript-backed) OR the title-heading block id with `offset = floor(currentTimeMs)` (transcript-less, offset-as-seconds) so the single `read_points` table resumes BOTH cases with no extra column, and reopening seeks back. The renderer ships only a picker-resolved path or a URL — it never reads the file, fetches the transcript, parses, or touches the vault. Typed `window.appApi` grows `sources.importMedia`/`importYouTube`/`getMediaData`; coverage spans importer unit + fixture tests, `media-import-service`, `media-source` repo, contract, and a `media-import` Electron e2e (import → read-point → restart-resume). All green: typecheck, lint, 1537 vitest tests.
- 2026-06-02 - T087 Semantic search (sqlite-vec) - done. `/search` now FUSES the existing FTS5 keyword index with on-device vector KNN so conceptually-related material surfaces WITHOUT a shared keyword ("spaced repetition" finds a card about "review intervals") — entirely on-device, **OFF BY DEFAULT**, and degrading cleanly to FTS-only on any vec-absent / disabled / model-missing path. **The vector store is `sqlite-vec` (`vec0`) on the SAME better-sqlite3 file — never pgvector, no server, no second datastore** (the re-scope decision): vectors back up with the DB and survive restart. **The ABI trap is the load-bearing correctness concern, handled honestly**: this repo pins `better-sqlite3 ^12` (SQLite ~3.50) while the prebuilt `sqlite-vec` binaries compile against ~3.45, and the documented failure mode is that `db.loadExtension` SUCCEEDS yet `vec0` registers NO functions — so the SOURCE OF TRUTH for "vec available?" is NOT `loadVectorExtension` returning but `vecFunctional(sqlite)` (`packages/db/src/vec.ts`), a FULL round-trip smoke test (load → `vec_version()` → create a `vec0` table → insert a `float[384]` → KNN `MATCH … LIMIT 1` → drop); every caller gates on it — the repo's `vecAvailable` flag, the real-`vec0` tests (`it.skipIf(!vecFunctional)` so an ABI-mismatched host skips cleanly instead of failing inside a query it assumed worked), the guarded migrator, AND a build-time guard (`apps/desktop/scripts/vendor-sqlite-vec.mjs` runs the same smoke test against the SHIPPED binary at package time and fails the build if `vec0` does not register, so a packaged app can never ship a loaded-but-non-functional vec). **Guarded migration (`packages/db/src/migrator.ts`)**: Drizzle's stock migrator applies every journaled `.sql` UNCONDITIONALLY with no per-step hook, but `CREATE VIRTUAL TABLE … USING vec0` THROWS on a vec-absent host — so the journaled `0022_semantic_vec0.sql` is an INTENTIONAL NO-OP (`SELECT 1;`, recorded "applied" everywhere to keep the journal consistent) and the REAL `element_vectors` DDL lives in `applyVecMigration(db)`, run by the widened `migrateDatabase(db, { vecAvailable })` ONLY when `vecFunctional` is true (idempotent `IF NOT EXISTS`); on a vec-absent host the table is simply never created, all other migrations apply, and the suite stays green FTS-only. **Schema**: a `vec0` `element_vectors(embedding float[384])` virtual table + a DERIVED-INDEX sidecar `embeddings` table (migration `0021` + `packages/db/src/schema/embeddings.ts`) mapping `element_id` (PK, FK → `elements` `onDelete:cascade`) → the `vec_rowid` (UNIQUE) + `model_id` + `dim` + a `content_hash` (sha256 of the exact embedded text — the skip-if-unchanged / re-embed gate). Like FTS5 it is a derived index rebuildable from the base tables, so an embeddings write appends **NO `operation_log` entry** (the closed `OPERATION_TYPES` set does not grow) and the dim is the shared `EMBEDDING_DIM = 384` core constant so the column DDL and the constant move together. **The default model is a REAL on-device sentence-transformer, with a deterministic fallback (decision, justified)**: the worker (`apps/desktop/src/worker/embedding-model.ts`, DB-FREE) runs the real `all-MiniLM-L6-v2` (384-dim) ONNX model via **`fastembed`** (chosen over `@huggingface/transformers`, which hard-depends on native `sharp` irrelevant to text) — it streams the ~23 MB quantized ONNX + tokenizer into `INTERLEAVE_MODEL_DIR` (a fork-env seam like `INTERLEAVE_ASSETS_DIR`) on first load and reuses it from disk across restarts (no bytes leave the device after that), producing TRUE semantic vectors. When the real model cannot load (the dev/Vitest path bundles no model, an offline first run, an `onnxruntime` ABI miss) the worker falls back to a dependency-free feature-hashing → L2-normalized `float[384]` embedder (`embedTextLocal` in `@interleave/core`, dep-free so the worker AND the tests call the identical impl) recorded under a DISTINCT `model_id` (`local:minilm-hash-384`) so the two spaces are NEVER KNN-mixed (a host flipping between them re-embeds via the `model_id` gate rather than comparing incompatible vectors) — the feature degrades to a lexical baseline instead of ever hard-failing, and the deterministic vectors let the unit/integration tests assert exact KNN neighbors with no model mock. **The opt-in API provider** (`embeddingProvider:"api"`) calls the user's OWN OpenAI-compatible `/embeddings` endpoint with the user's OWN key — the only network call is to the provider the user configured, NEVER our server. **Secret discipline (load-bearing)**: the API key lives only in SQLite settings; the enqueued/persisted `embed` job payload carries NO secret — it is read LIVE from settings and injected into the worker request OUT-OF-BAND at POST time by the runner's new `JobSecretsProvider` (`embedJobSecrets`), so a runtime key change is picked up without a restart and the key never lands in the restart-safe `jobs` row, never reaches the renderer. **Main-side orchestrator** `EmbeddingService` (`apps/desktop/src/main/embedding-service.ts`, the OcrService twin): `buildText` reads the DB and builds the pure per-type text (source: title + bounded `documents.plain_text`; extract: title + `source_locations.selected_text`; card: prompt/cloze + answer — mirroring what FTS indexes, bounded to ~512 tokens) so the worker stays DB-free (it only ever sees text); `enqueueElement` computes the content hash + skips if `needsEmbedding` is false; `reindexAll` enqueues `embed` jobs for every live source/extract/card needing (re-)embedding (the `semantic.reindex` build-the-index path); the SINGLE `embed` apply handler branches on `payload.persist` — the index path UPSERTs the vector (idempotent by element), the transient `persist:false` QUERY path stashes the vector in a bounded main-side map for `embedQuery` (or DROPS it if the waiter timed out, so the map never leaks); `embedQuery` embeds a query via a transient job and recovers the vector from that map (because `waitForTerminal` resolves with the persisted `Job` snapshot, not the apply return value), racing a short 800 ms timeout so `/search` NEVER hangs and falls back to FTS-only; `downloadModel` pre-warms the worker's first model load on first enable and flips `embeddingModelDownloaded` (the spec's mechanism (b) — let `fastembed` own the integrity-checked atomic cache rather than re-implement a main-side fetch; tradeoff documented). **`EmbeddingRepository`** (`packages/local-db`, main-owned single writer/reader; the worker NEVER opens the DB or loads vec): `upsert` writes the vector into `element_vectors` + the sidecar in ONE tx, REUSING the existing `vec_rowid` on re-embed (`vec0` has no UPSERT, so delete-then-insert the same rowid replaces in place — and `vec0` requires an INTEGER LITERAL rowid, not a bound param, so the controlled read-back rowid is inlined via `sql.raw`); `knn` runs the `WHERE embedding MATCH … ORDER BY distance LIMIT k` join back to the sidecar + live `elements` (over-fetching a margin so the type/exclude/liveness post-filter still returns up to `limit`); `needsEmbedding`/`has`/`getVector` (exposed for the T088 related-item path)/`delete` (the explicit PORTABLE cleanup, never a `vec0` trigger)/`stats`/`pruneOrphanVectors` (a backstop sweep). Everything no-ops/`[]` when `vecAvailable` is false. **The fusion layer** `SemanticSearchRepository` combines the FTS hits + the KNN neighbors with RECIPROCAL-RANK FUSION (RRF, k=60) keyed per element so a purely-semantic neighbor with zero keyword overlap still surfaces, deduped + sorted + capped, READ-ONLY (no op-log), excluding soft-deleted — and returns `mode: "semantic" | "fts" | "disabled"` so the UI labels whether semantics actually ran, degrading to FTS-only (never a throw) on any disabled / vec-absent / missing-query-vector condition. **Background-runner integration**: the reserved `embed` `JOB_TYPE` is now LIVE — the DB-free worker computes + posts the vector, main applies it (single writer intact, at-least-once safe since upsert is idempotent by element). **Settings** (`packages/core/src/settings.ts`): five new keys — `semanticSearchEnabled` (default **false**), `embeddingProvider` (`local`/`api`, default `local`), `embeddingApiKey` (default empty, NEVER sent to our server), `embeddingModelId` (default `local:all-MiniLM-L6-v2`), `embeddingModelDownloaded` (default false) — each coerced/bounded so a corrupt stored value never reaches the worker. **Typed seam**: new `semantic:search` / `semantic:status` / `semantic:reindex` / `semantic:downloadModel` channels (`channels.ts`) with zod request/result schemas (`contract.ts` + `contract.test.ts`), threaded `ipc.ts` → `db-service.ts` (a lazily-built `embeddingService` getter + the fused-search delegate that enriches purely-semantic hits' title/snippet from the element row) → `preload/index.ts` → `apps/web/src/lib/appApi.ts`; there is NO generic `db.query`/`jobs.enqueue` exposure. **Renderer**: the Library/search screen (`LibraryScreen.tsx` + `library.css`) labels semantic-only vs keyword hits and shows the index/mode state, and **Settings** (`Settings.tsx`) gained a "Semantic search" card (the off-by-default master toggle, the local-vs-own-API-key provider choice with a masked key field that visibly discloses the key only authorizes the user's own provider, the model-download/reindex affordances + coverage stats) — no fs/SQL/vec in React, the renderer reaches everything only through the validated `window.appApi` surface. Covered by Vitest (`packages/core/src/embedding.test.ts` — the deterministic fallback's determinism + shared-token nearness + empty→zero; `packages/db/src/vec.test.ts` — the functional smoke test + blob encoding (real-vec0 cases `skipIf`-gated); `packages/local-db/src/embedding-repository.test.ts` — upsert/re-embed rowid reuse, KNN narrowing + liveness, needsEmbedding gate, delete, stats, orphan prune; `packages/local-db/src/semantic-search-repository.test.ts` — RRF fusion, semantic-only surfacing, FTS-only degrade, the mode; `apps/desktop/src/main/embedding-service.test.ts` — per-type buildText, content-hash skip, the persist-vs-query apply branch + abandoned-query drop, the out-of-band `embedJobSecrets` (key never in the payload), the embedQuery timeout→FTS-only; `apps/desktop/src/main/embedding-job.test.ts` — the live `embed` runner job; `apps/desktop/src/worker/embedding-model.test.ts` + `messages.test.ts` — the local/fallback/API compute paths + worker message shapes; `contract.test.ts`, `db-service.test.ts`, schema round-trip) and a Playwright/Electron spec (`tests/electron/semantic-search.spec.ts`: enabling semantic search, embedding the corpus, and a query with NO shared keyword surfacing a conceptually-related element, with the index + setting surviving an app restart). `pnpm typecheck` + `pnpm lint` + `pnpm test` (1941 tests) all green.
- 2026-06-02 - T072 Formula & code cards - done. Math (KaTeX) and syntax-highlighted code (Shiki) now render IDENTICALLY across the three surfaces — source reader, extract/card builder, and review — so a card whose prompt/answer is a formula or a code snippet no longer shows raw LaTeX/source, plus code cards get code-aware quality heuristics. **One render core, three surfaces (load-bearing)**: the math + code render helpers live ONCE in `@interleave/editor/render/` — `renderMathHtml` (`katex.ts`, KaTeX → a fixed sanitized HTML subset, `throwOnError:false` so a malformed `$…$` degrades to visible source not a crash) and `highlightCodeHtml` (`shiki.ts`, async grammar/theme load via `@shikijs/core`+`langs`+`themes`, light/dark theme by the app's `data-theme`, an unsupported language stays plain `<pre><code>`) — and BOTH the editor NodeViews and the review body consume them, so the pixels match by construction. **Editor schema (math + constrained code-block nodes, defined ONCE)**: `packages/editor/src/nodes/` adds a LaTeX `math` node (`math.ts` — stores `{ latex, display }`, NEVER pre-rendered HTML) and a `CodeBlockLanguage` node (`code-block-language.ts` — StarterKit's `codeBlock` is disabled in favour of the same-named node WITH a `language` attr); the node SPECS are React-free so the headless schema (importers/tests) is byte-identical, and the React editor attaches KaTeX/Shiki via `.extend({ addNodeView })` (`MathNodeView.tsx`/`CodeBlockNodeView.tsx`, wired through `react-node-views.ts` → `SourceEditor.tsx`) — so the renderer-only render code never leaks into the pure schema package. Markdown import (`packages/importers/src/markdown.ts`) now carries fenced-code `language` info-strings through into the `codeBlock` node, and serialize round-trips them. **Review body (`apps/web/src/review/CardBody.tsx`)**: the review faces render `prompt`/`answer` as raw STRINGS, so a shared `parseBodySegments` (`render/body-segments.ts`) splits a body into ordered text / inline `$…$` / block `$$…$$` math / fenced ```lang code segments and renders each via the SAME KaTeX/Shiki helpers — math renders synchronously to safe markup, code renders RAW first then swaps in the highlighted HTML when Shiki resolves (so a slow first highlight never blocks paint), and a plain-text body renders verbatim (existing cards unaffected). Wired into `CardFront.tsx`/`ReviewScreen.tsx`; the only `dangerouslySetInnerHTML` is over KaTeX/Shiki-sanitized markup, never raw user HTML. **Code-specific quality heuristics (`packages/core/src/card-quality.ts`)**: a code body is judged in LINES not chars/words — a new `CODE_MAX_LINES` (12) cap + a `code-too-long` check fire `prompt-too-long`/`answer-too-long` with a code-aware message ("narrow it to one construct"), and for a detected fenced-code prompt/answer the char/word length AND the ambiguous-pronoun heuristics are SKIPPED (they over-warn or are meaningless on code), so a legitimate code card isn't drowned in false warnings while still being held to one-construct-per-card. Covered by Vitest (`render/katex.test.ts`, `render/shiki.test.ts`, `render/body-segments.test.ts` — segment splitting incl. inline/block math + fenced code + plain-text passthrough; `editor/schema.test.ts`+`serialize.test.ts` — the math/code nodes + language round-trip; `importers/markdown.test.ts` — fenced language carry-through; `core/card-quality.test.ts` — the line cap + skipped char/pronoun checks on code; `review/CardBody.test.tsx`, `reader/SourceEditorRichRender.test.tsx`, `reader/CardBuilder.test.tsx`) and a Playwright/Electron spec (`tests/electron/formula-code-cards.spec.ts`: a source/extract/card with math + code renders highlighted across reader → builder → review and survives an app restart). `pnpm typecheck` + `pnpm lint` + `pnpm test` all green.
- 2026-06-02 - T071 Image occlusion - done. One image extract (a `media_fragment` crop from T065) now generates N **sibling** `image_occlusion` cards — one per drawn mask — with the masks/regions stored SEPARATELY from the base image bytes, never baked into a flattened picture. **Masks separate from the base image (load-bearing)**: the base image stays a single immutable asset in the vault; a new `occlusion_masks` table (migration `0014`, schema `packages/db/src/schema/occlusion.ts`) holds one row per mask — a normalized `RegionRect` (0..1 coords, so it survives any render scale) + an optional reveal `label` + a one-to-one link to the card element it hides — so the same diagram can be re-masked/re-cropped without ever rewriting the image, and the review face composites mask-over-image at render time (`apps/web/src/review/CardOcclusionFace.tsx`, the crop sourced via `useRegionImage.ts`). **N sibling cards in ONE tx (`OcclusionService`, `packages/local-db/src/occlusion-service.ts`)**: mirrors `CardService.createFromExtract` exactly — image occlusion is a card VARIANT, not a parallel system — deriving lineage from the image (`parentId` = the image extract, `sourceId` = its source root, `sourceLocationId` = the image's page+region anchor so jump-to-source in review lands on the figure), minting ONE `siblingGroupId` for the whole diagram, then per mask creating a real `card` element + `cards` row (kind `image_occlusion`, stage `card_draft`) + an UN-DUE `review_states` row + a `sibling_group` edge + inherited tags/priority — a throw rolls back EVERY card/mask/edge/tag row. **Two-scheduler split honored**: the cards are left `card_draft` with `fsrsState:"new"`; M7's FSRS owns the first schedule + the `card_draft → active_card` transition (we never schedule here), and the originating `media_fragment` stays an ATTENTION item with no FSRS row of its own. The `sibling_group` edges mean T039 burying keeps a diagram's cards from appearing back-to-back. **Edit-then-regenerate is first-class**: re-running on the same image RETIRES the prior batch — because `replaceMasksForImage` DELETES the old mask rows (which would orphan their cards to a blank face), the service soft-deletes the prior cards in the SAME tx first, so a regenerate REPLACES the diagram's cards rather than accumulating them. Authoring UI is `apps/web/src/reader/OcclusionEditor.tsx` (draw/label masks over the crop); the renderer reaches all of this only through the validated `cards.generateOcclusion` IPC command — no `fs`/SQL in the renderer. Covered by `occlusion-service.test.ts`, the face/editor component tests, and the `tests/electron/image-occlusion.spec.ts` E2E (generate → review → restart-persistence).
- 2026-06-02 - T070 Anki import/export - done. Interleave cards now export to an Anki-compatible `.apkg` (and a CSV) CARRYING SOURCE REFERENCES, and an Anki `.apkg` imports back as `card` elements PRESERVING review history when available — all on-device, the renderer never touching `fs`/zip/SQL. **The pure/native split (load-bearing)**: the `.apkg` zip wrap/unwrap + the Anki row⇄note mapping live in the PURE `@interleave/importers/anki.ts` (DB/Electron/native-free, fixture-testable, bundles into `main.cjs`), but reading/writing the EMBEDDED `collection.anki2` — itself a SQLite DB — needs `better-sqlite3` (a native module that would poison the pure package), so that native open/read/write is isolated in the MAIN-only `apps/desktop/src/main/anki-collection.ts`; the two import/export services COMPOSE them. **Format-version matrix (documented + typed)**: `parseApkgZip` (via `fflate`, the zip dep T067 added) unwraps an `.apkg` → `{collectionBytes, variant, media map, mediaFiles}`, accepting the legacy uncompressed `collection.anki2` (import + export) and the newer uncompressed `collection.anki21` (import), and REJECTING the zstd-compressed `collection.anki21b` with a typed `unsupported_compression` error (no pure-JS zstd is bundled — the friendly line tells the user to re-export with “Support older Anki versions”), plus `not_a_zip`/`no_collection`/`empty_collection`. We EXPORT the maximally-importable uncompressed `collection.anki2`; we target Anki's IMPORTER, not byte-identical internals (the CI contract is the ROUND-TRIP — our import of our export). **Import (`AnkiImportService`, main, injected `{ db, repositories, assetsDir, assetVault, nativeBinding }`)**: validates extension + a 500 MB cap, unwraps the zip (pure), writes the embedded collection bytes to a temp file + opens it with the Electron-ABI `better-sqlite3` (`query_only=ON`) to read `notes`/`cards`/`revlog`/`col.models` (tolerating a missing `revlog`), then `ankiRowsToNotes` maps them to normalized `AnkiNoteRecord[]` — splitting `flds` on `\x1f` by the model's field list, classifying Basic (`type:0`/name) → `qa` vs Cloze (`type:1`/name) → `cloze`, deriving prompt/answer or the `{{cN::…}}` cloze text, stripping field HTML to plain text via the constrained `sanitizeArticleHtml` (+ `<br>`/block→space + entity decode), collecting `[sound:…]`/`<img src>` media refs, and attaching the most-reviewed card's SM-2 scheduling + its union revlog. Every note becomes a `card` element under a per-deck lineage-root `source` (“Imported Anki deck: <name>”, priority `C` so a fresh deck never dominates older high-value work) authored in ONE transaction (the deck `source` + an `import_archive` asset row + every card) — atomic, with the `.apkg` written to the vault at `sources/<deck_id>/original.apkg` BEFORE the tx (content-hashed) and a best-effort partial-dir `rmSync` on rollback (mirrors EPUB import); cards are NEVER orphaned. The deck's media files stream into the vault as individually addressable image/audio/video assets AFTER the cards commit (best-effort — bytes also live in the retained archive). **Review history: FSRS-vs-SM-2, handled HONESTLY (not faked)** — `schedulingToSeed` carries the COMPARABLE counters directly (`reps`/`lapses`), seeds FSRS `stability ≈ the Anki interval` (“days to ~90% retrievability”), seeds `difficulty` from a clamped inversion of the Anki ease factor (`[1300,3500]` permille → `[10,1]`, neutral 5 when absent), graduates `review` vs `learning`, and places the card ~`interval` days out; we DELIBERATELY do NOT fabricate per-review `review_logs` for historical Anki reviews (that would corrupt analytics + future FSRS optimization — only real in-Interleave reviews ever log), so the FSRS params re-converge over the next few reviews. A note with no scheduling imports as a brand-new card. This flows through the WIDENED `ReviewRepository.createCardWithin` `reviewSeed` input; the seam owns activation (a seeded or first-scheduled card defaults `card_draft` → promoted to `active_card`/`active`, so imported cards land “active” not parked “pending”), and the Anki `Source` field / deck name lands in the NEW `cards.source_uri` column so attribution survives without an in-app `source_locations` anchor. **Export (`AnkiExportService`, main, READ-ONLY on the DB — no mutation, no op-log, it produces a FILE)**: resolves a selection (explicit card ids / a concept's cards / all live cards) to `ExportNote[]`, deriving each card's SOURCE REF from its own `source_uri` (an Anki-imported card kept its ref — round-trips OUT) else the owning source's title + URL + source-location label; `notesToAnkiRows` builds the per-note `notes`/`cards` rows for the minimal Basic/Cloze note types, putting the source ref into a dedicated `Source` FIELD AND an `interleave::source::<slug>` TAG (lineage is not lost on the way to Anki), with `\x1f` separators, stable GUIDs (base64url of the element id), and the `csum` first-field SHA-1 checksum Anki uses for dedup (the pure module takes the hex digest so it stays dep-free; the service has crypto). `writeAnkiCollection` writes a MINIMAL but Anki-importable `collection.anki2` (the `col`/`notes`/`cards`/`revlog`/`graves` schema + indexes + the single `col` row from `buildAnkiModels`/`buildAnkiDecks`/`buildAnkiDconf`) to a temp DB, `buildApkgZip`s it into `exports/anki-export-<ts>.apkg`; a sibling `exportCsv` writes an RFC-4180 `Front,Back,Cloze,Tags,Source` CSV Anki imports directly. We do NOT export historical reviews back out (no revlog). **Schema**: a new `import_archive` `ASSET_KIND` (`packages/core/src/enums.ts`; bytes in the vault, never SQLite) + the new nullable `cards.source_uri` column, both via Drizzle migration `0013_charming_senator_kelly.sql` (the assets-kind CHECK rebuild + the column add). **Typed seam**: two new channels — `cards:importAnki` + `cards:exportAnki` — in `channels.ts` with zod request/result schemas (`contract.ts`/`contract.test.ts`; the import error `code` is the typed union the IPC layer maps to a friendly line, the result carries deck/card/with-history counts + the inbox summary; the export result is relative + abs path + count), threaded `ipc.ts` → `db-service.ts` (lazily-built `ankiImportService`/`ankiExportService` getters that require `assetsDir`/`exportsDir`/the native binding, mirroring the other import services) → `preload/index.ts` → `apps/web/src/lib/appApi.ts`; it REUSES the existing MAIN-owned file picker (the renderer never gets a raw path) and there is NO generic `db.query` exposure. **Renderer**: the inbox import strip (`ImportFileModal.tsx`/`InboxScreen.tsx`) gained an Anki chip wired to pick → import, and the inspector (`Inspector.tsx`) gained an Export-to-Anki affordance; no fs/zip/SQL in React, bytes go to the vault never SQLite, no network. Covered by Vitest (`packages/importers/src/anki.test.ts` — apkg zip wrap/unwrap incl. the `anki21b`/`not_a_zip`/`no_collection` typed errors + the variant precedence, Basic/Cloze classification + field-split + HTML-strip + media-ref extraction, the scheduling/revlog lift, the export row build incl. the `Source` field + `interleave::source` tag + GUID + `csum`; `apps/desktop/src/main/anki-import-service.test.ts` — the per-deck source + atomic tx + rollback partial-dir cleanup, `schedulingToSeed`'s honest SM-2→FSRS map (stability≈interval, ease→difficulty, graduated vs learning, reps/lapses carried, NO fabricated review_logs), tag inheritance skipping the provenance tags, the deck-media vault import, the too-large/not-apkg/unreadable typed errors persisting nothing; `apps/desktop/src/main/anki-export-service.test.ts` — selection resolution (ids/concept/all), the source-ref precedence + the carried `Source` field/tag, the `.apkg` + CSV file artifacts, the empty-selection error; `packages/local-db/src/review-repository.test.ts` — the widened `reviewSeed` create path; `contract.test.ts`, `db-service.test.ts`) and a Playwright/Electron spec (`tests/electron/anki.spec.ts`: exporting cards writes an `.apkg`/CSV with source refs into `exports/`, RE-importing that `.apkg` round-trips them back as `card` elements under a deck source preserving the carried scheduling, and the imported deck + cards + the vault `original.apkg` survive an app restart). `pnpm typecheck` + `pnpm lint` + `pnpm test` all green.
- 2026-06-02 - T069 Highlight import (Readwise/Kindle-style) - done. An external highlight export (a **Readwise CSV**, a **Readwise JSON**, or a **Kindle `My Clippings.txt`**) now imports into the inbox as `extract` elements — explicitly **NOT cards, NOT in-body highlights** — grouped under one `source` per book/article, with full source attribution, entirely on-device (the renderer never touching `fs`/parse/SQL). **Extracts, not cards (the roadmap's load-bearing constraint)**: a highlight is raw, un-distilled material, so the import FLOOR is `extract` (stage `raw_extract`) — the service creates ONLY `extract` elements (+ their `source_locations` anchor + `derived_from` lineage edge + inherited tags + an initial ATTENTION due date) and NEVER touches `cards` / `review_states`; the user later runs the normal extract → card distillation through the existing T035 quality gate, so a flood of clippings can't bypass the minimum-information checks or dominate review. **Pure adapters (`@interleave/importers/highlights.ts`, DB/Electron/network/fs-free, fixture-testable)**: every format converges on ONE normalized `ImportedHighlight` intermediate (`{ text, note, title, author, sourceUrl, location, page, highlightedAt, tags }`) so the import service has ONE grouping + authoring path regardless of tool. `parseReadwiseCsv` parses with **`papaparse`** (a tiny dependency-free RFC-4180 parser — chosen over a hand-rolled `split(",")` which is WRONG on the quoted commas/newlines Readwise embeds inside highlight cells, and over heavier streaming CSV libs we don't need since the export is already an in-memory string), resolving columns case-insensitively by name (`Highlight`/`Title`/`Author`/`Note`/`Location`/`Highlighted at`/`URL`/`Tags`) so header-order variance is tolerated. `parseReadwiseJson` walks the documented book-array shape (a bare `[...]` OR `{ books: [...] }`), normalizing the `[{name}]`-or-`["tag"]` tag variants and a `location`+`location_type` pair into a `"Page 56"`/`"Location 1234"` label. `parseKindleClippings` splits `My Clippings.txt` on the `==========` separator and parses each record's `Title (Author)` line + the `- Your Highlight on page 56 | location 1234-1240 | Added on …` metadata line (page preferred over location, `Added on` → ISO date) — tolerantly SKIPPING bookmarks (no body), standalone note records, and malformed/empty entries (Kindle clippings are notoriously messy: BOM-prefixed, locale-specific, duplicate-laden) rather than failing fatally. A shared `pageFromLocation` derives a real page number ONLY when the label denotes a page — a Kindle "Location 1234" is an addressing unit, never coerced to a page. `detectHighlightFormat(filename, content)` sniffs by extension + content shape (the unmistakable `==========` ⇒ Kindle; a leading `[`/`{` parsing to the book shape ⇒ Readwise JSON; a Readwise-style header row ⇒ Readwise CSV), returning `null` ⇒ a friendly "unrecognized export" error; `parseHighlights` dispatches. Three real fixtures (`__fixtures__/highlights/readwise.csv` with quoted commas/newlines, `readwise.json`, `MyClippings.txt`) anchor `highlights.test.ts`. **Orchestrator (Electron main)** `HighlightImportService` (`apps/desktop/src/main/highlight-import-service.ts`, construction-injected `{ db, repositories }` — NO vault, highlights are text): `importFromFile` reads the picked file MAIN-side (the renderer passed only a path) under a cheap 64 MB cap + UTF-8 decode, auto-detects (or uses the supplied) format, runs the matching pure adapter, then GROUPS by `(title, author)` — one source per book/article — and authors each group in **ONE transaction PER source** (so a failure on one book doesn't discard books already imported in the batch). Per group it find-or-creates the book/article `source` (DEDUP: canonical-URL match via `SourceRepository.findByCanonicalUrl` for the Readwise URL case, else title+author via the new `findByTitleAndAuthor` — so re-running an export never creates duplicate books), then authors one inbox `extract` per non-duplicate highlight: the extract element + a `source_locations` **attribution anchor** (these highlights did NOT come from a document we hold, so the anchor has EMPTY `blockIds` — no jump-to-paragraph — and instead carries the highlight's `page`/`label` + the `selectedText` snapshot, while the owning source carries `title`/`author`/`url`), a body seed (the highlight text, + the user's note as a second paragraph when present, via `plainTextToProseMirrorDoc`), a `derived_from` edge extract → source (lineage is sacred), the highlight's inherited Readwise tags, and an initial ATTENTION due date (`addDays(now, rawExtractIntervalDays(priority))`, status `scheduled` — NEVER FSRS, no `review_states` row). EXTRACT dedup is exact-text: a highlight whose exact text already exists as an extract `selectedText` under the (reused or new) source is skipped (`SourceRepository.listExtractSelectedText`, also guarding against in-batch duplicates) — exact-match only, semantic dedup is deferred to T088. New material defaults to priority **C** so imported highlights never dominate older high-value work (an A/B/C/D override is accepted). All failures surface as a typed `HighlightImportError` with a closed `code` set (`unrecognized`/`too_large`/`unreadable`/`empty`) the IPC layer maps to a friendly line; nothing is persisted on a failed parse. **`SourceRepository` (`packages/local-db`)** gained `findByTitleAndAuthor` (the no-URL dedup lookup) + `listExtractSelectedText` (the per-source exact-text extract set). **Typed seam**: one new channel — `sources:importHighlights` — in `channels.ts` with its zod request/result schemas (`contract.ts` + `contract.test.ts`; the result carries the detected `format` + counts: sources created/updated, extracts added, duplicate highlights skipped, + the inbox summaries; the error `code` is the typed union), threaded `ipc.ts` → `db-service.ts` (a lazily-built `highlightImportService` getter, mirroring the other import services) → `preload/index.ts` → `apps/web/src/lib/appApi.ts` as `window.appApi.sources.importHighlights`; it REUSES the existing T067/T068 MAIN-owned `sources:pickImportFile` picker (the renderer never gets a raw path, only the picked abs path passed straight back into the import command). **Renderer**: the inbox import strip (`ImportFileModal.tsx`/`InboxScreen.tsx`) gained a Highlights chip wired to the pick → import flow; no fs/parse/SQL in React. Covered by Vitest (`packages/importers/src/highlights.test.ts` — Readwise CSV quoted-comma/newline parsing + column-name resolution, Readwise JSON book/highlight/tag-variant walking + location-label synthesis, Kindle separator splitting + title/author/page/location/date parsing + bookmark/note/malformed skipping, page-vs-location coercion, format auto-detection; `apps/desktop/src/main/highlight-import-service.test.ts` — grouping into one source per book, one-tx-per-source, source dedup by canonical-URL + title+author, exact-text extract dedup incl. in-batch, the attribution anchor with empty blockIds + page/label + selectedText, the `derived_from` edge, tag inheritance, the ATTENTION schedule + NO review_states row, priority default C + override, the too-large/unreadable/unrecognized/empty typed errors persisting nothing; `packages/local-db/src/source-repository.test.ts` — `findByTitleAndAuthor` + `listExtractSelectedText`; `contract.test.ts`) and a Playwright/Electron spec (`tests/electron/highlight-import.spec.ts`: importing a real export lands inbox `extract`s grouped under one source per book with their attribution anchors + tags and NO cards, a re-import dedups, and the sources + extracts survive an app restart). `pnpm typecheck` + `pnpm lint` + `pnpm test` all green.
- 2026-06-02 - T068 Markdown & HTML import/export - done. Markdown and HTML are now first-class, local-first imports that land an `inbox` `source` through the existing transactional pipeline, plus a Markdown EXPORT that round-trips back with documented fidelity — all on-device, the renderer never touching `fs`/parse/SQL. **Pure transform pair (`@interleave/importers`, DB/Electron/network/fs-free, fixture-testable)**: `markdownToProseMirrorDoc(markdown, mint)` (`markdown.ts`) parses CommonMark with `markdown-it` (chosen over `marked`/`remark` — its flat **token stream** maps 1:1 onto the constrained block set with NO intermediate HTML re-parse; `html: false` so inline raw HTML is escaped, never injected — import stays safe + the schema constrained) and walks the tokens with a manual-cursor `TokenWalker` into the SAME `{ doc, plainText, blocks }` `PlainTextConversion` shape `htmlToProseMirrorDoc` produces (so the source pipeline stores it verbatim): heading (clamped to the 1–3 level set) / paragraph / blockquote / bullet+ordered list / list_item / fenced+indented code (verbatim, info-string language dropped) / hr blocks + strong/em/code/link inline marks, with stable block ids minted on the OUTERMOST row of each block (ids on listItem/blockquote wrappers, NOT their inner paragraphs) exactly mirroring the HTML path, images flattened to their alt text, and adjacent same-mark text runs MERGED (`mergeRuns`) so markdown-it's softbreak fragmentation collapses to stable runs that survive a round-trip. `proseMirrorDocToMarkdown(doc)` is a HAND-WRITTEN serializer (no `prosemirror-markdown` dep — the schema is tiny + we need EXACT control of the normalization so the round-trip is a deterministic fixed point): it walks the `ProseMirrorBlockNode` union to deterministic Markdown, ESCAPING always-significant inline chars (`escapeText`) AND line-leading block markers (`escapeLeadingMarker` — so a paragraph starting `# …` / `- …` / `1. …` round-trips as literal text, while a mid-sentence `.`/`-` is left untouched), fixed innermost→outermost mark order (code → italic → bold → link) for determinism, and `\n{3,}`→`\n\n` collapse. `htmlFileToProseMirrorDoc` + `extractHtmlTitle` (`html-file.ts`) REUSE the T060 URL-import transforms verbatim — an imported `.html` is UNTRUSTED so it goes through `sanitizeArticleHtml` (the load-bearing security boundary — no scripts/styles/iframes/`javascript:` survive) → `htmlToProseMirrorDoc`, with a best-effort `<title>` extraction (before sanitize drops the `<head>`) as a title fallback; there is NO second HTML parser. Documented **fidelity ceiling**: the constrained schema is the ceiling — images (→ alt only), code-block language, tables, HTML/footnote/task-list/strikethrough passthrough are normalized away on import and do NOT round-trip; the round-trip contract is "structurally equal modulo freshly minted block ids", asserted as a fixed-point in `markdown.test.ts` against the `__fixtures__/markdown/sample.md` + `__fixtures__/html/sample.html` fixtures. **Orchestrator (Electron main)** `DocumentImportService` (`apps/desktop/src/main/document-import-service.ts`, construction-injected `{ db, repositories, assetsDir, exportsDir }`, mirroring `UrlImportService`): `importFromFile` reads the picked file MAIN-side (the renderer passed only a path), enforces a cheap 32 MB cap (a note is tiny; books go via EPUB) + UTF-8 decode, dispatches on format, and creates the `inbox` `source` in ONE logged `db.transaction` via `SourceRepository.createWithDocumentWithin` (title resolved best-effort from the doc's first heading / HTML `<title>` / filename stem; new material defaults to priority `C` so it never dominates older high-value work); `importFromText` is the Markdown PASTE path (no file read; whitespace-only → typed `empty` error). The Markdown body IS its own canonical form so NO `original.*` snapshot is stored, but an imported `.html` ALSO streams the ORIGINAL untrusted bytes into the vault as a `source_html` asset at `assets/sources/<id>/original.html` (content-hashed, mirroring URL import) inside the SAME transaction, with the bytes written BEFORE the (synchronous) tx and a best-effort partial-dir `rmSync` on rollback (no orphan element + asset row + file). `exportToMarkdown` is a READ-ONLY FILE ARTIFACT — it loads the stored ProseMirror doc, runs `proseMirrorDocToMarkdown`, and writes `exports/<element_id>-<slug>.md` into the MAIN-owned `exports/` vault dir, appending NO `operation_log` entry (it changes no domain data; the renderer never picks the path). All failures surface as a typed `DocumentImportError` with a closed `code` set (`not_supported`/`too_large`/`unreadable`/`empty`) the IPC layer maps to a friendly line. **Typed seam**: four new channels — `sources:pickImportFile` (MAIN-owned file picker; the renderer never gets a raw path, only the picked abs path passed straight back into an import command), `sources:importDocument` (`.md`/`.html` file → inbox source), `sources:importMarkdownText` (paste → inbox source), and `documents:exportMarkdown` — in `channels.ts` with zod request/result schemas (`contract.ts` + `contract.test.ts`; the import error `code` is the typed union), threaded `ipc.ts` → `db-service.ts` (a lazily-built `documentImportService` getter that requires `assetsDir`/`exportsDir`, throwing clearly if absent — mirrors `urlImportService`) → `preload/index.ts` → `apps/web/src/lib/appApi.ts`. **Renderer**: the inbox import strip's Markdown + HTML chips (`ImportFileModal.tsx`/`NewSourceModal.tsx`/`InboxScreen.tsx`) are now live (pick → import), and the inspector (`Inspector.tsx`) gained an Export-to-Markdown affordance; no fs/parse/SQL in React, the `.html` original goes to the vault never SQLite, no network. Covered by Vitest (`packages/importers/src/markdown.test.ts` — Markdown→PM block/mark/list/code/blockquote/hr mapping, image→alt, run merging, the export round-trip fixed point + leading-marker escaping, empty/whitespace → empty doc, `html-file.ts` sanitize+title; `apps/desktop/src/main/document-import-service.test.ts` — file + paste import, HTML stores the vault `original.html` + content hash, MD stores no snapshot, the too-large/unreadable/empty typed errors, rollback + partial-dir cleanup on a failed insert, priority default + override, the read-only Markdown export round-tripping a stored doc; `contract.test.ts`, `db-service.test.ts`) and a Playwright/Electron spec (`tests/electron/markdown-import.spec.ts`: importing a `.md` and a `.html` lands inbox sources with the right body + (for HTML) a vault `original.html`, exporting a stored doc writes a round-tripping `.md` into `exports/`, and the imports + export survive an app restart). `pnpm typecheck` + `pnpm lint` + `pnpm test` all green.
- 2026-06-02 - T067 EPUB import - done. A local `.epub` now imports as a small LINEAGE TREE of sources — one **book `source`** plus one independently-schedulable, incrementally-readable **chapter `topic`** per spine item — entirely on-device, with the original bytes in the vault and never in SQLite. **Pure transform (`@interleave/importers/epub.ts`, DB/Electron/network/fs-free, fixture-testable)**: `parseEpub(bytes)` unzips the EPUB container with `fflate` (`unzipSync`), parses `META-INF/container.xml` → the OPF package, and reads `<metadata>` (title/author/`dc:date`/language/identifier), the `<manifest>` (id → OPF-relative resolved zip path + media type + properties), and the `<spine>` reading order with `fast-xml-parser` (one shared `XMLParser` config: keep attrs, never coerce ids/dates to numbers, force `itemref`/`item`/`navPoint`/`dc:*` to arrays so single + repeated elements walk identically); chapter titles resolve best-effort from the EPUB3 nav doc (`manifest properties="nav"`, `nav[epub:type=toc]` anchors) OR the EPUB2 `toc.ncx` `navMap` (recursively), keyed by resolved href, falling back to a synthesized "Chapter N". It throws a typed `EpubParseError` with a closed `code` set — `not_a_zip` / `no_opf` / `no_spine` / `empty_book` / `drm` — and a **DRM-protected** book (`META-INF/encryption.xml` present) imports NOTHING (we never circumvent DRM). `chapterToProseMirror(chapter, mint)` maps each chapter's XHTML to the SAME constrained `{ doc, plainText, blocks }` conversion the URL/PDF paths produce by REUSING `sanitizeArticleHtml` + `htmlToProseMirrorDoc` (EPUB XHTML is the same constrained tag set — no second sanitizer/DOM), threading ONE shared `BlockIdMinter` so stable block ids stay unique across the whole book. **Footnotes (roadmap-named — preserved, not dropped)**: the constrained schema has no superscript/inter-doc links, so `liftFootnotes` walks the chapter's linkedom DOM, numbers each note reference (`epub:type=noteref`, or an `<a href="#id">` to a recognized `epub:type=footnote/endnote` body / a bare-marker-text anchor), replaces the reference in the flow with a bracketed `[n]` marker (punctuation-aware spacing so it reads `…practice[1].`), removes the note BODIES from the flow, and appends them as a synthesized `<hr><h2>Notes</h2>` endnotes section (one `[n] …` paragraph per note, HTML-escaped) — keeping the note CONTENT + its anchor position. A note whose body lives in another spine item (shared endnotes file) is resolved best-effort (the `[n]` stays; the note surfaces in whichever chapter OWNS its body). Three real fixtures (`epub3-three-chapters.epub` with a nav + EPUB3 footnotes, `epub2-toc-ncx.epub` with an NCX, `malformed.epub`) are built by `__fixtures__/epub/build-epub-fixtures.mjs` and asserted in `epub.test.ts`. **Orchestrator (Electron main)** `EpubImportService` (`apps/desktop/src/main/epub-import-service.ts`, construction-injected `{ db, repositories, assetsDir }`, mirroring `UrlImportService`/`PdfImportService`): validates extension + a 200 MB size cap (cheap `statSync` before the read) + a `parseEpub`-backed ZIP/OPF/spine/DRM check, mints the book id up front, writes the ORIGINAL `original.epub` into the vault at `assets/sources/<book_id>/original.epub` (content-hashed via `sha256`) BEFORE the transaction so the bytes exist for the asset row, then in ONE `db.transaction` (via the `*Within(tx, …)` seams) creates the book `source` (its `snapshotKey` = that epub; body = a structured TOC: the title as a real `heading` + a `bulletList` of chapter titles, rendered through the SAME `htmlToProseMirrorDoc` so it validates against `buildSchema()` — not empty, not one mashed paragraph; titles are HTML-escaped since they come from untrusted EPUB metadata), the `source_epub` asset row (metadata only — bytes already on disk), and for each spine item a chapter `topic` (body + `book → chapter` `parent_child` lineage edge logging `add_relation` + a `source_locations` anchor whose `page` = 1-based spine ordinal and `label` = chapter title). The WHOLE book is atomic — a parse/insert failure mid-book rolls back the entire book (no orphan book-with-half-its-chapters, no orphan asset row) and best-effort removes the partial `assets/sources/<book_id>/` dir; chapters convert BEFORE the tx so a malformed chapter aborts without writing any row. New material defaults to priority `C` so it never dominates older high-value work. The book is returned as the single inbox summary (chapters appear UNDER it in the hierarchy, not as N inbox rows). **`source_epub` + `epub_import` declared**: a new `source_epub` `ASSET_KIND` (`packages/core/src/enums.ts`; bytes in the vault, `snapshotKey` points at it, mirroring `source_pdf`) and a RESERVED `epub_import` `JOB_TYPE` (v1 parses inline in main — a book parse is sub-second; declared now so a later heavy-book move onto the T058 runner is a non-breaking worker-dispatch addition) — both threaded into the Drizzle CHECK constraints via migrations `0011_young_unicorn.sql` (assets-kind) + `0012_abnormal_strong_guy.sql` (jobs-type). `SourceRepository` (`packages/local-db`) gained `createTopicWithDocumentWithin` + a pre-minted-id path on `createWithDocumentWithin` so the chapter topics + book are created inside the book transaction. **Typed seam**: two new channels — `sources:pickImportFile` (MAIN-owned `.epub`/PDF file picker; the renderer never gets a raw path, only the picked abs path passed straight back into an import command) and `sources:importEpub` — in `channels.ts` with zod request/result schemas (`contract.ts` + `contract.test.ts`; the import error `code` is the typed union the IPC layer maps to a friendly line), threaded `ipc.ts` → `db-service.ts` (a lazily-built `epubImportService` getter that requires `assetsDir`, throwing clearly if absent — mirrors `pdfImportService`) → `preload/index.ts` → `apps/web/src/lib/appApi.ts` as `window.appApi.sources.pickImportFile`/`importEpub`. **Renderer**: a new `ImportFileModal.tsx` + the inbox import strip wire the EPUB chip to the pick → import flow; no fs/parse/SQL in React, bytes go to the vault never SQLite, no network. Covered by Vitest (`packages/importers/src/epub.test.ts` — spine order, EPUB3 nav + EPUB2 NCX titles, footnote lifting + endnotes section, DRM/no-spine/not-a-zip errors; `apps/desktop/src/main/epub-import-service.test.ts` — book+chapter tree, page-ordinal anchors, vault epub + content hash, atomic rollback + partial-dir cleanup on a mid-book failure, priority default; `packages/local-db/src/source-repository.test.ts` — the topic-with-document + pre-minted-id create paths; `contract.test.ts`, `db-service.test.ts`) and a Playwright/Electron spec (`tests/electron/epub-import.spec.ts`: importing a real EPUB lands a book source with one chapter topic per spine item, a chapter reads incrementally with its read-point + its spine-ordinal source-location, and the book + chapters + the vault `original.epub` survive an app restart). `pnpm typecheck` + `pnpm lint` + `pnpm test` all green.
- 2026-06-02 - T066 OCR fallback - done. Scanned / text-free PDF pages can now be turned into searchable, extractable text **fully on-device** by a `tesseract.js` (WASM) job on the **T058 background runner** — and the recognized text lands as a confidence-tagged, REVIEWABLE suggestion layer, never blindly merged into the body. **Where the WASM runs**: the heavy `tesseract.js` call lives ONLY in the DB-free `utilityProcess` worker (`apps/desktop/src/worker/ocr.ts`); it reads a page PNG MAIN already rendered + wrote to the vault, recognizes its text with the LSTM engine (`OEM 1`), walks the v6 `blocks → paragraphs → lines → words` tree for per-word confidence + bbox, and posts `{ page, text, meanConfidence, words }` back — it never touches SQLite. **Offline, bundled WASM + langdata (load-bearing)**: `tesseract.js` defaults to FETCHING its WASM core + `eng.traineddata` from a CDN (forbidden — offline-first) and the packaged app ships NO `node_modules`, so `build.mjs` `stageTesseract()` copies a self-contained tree into `dist/resources/tesseract/` (the engine's pnpm peer `node_modules` + `lang/eng.traineddata.gz` from the pinned `@tesseract.js-data/eng`, copied locally, NEVER fetched at build time), packaged via the `dist/**` glob and `asarUnpack`'d (a `.wasm`/data file can't be read from inside the asar — `electron-builder.yml`); the worker is kept EXTERNAL in its esbuild target and `require`d by a `createRequire(__filename)` DYNAMIC require from those LOCAL staged paths (`workerPath`/`corePath`/`langPath`, `cacheMethod: "none"`, `gzip: true`) — never `node_modules`, never the CDN. The offline path builder `offlineTesseractPaths()` is pure + exported so a unit test asserts the no-CDN/all-local invariant without spinning up WASM. **Pure post-processor** (`packages/importers/src/ocr.ts`, DB/Electron/WASM-free): `aggregateOcrWords(words, engineText?)` cleans + clamps per-word confidences (0–100), averages them into a page mean, and reconstructs reading-order text — preferring tesseract's own `data.text`, else grouping word boxes into visual lines by Y-tolerance — so the line-grouping / confidence-averaging is fixture-testable without loading WASM (`ocr.test.ts`). **Worker → MAIN seam**: the reserved `ocr` `JOB_TYPE` is now LIVE — `job-worker.ts` resolves the job's VAULT-RELATIVE page path against the vault root it reads ONCE from `INTERLEAVE_ASSETS_DIR` (the fork-env seam: `JobRunner.defaultFork` now forks the worker with `{ env: { …, INTERLEAVE_ASSETS_DIR: assetsDir } }` — chosen over the job payload DELIBERATELY so the absolute vault root NEVER lands in a persisted, restart-safe `jobs` row), maps the engine's 0–1 progress into the back of the bar, and posts the result; the MAIN apply handler (`job-apply-handlers.ts`, now taking `getOcrService`) persists it. **Main-side orchestrator** `OcrService` (`apps/desktop/src/main/ocr-service.ts`, construction-injected `{ db, repositories, assetVault, getRunner }`): `enqueuePage` writes the renderer's page PNG to the vault FIRST (`sources/<id>/ocr/page-N.png`, keyed by source so a re-OCR overwrites — idempotent) then enqueues an `ocr` job carrying ONLY that relative path (never bytes); `applyResult` UPSERTS the text into the `ocr_pages` layer (status `suggested`, idempotent by `(source, page)`) + best-effort writes the durable `ocr/page-N.json` to the vault (the architecture-named `ocr.json` analog; the SQLite row is the queryable truth); `acceptPage` is the user's explicit Accept — it MERGES the recognized lines as fresh-block-id paragraphs directly after the page's "Page N" heading through the normal `documents.upsert` → `update_document` path (so accepted OCR becomes ordinary searchable/extractable body text + updates `plainText` → FTS) and flips the row `accepted`; `dismissPage` sets `dismissed`. The merge is a pure helper (`mergeOcrIntoBody`) aside from id minting; it preserves every other page's blocks + each block's `page`. **Schema**: a new `ocr_pages` table (Drizzle migration `0010_free_silver_fox.sql` + `packages/db/src/schema/ocr.ts`) — `{ id, source_element_id → elements (cascade), page, text, mean_confidence, words(JSON), status, source_location_id → source_locations (cascade, RESERVED for a future T065 region-scoped OCR — `null` for the page path), timestamps }` with a `UNIQUE(source_element_id, page)` index (the idempotent upsert key the at-least-once job needs) and a `status` CHECK against the new `OCR_PAGE_STATUSES` (`suggested`/`accepted`/`dismissed`) tuple in `packages/core/src/enums.ts` (so the DB + domain union can't drift). OCR rows append NO `operation_log` op (the text is a suggestion); the LOGGED mutation is the explicit Accept's `update_document`. `OcrPagesRepository` (`packages/local-db/src/ocr-pages-repository.ts`) owns `upsertPage`/`listForSource`/`findPage`/`setStatus` and JSON-round-trips the per-word `words`, clamping confidence to an integer 0–100. The page-image bytes the worker OCR'd never live in SQLite (vault only); the table holds only structured text + confidence. **Typed seam**: four new channels — `sources:runOcr` (render → enqueue), `sources:getOcr` (read the suggestion layer), `sources:acceptOcr` (merge into body), `sources:dismissOcr` — in `channels.ts` with zod request/result schemas in `contract.ts` (the `runOcr` PNG is `ArrayBuffer`-typed + size-capped at 24 MB; `contract.test.ts`), threaded `ipc.ts` → `db-service.ts` (a lazily-built `ocrService` getter that requires `assetsDir`, plus `setRunner` so the command path can enqueue while the apply handler reaches the service the other direction) → `preload/index.ts` → `apps/web/src/lib/appApi.ts` as `window.appApi.sources.runOcr`/`getOcr`/`acceptOcr`/`dismissOcr`; there is NO generic `jobs.enqueue` channel. The renderer observes OCR progress via the existing T058 `jobs.subscribe`. **Reader**: `PdfReader.tsx` (+ `pdf-reader.css`) detects a text-free page, AUTOMATICALLY enqueues OCR on first read (the lazy on-import trigger, no click), renders the page to a PNG off its existing `<canvas>` for `runOcr`, and shows each page's recognized text as a SUGGESTION with a green/amber/red **confidence badge** + Accept / Dismiss affordances (the text enters the body only on Accept). No fs/parse/SQL/WASM in React; bytes go to the vault, never SQLite; no network. Covered by Vitest (`packages/importers/src/ocr.test.ts` — line grouping + confidence averaging + empty-words mean-0; `apps/desktop/src/worker/ocr.test.ts` — the offline-paths invariant; `apps/desktop/src/main/ocr-job.test.ts` — enqueue-writes-vault-first + idempotent apply-upsert + accept-merges-into-body + dismiss; `packages/local-db/src/ocr-pages-repository.test.ts` — the upsert/find/list/status path; `contract.test.ts`, `db-service.test.ts`, schema round-trip) and a Playwright/Electron spec (`tests/electron/pdf-ocr.spec.ts`: a REAL scanned fixture PDF, network DISABLED, runs the actual `tesseract.js` WASM worker against the bundled lang data, lands a confidence-tagged `ocr_pages` suggestion, Accept merges it into the now-searchable body, and the accepted text + row + durable `ocr/page-N.json` survive an app restart). `pnpm typecheck` + `pnpm lint` + `pnpm test` all green.
- 2026-06-02 - T065 PDF region extraction - done. Drawing a rectangle over a figure/table in the PDF reader now creates a real **image extract** — its own attention-scheduled `media_fragment` topic anchored to the page + normalized region coordinates, with the cropped figure stored in the vault and full jump-back-to-page-region lineage, entirely on-device. **Where the crop happens (renderer) vs where bytes/rows land (main)**: the renderer ALREADY has the page on a `<canvas>` (T064), so it crops the drawn rectangle to a small PNG `ArrayBuffer` there and ships `{ sourceElementId, page, pageBlockId, region, imagePng, caption?, priority? }` to MAIN; the renderer never resolves a vault path, never writes bytes, never touches SQLite. **Normalized geometry**: a new `RegionRect { x0, y0, x1, y1 }` core type (`packages/core/src/element.ts`, exported from `index.ts`) stores the bbox as **fractions 0–1 of the page** (`x0<x1`, `y0<y1`) so it is scale/zoom-independent and maps back correctly at any render size; `SourceLocation` gains a nullable `region: RegionRect | null` alongside its existing `page`. **Schema**: `source_locations` gains a nullable `region text` column (Drizzle migration `0009_public_steel_serpent.sql`; pure widening, NO backfill — existing rows get `null`), the canonical JSON-encoded bbox that the jump-to-page-region path reads; `packages/db/src/schema/sources.ts` adds the column and `packages/local-db/src/mappers.ts` (+ new `mappers.test.ts`) round-trips it (`null` for text/page-only anchors). **The orchestrator** `PdfRegionService` (`apps/desktop/src/main/pdf-region-service.ts`, construction-injected `{ db, repositories, extraction, assetVault }` mirroring `PdfImportService`) enforces a careful **element-row-first, then streamed-image** ordering because the `assets` FK requires the owning element to exist and `importAsset` opens its OWN tx: it (1) mints the `media_fragment` element id up front, (2) creates the region extract — element + body + region source-location anchor + lineage edge + initial ATTENTION schedule (status `scheduled`, a region fragment is a scheduled topic, NOT an FSRS card) — in ONE transaction via the extended `ExtractionService.createRegionExtract`, keyed by that id, (3) streams the cropped PNG into the **T059 asset vault** through `AssetVaultService.importAsset` (canonical `media/<asset_id>/original.bin`, mime `image/png`, content-hashed, dedup-on-write). On an image-import failure it **best-effort soft-deletes the just-created fragment** and re-throws (no orphan element/location/asset/file), mirroring the rollback discipline of `PdfImportService`/`UrlImportService`. Priority **inherits the source's numeric priority** unless the renderer passed an A/B/C/D override. `SourceRepository.createExtract` (`packages/local-db/src/source-repository.ts`) was widened to accept a pre-minted `elementId`, a `media_fragment` element type, and the `region` bbox (JSON-encoded into the new column), and `inspector-query.ts` surfaces the region anchor so the inspector/extract view can render the figure. **Reader**: `apps/web/src/pages/source/PdfReader.tsx` (+ `pdf-reader.css`) gains a drag-to-draw region overlay over the page canvas that crops the rectangle and calls `appApi.sources.extractRegion`; `ExtractView.tsx` (+ `extract-view.css`) renders a `media_fragment` extract as the **cropped figure image** (fetched lazily as bytes via the new region-image channel) plus a **jump-to-page-region** affordance that opens the source at the right page and highlights the region. Typed seam: TWO new channels — `sources:extractRegion` (crop → scheduled extract) and `sources:getRegionImage` (serve the cropped bytes to the renderer; MAIN owns the vault read, the renderer gets bytes not a path) — in `channels.ts` with their zod request/result schemas (`contract.ts` + `contract.test.ts`), threaded `ipc.ts` → `db-service.ts` (lazily-built `pdfRegionService` getter that requires `assetsDir`, throwing clearly if absent, mirroring `assetVaultService`) → `preload/index.ts` → `apps/web/src/lib/appApi.ts` as `window.appApi.sources.extractRegion`/`getRegionImage`; a new `image`-shaped icon was added (`Icon.tsx` + `design/icon-map.md`). No raw DB/filesystem access reaches the renderer; the cropped figure lives in the vault, never SQLite. Covered by Vitest (`apps/desktop/src/main/pdf-region-service.test.ts` — element-first ordering, vault image, priority inheritance + override, soft-delete rollback on import failure; `packages/local-db/src/source-repository.test.ts` — the `media_fragment` + region-bbox create path; `packages/local-db/src/mappers.test.ts` — region JSON round-trip incl. `null`; `contract.test.ts`, `db-service.test.ts`, schema round-trip) and the extended Playwright/Electron spec (`tests/electron/pdf-import.spec.ts`: drawing a region on a real PDF page lands a scheduled `media_fragment` extract with a stored page+region anchor and a vault image, and the region extract + its page-region lineage survive an app restart). `pnpm typecheck` + `pnpm lint` + `pnpm test` (1303 tests) all green.
- 2026-06-02 - T064 PDF import - done. PDFs are now first-class, page-anchored, local-first sources: the **renderer renders pages with pdfjs-dist** while the **Electron main extracts text + builds the ProseMirror body**, and every paginated body carries page-level lineage so a PDF text extract links back to its page. **Pure pipeline (`@interleave/importers`)** — DB-free, browser/Node-agnostic: `pdf-text.ts` drives `pdfjs-dist` (`getDocument` over the raw bytes, `legacyBuild`/no-worker path so it runs the same in the main process and Vitest) to walk every page's `getTextContent`, grouping text items into lines by Y-position and joining them into per-page paragraph runs with the 1-based `pageNumber` attached; `pdf-to-prosemirror.ts` (`pdfPagesToProseMirrorDoc`) turns those per-page runs into a single ProseMirror `doc` where **each block records its `page`** (heading vs body inferred from run shape), so the document substrate is paginated, not a flat blob. `pdfjs-worker.d.ts` types the legacy entry; three real fixtures (`two-page-text.pdf`, `heading-body.pdf`, `scanned-no-text.pdf` — the last has NO embedded text, exercising the empty-extraction path that T066 OCR will later fill) are generated by `packages/importers/scripts/` and asserted in `pdf.test.ts`. **Schema**: `document_blocks` gains a nullable `page integer` column (Drizzle migration `0008_outgoing_sir_ram.sql`; `null` for non-paginated HTML/text bodies) — the canonical block→page map that the page-granular **read-point** and the **page-level source-location** path both read, so a PDF extract resolves to a concrete page number with no separate side table. **Orchestration (Electron main)**: `apps/desktop/src/main/pdf-import-service.ts` (construction-injected `{ db, repositories, assetsDir }`, mirroring `UrlImportService`) takes the picked PDF bytes, **streams the original into the T059 asset vault** at `sources/<source_id>/original.pdf` (content-hashed, atomic) via `AssetRepository`, runs the pure `pdf-text` → `pdf-to-prosemirror` pipeline, and persists the source + paginated document blocks (each with its `page`) + the `original.pdf` asset row in ONE transaction through the existing repositories, appending `operation_log` (`create_source`/`update_document`) so the import survives restart. Page source-locations + the page read-point thread through `packages/local-db/src/source-repository.ts`, `extraction-service.ts`, and `source-location-label.ts` (now formats a "p. N" page label, with its own `source-location-label.test.ts`); `useDocument.ts` carries the per-block page through to the renderer. **Reader**: `apps/web/src/pages/source/PdfReader.tsx` (+ `pdf-reader.css`) renders pages with `pdfjs-dist` to a canvas, syncs the **page read-point** as the user scrolls, and lets text selection on a page create a page-anchored extract; `SourceReader.tsx` routes paginated PDF sources to it while keeping the HTML/text reader for everything else. Typed seam: a new `sources:importPdf` channel (`channels.ts`) with its zod request/result (`contract.ts` + `contract.test.ts`), threaded `ipc.ts` → `db-service.ts` → `preload/index.ts` → `apps/web/src/lib/appApi.ts`; the inbox import strip's PDF chip is now live. No raw DB/filesystem access reaches the renderer; bytes go to the vault, never SQLite. Covered by Vitest (`packages/importers/src/pdf.test.ts`, `apps/desktop/src/main/pdf-import-service.test.ts`, `packages/local-db/src/source-repository.test.ts` + `source-location-label.test.ts`, `apps/web/src/pages/source/useDocument.test.tsx`, `contract.test.ts`, `db-service.test.ts`, schema round-trip) and a Playwright/Electron spec (`tests/electron/pdf-import.spec.ts`: importing a real two-page PDF lands a paginated inbox source, a page-anchored extract links to its page number, and both the read-point and the page lineage survive an app restart). `pnpm typecheck` + `pnpm lint` + `pnpm test` all green.
- 2026-06-02 - T059 Asset-vault scaling for large media - done. The filesystem asset vault now handles LARGE binaries (PDFs, images, audio/video, snapshots) robustly — streamed write+hash, content-hash dedup, integrity verification, and safe orphan GC — all behind the typed `window.appApi.vault.*` / `AssetRepository` seam, with NO app-facing S3 (the local vault stays the canonical store; object storage exists only inside the future T052 backup server). **Streamed I/O primitives** (`apps/desktop/src/main/vault-io.ts`, pure Node — no Electron, no DB): `writeStreamedToVault` pipes a source (a `ReadableStream` download/fetch body OR an absolute source path opened as a `createReadStream`) through a `crypto.createHash("sha256")` tap AND a `createWriteStream` in a single `pipeline`, so a multi-hundred-MB file is hashed WHILE it writes and only the running hash + one chunk are ever resident; the bytes land in a sibling `<dest>.tmp` and are `fs.rename`d into place ONLY after the stream fully flushes (atomic write — a partial/aborted/disk-full write removes the temp and re-throws, never leaving a corrupt asset at the final path). `hashFileStreamed` is the streamed re-hash for the integrity sweep (matches the buffered `sha256File` byte-for-byte). These COMPLEMENT, not replace, the small-file `sha256`/`sha256File` (`backup-manifest.ts`) used for HTML snapshots + the backup manifest. **The orchestrator** `AssetVaultService` (`apps/desktop/src/main/asset-vault-service.ts`, construction-injected `{ db, repositories, assetsDir }` mirroring `UrlImportService`) composes those primitives + `AssetRepository` + the vault paths into three operations: (1) `importAsset` — stream-write a binary to `media/<asset_id>/original.bin` (or a caller-supplied `sources/<id>/original.pdf`-style rel path) while hashing, then **content-hash DEDUP on write** — if a LIVE asset already has that hash (`AssetRepository.findLiveByContentHash`, an `INNER JOIN elements` so the reused bytes provably belong to a still-reachable owner) it DROPS the just-written copy and records a new metadata row pointing at the SAME `relative_path` (shared-path reuse — identical bytes stored ONCE on disk), all in ONE transaction via `createWithin`, with best-effort file rollback on a failed insert; (2) `verifyIntegrity` — read-only: re-hash every live asset's stored bytes STREAMED and compare to the recorded `assets.content_hash`, reporting `ok` count + `mismatched`/`missing` asset ids + `extraFiles` (on-disk vault files with no live row); (3) `findOrphans` / `collectOrphans` — FILE-centric orphan GC: a vault file is an orphan iff its canonical relative path is NOT in `AssetRepository.referencedRelativePaths("assets")` (the live reference set), i.e. the bytes a HARD-purge's FK cascade left behind; `collectOrphans` is guarded by a `confirm: true` literal, RE-checks the live reference set at removal time (a path that became referenced since the scan is never deleted), and takes an optional `relativePaths` allow-list so the UI removes exactly the files it confirmed. Two correctness contracts are documented + enforced: the verify/GC join key is the **POSIX, leading-slash-free relative path under `assetsDir`** (the same key `listFilesRelative` yields and `assets.relative_path` stores — normalized identically on both sides so a separator/slash mismatch can't misclassify a real asset as an `extraFile`), and the walk root is **`assetsDir` ONLY** (`exports/`/`backups/` are siblings and OUT OF SCOPE — a backup archive is never flagged or reclaimed). `referencedRelativePaths` is scoped to the `"assets"` vault root so a same-named path in `exports`/`backups` can never falsely protect (or shadow) an `assets/`-rooted file. **AssetRepository** (`packages/local-db/src/asset-repository.ts`) gained `findLiveByContentHash`, `listAll`, and `referencedRelativePaths(vaultRoot = "assets")` for the dedup + verify + GC sweeps (the existing non-liveness-aware `findByContentHash` is kept for T061's snapshot lookup, with its limitation now documented). **Typed seam**: new `vault:verify` / `vault:findOrphans` / `vault:collectOrphans` channels (`channels.ts`) with `VaultVerifyResult` / `VaultOrphansResult` / `VaultCollectOrphansRequest`+`Result` zod schemas (`contract.ts` + `contract.test.ts`), threaded through `ipc.ts` → `preload/index.ts` → `apps/web/src/lib/appApi.ts` as `window.appApi.vault.*`; `DbService` exposes a lazily-built `assetVaultService` getter (needs the `assetsDir` injected at `open()`, throws clearly if absent — mirrors `urlImportService`) plus `verifyVault`/`findVaultOrphans`/`collectVaultOrphans` delegates returning renderer-safe shapes (asset ids + relative paths only — never a raw absolute path or bytes). **Background-runner integration (T058 reuse)**: the reserved `vault_verify`/`vault_gc` `JOB_TYPES` are now live — the DB-FREE worker (`job-worker.ts`) passes them straight through (posts an empty result; it has no DB + `assetsDir` to do the walk), and the MAIN-side apply handlers (`job-apply-handlers.ts`, now taking a `JobApplyHandlerDeps` bag with `getUrlImportService` + `getAssetVaultService`) run the actual streamed sweep through `AssetVaultService` so a slow large-vault hash/walk gets the same off-main scheduling + observe surface as any other job; both are read-only/idempotent (`vault_gc` here only SCANS — the destructive `collectOrphans` stays a confirmable direct `vault.collectOrphans` command, never an unattended background deletion), so an at-least-once re-run is always safe. A documented future optimization (NOT a bug, no current invariant requires it): the SHA-256 compute still runs on main's thread because only main can read the asset rows; a later pass could stream the resolved bytes to the worker purely for hashing. `bootstrap()` wires `getAssetVaultService: () => dbService.assetVaultService` into the runner. Covered by Vitest (`apps/desktop/src/main/vault-io.test.ts` — streamed hash matches buffered, atomic-rename, partial-write cleanup; `apps/desktop/src/main/asset-vault-service.test.ts` — dedup shares one file, verify catches a corrupted byte + missing file + extra file, GC reclaims only unreferenced files and respects the live reference set + allow-list + `confirm` guard; `packages/local-db/src/asset-repository.test.ts` — the new live/reference-set queries; `contract.test.ts`, `db-service.test.ts`) and a Playwright/Electron spec (`tests/electron/vault.spec.ts`: a real import dedups identical bytes to one file, `verify` is clean, a hard-purge leaves an orphan that `findOrphans` surfaces and `collectOrphans` reclaims, and integrity survives an app restart). `pnpm typecheck` + `pnpm lint` + `pnpm test` all green.
- 2026-06-02 - T058 Local background runner - done. Heavy/async work now runs in an on-device background job queue off the Electron main thread, with URL import rewired as the end-to-end proof job. **Mechanism: an Electron `utilityProcess` worker** (decided over `worker_threads` and explicitly NOT pg-boss/a server worker) — chosen for strong process isolation so a hostile/huge fetch or a future native OCR/PDF/embed can't corrupt or stall main's V8 heap, and so the worker can NEVER be tempted to share the single `better-sqlite3` handle. The worker (`apps/desktop/src/worker/job-worker.ts`, bundled DB-FREE into its own third esbuild target `dist/job-worker.cjs` via `build.mjs`) does pure compute + network I/O only and posts a serializable result back over `process.parentPort`; ALL DB writes stay in main, in a transaction, through the existing repositories (single-writer invariant intact). The queue is **persisted** in a new infra-only `jobs` SQLite table (Drizzle migration `0007_parched_killmonger.sql` + `packages/db/src/schema/jobs.ts` — NO element FK, NO lineage, NO `operation_log` op; `type`/`status` CHECK-constrained against the new `JOB_TYPES`/`JOB_STATUSES` tuples in `packages/core/src/enums.ts` so the DB and domain union can't drift), so an in-flight job survives an app restart: on launch `JobRunner.start()` runs `jobsRepo.recoverRunning()` to re-queue (or terminal-fail, if the retry budget is spent) any row left `running` by a crash, then drains `queued` rows. Delivery is **at-least-once with bounded retry + backoff** (recovery Option A — a crash counts against `maxAttempts`), so apply handlers MUST be idempotent/dedup-guarded; the `url_import` apply is safe because T061 canonical-URL/content-hash dedup turns a re-run into a `"duplicate"` no-op. New domain type `Job`/`JobProgress` + `JobId`/`JobType`/`JobStatus` (`packages/core/src/job.ts`, `enums.ts`, `ids.ts`); persistence in `JobsRepository` (`packages/local-db/src/jobs-repository.ts`, wired into `Repositories.jobs`) with `enqueue`/`claimNext` (oldest `queued` whose `notBefore` ≤ now)/`markProgress`/`succeed`/`fail`/`requeue`/`cancel`/`recoverRunning`. The MAIN-side orchestrator `JobRunner` (`apps/desktop/src/main/job-runner.ts`) owns the worker (injectable `fork` factory for unit tests — no real child process), a coalesced `setImmediate` tick loop, fixed small concurrency (2), Zod-validated worker messages both ways (`apps/desktop/src/worker/messages.ts`), an `observe`/`waitForTerminal` emitter, and the apply-handler registry (`apps/desktop/src/main/job-apply-handlers.ts`, bound lazily to `dbService.urlImportService`). URL import is the proof job: `sources:importUrl` now ENQUEUES a `url_import` job and AWAITS its terminal `job:update` (main never blocks on the network), the worker fetches off-main via the shared SSRF-guarded `apps/desktop/src/main/url-fetch.ts` (extracted from `url-import-service.ts`), and main applies the existing Readability → sanitize → vault → `createSource` pipeline over the worker HTML — the single-result `imported | duplicate` contract is preserved and a terminal `failed`/`cancelled` re-throws the reconstructed typed `UrlImportError`. A renderer-safe **observe** surface (`jobs.list` + receive-only `jobs.subscribe`, projecting `Job` → `JobSummary`) was threaded through `channels.ts`/`contract.ts`/`ipc.ts`/`preload/index.ts`/`apps/web/src/lib/appApi.ts`; there is intentionally NO generic `jobs:enqueue` channel (deferred to M14/M18). `bootstrap()` starts the runner and `will-quit` stops it BEFORE `dbService.close()` so no apply handler writes to a closed connection (leaving the persisted queue intact for next launch). The other `JOB_TYPES` (`ocr`/`embed`/`ai`/`cleanup`/`vault_verify`/`vault_gc`) are reserved so M14/M18/T059 slot in with only a worker dispatch case + a main apply handler — no queue/table/IPC shape change. Covered by Vitest (`packages/local-db/src/jobs-repository.test.ts`, `apps/desktop/src/main/job-runner.test.ts` + `job-runner.integration.test.ts`, `contract.test.ts`, `db-service.test.ts`, `packages/db/src/schema.roundtrip.test.ts`) and a Playwright/Electron spec (`tests/electron/job-runner.spec.ts`: a real `utilityProcess`-backed URL import lands an inbox source off-main and the queue survives an app restart). `pnpm typecheck` + `pnpm lint` + `pnpm test` (1244 tests) all green.
- 2026-06-01 - T063 Side-panel capture - done. The MV3 extension's **Side Panel** (`apps/extension/sidepanel.html` + `src/sidepanel.ts`, formerly a T062 placeholder) is now a real capture surface that sits beside the page: it shows the active tab + the on-demand current selection (pulled via `chrome.scripting.executeScript`, no live selection tracking), lets the user pick a **priority** (A/B/C/D chips, default **C** = `DEFAULT_CAPTURE_PRIORITY`) and type a short **reason** ("why this matters"), then saves either the selection or the whole page. **Every capture routes through the SAME token-protected `127.0.0.1` loopback `/capture` path the popup uses** — the panel only `chrome.runtime.sendMessage`s the existing `save-page`/`save-selection` message (which already carried optional `priority` + `reason` from T062) to the background service worker; there is NO new endpoint, NO new network surface, and NO direct DB/`window.appApi` write. It is DESIGN-LANGUAGE reuse only — the panel re-declares the OKLCH + A/B/C/D priority tokens in `tokens.css` and depends solely on the zod-only `@interleave/capture-contract` (via `./shared`); it never imports `apps/web`, `@interleave/core`, `@interleave/local-db`, or the renderer. The panel also renders a bounded **recent-captures** list driven entirely off `chrome.storage.local` (subscribing to `chrome.storage.onChanged`, populated by the worker on each success — no live inbox-read endpoint, intentionally out of scope). Opening the panel is wired from the popup ("Open panel" button) and a new `open-panel` keyboard command (`manifest.json`) via `chrome.sidePanel.open({ tabId })` (`src/popup.ts`, `src/background.ts`). Selection captures continue to land a fresh inbox `source` through the existing `importSelection` loopback handler with priority mapped to numeric (`A` → 0.875) and the reason folded into `reason_added` alongside the anchor `blockContext` (no schema change — T062's decision). Covered by Vitest (`packages/capture-contract/src/capture-contract.test.ts` adds the default-C panel case) and the Playwright/Electron integration spec (`tests/electron/capture-server.spec.ts` now asserts a priority-A reason-carrying selection capture lands with the right numeric priority + `reasonAdded` provenance, and that BOTH survive an app restart).
- 2026-06-01 - T062 Browser extension MVP (local-first capture) - done. A Manifest V3 Chrome extension can now "save page" / "save selection" / "save to inbox" and have the capture land in the running desktop app's inbox with full source lineage, entirely on-device. The extension's service worker shapes a capture and POSTs it (with a pairing token) to a token-protected `127.0.0.1` **loopback** HTTP capture server mounted in the Electron main; a "save page" runs the **same M12 import pipeline** (`importFromHtml`: Readability → sanitize → snapshot-to-vault → HTML→ProseMirror → `createSource` + `operation_log`) so the page becomes a proper inbox `source` with a vault snapshot, while a "save selection" lands a fresh inbox `source` via the new `importSelection` method. A new framework-free **`packages/capture-contract`** (zod-only — imported by BOTH the extension and the main, so neither side pulls the other's deps) holds the wire schemas (`CaptureRequestSchema` discriminated union, `CaptureResponseSchema`, `PairingPingResponseSchema`) plus the pure, unit-tested helpers `shapeCapture` / `validateOrigin` / `timingSafeTokenEqual`. The new **`apps/extension`** workspace package (MV3 manifest, `background.ts` service worker, popup, options/pairing page, sidepanel placeholder, four raster PNG icons, esbuild `build.mjs`, `README.md` load-unpacked checklist) depends only on `@interleave/capture-contract` — no React/Electron/`@interleave/core`. The loopback server (`apps/desktop/src/main/capture-server.ts`, canonical port `47615` with a small fallback scan, `127.0.0.1`-only, POST `/capture` + GET `/ping`, off until paired) and the pure, injectable handler (`capture-handler.ts` — `handleCapture` validates unpaired/Origin/token/body-size/zod and dispatches into the one construction-injected `UrlImportService`, mapping both the `imported` and `duplicate` arms to a `200 CaptureResponse`). Pairing lifecycle is in `capture-pairing.ts` (`capture.token`/`capture.enabled`/`capture.port`/`capture.allowedOrigin` stored via the **raw `SettingsRepository.get`/`set`** key/value path — never the typed `AppSettings` patch — `enabled` defaults to **false**) + `capture-controller.ts`; `importSelection` was **appended** to the existing `apps/desktop/src/main/url-import-service.ts` (folds `blockContext` into `reasonAdded`, no schema change, no dedup, always `imported`). `bootstrap()` (`index.ts`) starts/stops the server off `capture.enabled` (with an `INTERLEAVE_CAPTURE_ENABLED=1` test opt-in mirroring `INTERLEAVE_SEED_ON_EMPTY`) and reuses the M12 `dbService.urlImportService` accessor. The typed `window.appApi.capture` surface (`getPairing`/`regenerateToken`/`setEnabled`) was threaded through `channels.ts`/`contract.ts`/`ipc.ts`/`preload/index.ts`/`apps/web/src/lib/appApi.ts`, and the **Settings** "Browser capture" card (enable toggle + running state/port + token copy/regenerate + paired-origin) plus the inbox import strip's now-live "Browser capture" chip route there. Covered by Vitest (`packages/capture-contract/src/capture-contract.test.ts`, `apps/desktop/src/main/capture-handler.test.ts`, `db-service.test.ts`, `contract.test.ts`) and the Playwright/Electron integration spec (`tests/electron/capture-server.spec.ts`: token-authenticated loopback POST lands a selection in the inbox, rejects bad-token/bad-Origin/unpaired, and the capture + token + port survive an app restart; `launch.ts` gains `captureEnabled`).
- 2026-06-01 - T061 Canonical URL & duplicate detection - done. URL import (T060) is now dedup-aware. A single shared normalizer (`packages/core/src/url.ts` `canonicalizeUrl`, exported from `packages/core/src/index.ts`) strips tracking params + fragment and lower-cases the host so two URLs for the same page collapse to one key. Before writing anything, `UrlImportService` (`apps/desktop/src/main/url-import-service.ts`) checks for an already-imported page by TWO signals against only LIVE (non-soft-deleted) sources: (1) the **canonical URL** — the primary signal, backed by a new non-unique index `sources_canonical_url_idx` (Drizzle migration `packages/db/drizzle/0006_round_vertigo.sql` + `packages/db/src/schema/sources.ts`; non-unique by design so "import new version anyway" can legitimately reuse a canonical URL); and (2) the cleaned-HTML snapshot's **sha-256 content hash** — a same-article-different-URL backstop looked up via `AssetRepository.findByContentHash`, disambiguated to the `cleaned.html` `source_html` asset so a byte-collision on `original.html` can't mis-match. The typed read-only lookups live in `packages/local-db/src/source-dedup-query.ts` (`SourceDedupQuery`, wired into `Repositories.sourceDedup` in `packages/local-db/src/index.ts` — `AssetRepository` is now shared with it). On a match (and `forceNewVersion` false) the import persists NOTHING — no vault file, no DB row — and returns the new discriminated `"duplicate"` arm with the existing match(es) (id/title/status/accessedAt/matchedBy); the user then picks Open existing / Import new version (re-call with `forceNewVersion: true`) / Cancel. Hashes are computed up front so a duplicate writes zero bytes. The result + request gain the `forceNewVersion` flag and `"duplicate"` arm across the typed `window.appApi` seam (`apps/desktop/src/shared/contract.ts` `SourceDuplicateSummary` + `SourcesImportUrlResult`, `apps/web/src/lib/appApi.ts`), and the `ImportUrlModal.tsx` renders the reuse-or-new-version prompt. Detection only — never auto-merges or destroys anything, and soft-deleted sources never block a re-import. Covered by Vitest (`packages/core/src/url.test.ts`, `packages/local-db/src/source-dedup-query.test.ts`, `apps/desktop/src/main/url-import-service.test.ts`, `apps/desktop/src/shared/contract.test.ts`, `db-service.test.ts`) and the Playwright spec (`tests/electron/url-import.spec.ts`).
- 2026-06-01 - T060 Automatic URL import (local-first) - done. Pasting a URL now imports a web page entirely on-device: the **Electron main process** fetches the page, runs Readability to extract the article, sanitizes the cleaned HTML, stores both the original snapshot and cleaned HTML in the filesystem asset vault, converts the article to ProseMirror JSON, and creates an inbox `source` with title/byline/site/canonical-URL metadata and full lineage — no renderer DB/filesystem access and nothing sent to a server. A new `@interleave/importers` package holds the pure pipeline (`readability.ts`, `sanitize.ts`, `html-to-prosemirror.ts` + their tests and `__fixtures__/`); the orchestration/IPC host lives in `apps/desktop/src/main/url-import-service.ts` and `url-import-host.ts`, exposed through the typed `window.appApi` surface (new `channels.ts` entry + `contract.ts`/`contract.test.ts` schema + `ipc.ts` + `db-service.ts` + `preload/index.ts` + `apps/web/src/lib/appApi.ts`). The inbox UI gets an `ImportUrlModal.tsx` wired into `InboxScreen.tsx`; the import persists snapshot/cleaned-HTML assets via `AssetRepository`, creates the source via `SourceRepository`, and appends `operation_log` (`create_source`/`update_document`) entries so it survives restart. ProseMirror helpers extended in `packages/core` (`prosemirror.ts`/`prosemirror.test.ts`). Covered by Vitest (`packages/importers` `readability.test.ts`, `sanitize.test.ts`, `html-to-prosemirror.test.ts`; `apps/desktop/src/main/url-import-service.test.ts`, `url-import-host.test.ts`; `packages/local-db/src/source-repository.test.ts`; `contract.test.ts`) and the Playwright spec (`tests/electron/url-import.spec.ts`).
- 2026-06-01 - Part II re-scoped local-first (server = encrypted backup only). The gold-standard half of the roadmap (T051–T100) is reframed so EVERYTHING runs on-device and the server is **only an end-to-end-encrypted backup target** — it never mirrors the domain model, replays the op-log, or processes content. Decisions: (1) **pure backup, single canonical device** — no live multi-device sync, so the old two-way-sync (T056) and conflict-UI (T057) tasks are **dropped and repurposed** into automatic-backup/retention/verification (T056) and encryption-keys/recovery (T057); (2) a **thin first-party backup server** (auth + encrypted blob store), so M11 collapses from a sync engine + server domain mirror into "Backup & restore" (T051 thin API, T052 minimal blob store, T053 light auth, T054 incremental-backup design, T055 encrypted backup+restore); (3) **local/BYO-key AI + an optional, off-by-default managed proxy** — semantic search moves to a local vector store (`sqlite-vec`, not pgvector — T087) and AI runs from the Electron main with the user's own key (T093). M12's server worker (T058) becomes a **local background runner** and app-level S3 (T059) becomes **asset-vault scaling**; M14/M15 storage is the local vault and OCR/embeddings run on the local runner; T098 becomes backup-encryption hardening; T100 drops multi-device from the load matrix. The MVP (T001–T050) is unchanged and nothing already built is wasted — the `operation_log` now powers undo/audit/**incremental backup** instead of server convergence. Reconciled `roadmap.md`, `architecture.md`, `CLAUDE.md`, and `domain-model.md`; the local-first `tasks/M12-web-import.md` and `tasks/M13-browser-extension.md` specs now match the roadmap.
- 2026-05-30 - T050 Ship MVP (Electron desktop) - done. The MVP now packages and runs as a local-first Electron desktop app on macOS: `apps/desktop` builds a distributable via electron-builder (`electron-builder.yml`, `scripts/dist.mjs`, `RELEASE.md`), SQLite persists in the app data directory and assets in the filesystem vault, backup/onboarding/backup-prompt flows are polished (`apps/web/src/components/Onboarding.tsx`, `BackupPrompt.tsx`), and the core loop survives restart with no raw DB/filesystem APIs exposed to the renderer. Covered by Vitest (`Onboarding.test.tsx`, `BackupPrompt.test.tsx`, `paths.test.ts`, `contract.test.ts`) and the Playwright spec (`tests/electron/onboarding.spec.ts`).
- 2026-05-30 - T049 MVP end-to-end tests - done. A single serial Playwright/Electron spec (`tests/electron/mvp-flow.spec.ts`) drives the whole MVP loop against the real Electron app: import a pasted article → activate → read + set read-point → extract a paragraph (with lineage) → author Q&A + cloze cards → review/reschedule (durable review log, advanced due date) → search → open original source → backup (valid hashed zip), then relaunches the app and verifies every artifact survives the restart. Exercises only the typed `window.appApi` surface, so the renderer-never-touches-DB/filesystem invariant is asserted end-to-end.
- 2026-05-30 - T048 Keyboard shortcuts & command palette - done. The main workflow is now mouse-free: scoped shortcuts (next-item, extract, cloze, postpone, done, delete, raise/lower priority, search, open-parent, open-source) plus a command palette invoke commands through the same typed `window.appApi` path as the UI buttons, with a native Electron menu wired up. Shortcuts/scope/global-actions live in `apps/web/src/shell/` (`shortcuts.ts`, `activeScope.ts`, `useGlobalActions.ts`, `useShellShortcuts.ts`, `CommandPalette.tsx`) and the native menu in `apps/desktop/src/main/menu.ts`. Covered by Vitest (`shortcuts.test.ts`, `CommandPalette.test.tsx`, `useShellShortcuts.test.tsx`, `nav.test.ts`, `contract.test.ts`) and the Playwright spec (`tests/electron/keyboard.spec.ts`).
- 2026-05-30 - T047 Backup / export - done. Backup creates a restore-ready zip (SQLite db + asset vault + manifest) behind the Electron/IPC boundary, exposed through the typed `window.appApi` surface so the renderer never touches the filesystem directly. Covered by Vitest (`backup-manifest.test.ts`, `backup-service.test.ts`, `contract.test.ts`, `db-service.test.ts`) and the Playwright spec (`tests/electron/backup.spec.ts`).
- 2026-05-30 - T046 Import/process balance warnings - done. A balance banner warns when imports outpace processing: the import/process rates are computed behind the Electron/IPC boundary (`packages/core/balance.ts`, `packages/local-db/balance-query.ts`) and read through the typed `window.appApi` surface, with a `BalanceBanner` shown on inbox/analytics so the renderer never touches the DB directly. Covered by Vitest (`balance.test.ts`, `balance-query.test.ts`, `BalanceBanner.test.tsx`, `contract.test.ts`, `db-service.test.ts`) and the Playwright spec (`tests/electron/balance.spec.ts`).
- 2026-05-30 - T045 Basic analytics - done. A new analytics view surfaces daily reviews, due cards/topics, new cards/extracts, deletions, leeches, and 30-day retention, computed behind the Electron/IPC boundary and read through the typed `window.appApi` surface so the renderer never touches the DB directly.
- 2026-05-30 - T044 Deletion, trash & undo - done. Soft delete now has a recoverable path: a `/trash` view lists soft-deleted elements and supports restore, backed by a `trash-query` in `packages/local-db`; command-level undo covers delete/mark-done/suspend/bulk-postpone via a transactional `UndoService` (`packages/local-db/src/undo-service.ts`). Everything is exposed through the typed `window.appApi` surface (channels + contract + ipc + db-service + preload + appApi) so the renderer never touches the DB directly; mutations append `operation_log` entries (`soft_delete_element`/`restore_element`/`update_element`/`reschedule_element`) and survive app restart. Covered by Vitest (`trash-query.test.ts`, `undo-service.test.ts`, `contract.test.ts`, `db-service.test.ts`, `nav.test.ts`, `settings.test.ts`) and the Playwright spec (`tests/electron/trash-undo.spec.ts`).
- 2026-05-30 - T043 Source/reference display - done. Every extract and card now surfaces its source reference (title/URL/author/date/location) via a shared refblock so nothing feels orphaned; review hides the reference until the answer is revealed.
- 2026-05-30 - T042 Search - done. Local full-text search over sources/extracts/cards is now backed by SQLite FTS5 (`source_fts`, `extract_fts`, `card_fts`), kept in sync by repository writes and exposed through the typed `window.appApi` search surface so the renderer never touches the DB directly.
- 2026-05-30 - T041 Concepts & tags - done. Hierarchical concepts and flat tags can now be created and assigned to elements, and elements can be filtered by concept and tags.
- 2026-05-30 - T040 Basic leech detection - done. Cards that repeatedly fail are now detected as leeches (warn at 4 lapses) and surfaced in a dedicated cleanup view with rewrite/suspend/delete actions. The leech-detection heuristic lives in `packages/scheduler` (`leech.ts`) with unit tests (`leech.test.ts`); leech state persists on `review_states`/`cards` (Drizzle migration `0001_clever_rictor.sql`) via `ReviewRepository` in `packages/local-db`, and the cleanup view (`apps/web/src/maintenance/`) plus the review repair bar drive everything through the typed `window.appApi` surface (channels + contract + ipc + db-service + preload + appApi) so the renderer never touches the DB directly. Covered by Vitest (`packages/scheduler/src/leech.test.ts`, `packages/local-db/src/leech.test.ts`, `db-service.test.ts`, `contract.test.ts`, `ReviewRepairBar.test.tsx`, `nav.test.ts`) and the Playwright spec (`tests/electron/leech.spec.ts`).
- 2026-05-30 - T039 Sibling burying - done. A review session no longer surfaces two cards that share the same parent element back-to-back: a transactional `ReviewSessionService` (`packages/local-db/src/review-session-service.ts`) tracks the recently-shown card lineage and skips/defers siblings of the just-reviewed card when picking the next due card (unless the user explicitly opts into showing siblings). The behavior is exposed through the typed `window.appApi` queue/review surface so the renderer never touches the DB directly, with a Settings toggle (`packages/core/src/settings.ts`, `apps/web/src/pages/Settings.tsx`). Covered by Vitest (`review-session-service.test.ts`, `db-service.test.ts`, `contract.test.ts`, `settings.test.ts`, `ReviewScreen.test.tsx`, `ReviewRepairBar.test.tsx`) and the Playwright spec (`tests/electron/sibling-burying.spec.ts`).
- 2026-05-30 - T038 Review editing - done. The review session's repair row is now functional: while a card is surfaced the user can edit its body (Q&A prompt/answer or cloze text), open the source, suspend, soft-delete, or flag-as-bad — without leaving review. The mutating repairs live in a transactional `CardEditService` (`packages/local-db/src/card-edit-service.ts`) that composes `ElementRepository`/`ReviewRepository` and writes the correct EXISTING `operation_log` op (no new op types): `update_element` for body edits and the schema-churn-free advisory flag marker (latest `{flagged, reason?}` marker wins), `update_element` for suspend, `soft_delete_element` for delete. Edits never touch lineage, `review_states`, or the append-only `review_logs`, preserving the two-scheduler split. The repair UI (`apps/web/src/review/ReviewRepairBar.tsx`) drives everything through the typed `window.appApi` `cards.*` surface (channels + contract + ipc + db-service + preload + appApi) so the renderer never touches the DB directly. Covered by Vitest (`card-edit-service.test.ts`, `ReviewRepairBar.test.tsx`, `ReviewScreen.test.tsx`, `db-service.test.ts`, `contract.test.ts`) and the Playwright spec (`tests/electron/review-edit.spec.ts`). Committer note: fixed a latent op-log ordering flake surfaced by this feature — `OperationLogRepository.listForElement`/`listAll` now break `createdAt` ties by the implicit insertion-order `rowid`, so the "latest flag marker wins" read is deterministic for same-millisecond flag/un-flag ops.
- 2026-05-30 - T037 Review UI - done. The `/review` route now runs a full FSRS review session: it shows the card prompt, reveals the answer, and lets the user grade Again/Hard/Good/Easy with next-interval previews; each grade logs response time, advances the scheduler state via the `SchedulerService`, moves to the next due card, and writes a durable `review_logs` row. The session UI lives in `apps/web/src/review/` (wired into `router.tsx`) and drives grading through the typed `window.appApi` review surface (channels + contract + ipc + db-service + preload + appApi) so the renderer never touches the DB directly; mutations append `operation_log` (`add_review_log`/`reschedule_element`) entries and survive app restart. Covered by Vitest (`db-service.test.ts`, `contract.test.ts`) and the Playwright spec (`tests/electron/review.spec.ts`).
- 2026-05-30 - T036 Integrate ts-fsrs - done. A `SchedulerService` now wraps `ts-fsrs` for active-recall card scheduling: grading a card (Again/Hard/Good/Easy) advances and persists FSRS state (due/stability/difficulty/elapsed/scheduled/reps/lapses) on `review_states`, and new cards reschedule by rating. Domain logic stays behind the Electron/IPC boundary in `packages/local-db`; mutations append `operation_log` (`add_review_log`/`reschedule_element`) entries and survive app restart.
- 2026-05-30 - T035 Card-quality warnings - done. The card builder now surfaces minimum-information-principle warnings before activation: prompt-too-long, answer-too-long, missing source, multiple clozes, ambiguous pronouns, and empty answer. The heuristics live in `packages/core` (`card-quality.ts`) with unit tests (`card-quality.test.ts`), exported from the core index, and are evaluated in the renderer's `apps/web/src/reader/CardBuilder.tsx` (with styles in `extract-view.css`) so domain logic stays out of React internals. Covered by Vitest (`card-quality.test.ts`, `CardBuilder.test.tsx`) and the Playwright spec (`tests/electron/cards.spec.ts`).
- 2026-05-30 - T034 Cloze card creation - done. Selecting text inside an extract now creates a cloze `card` linked to that extract: the selection is wrapped as `{{c1::answer}}` cloze text and stored alongside structured cloze metadata (cloze number, answer, span offsets) so the prompt/answer can be reconstructed deterministically. Cloze parsing/serialization lives in `packages/core` (`cloze.ts`) with unit tests, the editor `cloze` mark lives in `packages/editor` (`marks/cloze.ts`) with tests, and creation flows through the `CardService` in `packages/local-db` behind the typed `window.appApi` card surface so the renderer never touches the DB directly. The cloze builder UI is wired into `apps/web/src/reader/CardBuilder.tsx`; creation appends an `operation_log` (`create_card`) entry, preserves source lineage back to the extract, and the card appears in the review queue. Covered by Vitest (`cloze.test.ts`, `marks/cloze.test.ts`, `CardBuilder.test.tsx`, `card-service.test.ts`, `db-service.test.ts`) and the Playwright spec (`tests/electron/cards.spec.ts`).
- 2026-05-30 - T033 Q&A card creation - done. From an extract, a "Create Q&A card" builder lets the user enter a prompt and answer with the source context shown and a live preview, then creates a Q&A `card` element linked to the extract that appears in the review queue. The builder UI lives in `apps/web/src/reader/CardBuilder.tsx` (wired into `ExtractView.tsx`) and drives card creation through the existing typed `window.appApi` card surface, so the renderer never touches the DB directly; creation appends an `operation_log` (`create_card`) entry and preserves source lineage back to the extract. Covered by Vitest (`CardBuilder.test.tsx`, `ExtractView.test.tsx`) and a Playwright spec (`tests/electron/cards.spec.ts`).
- 2026-05-30 - T032 Card model & templates - done. `card` elements are now first-class with `card_type` (Q&A / cloze), `prompt`, `answer`, `cloze_text`, `source_extract_id`, and `sibling_group_id`, alongside parents, priority, and review state. The transactional `CardService` lives in `packages/local-db` (`card-service.ts`) with unit tests; card creation flows through the typed `window.appApi` surface (channels + contract + ipc + db-service + preload + appApi) so the renderer never touches the DB directly. Mutations append `operation_log` (`create_card`) entries and preserve source lineage back to the extract.
- 2026-05-30 - T031 Process-queue learning loop - done. A single "process queue" mode now shows one due element at a time and advances to the next after each action, so the user can process a run of mixed elements (sources/extracts/cards) without returning to the list. The mode lives in `apps/web/src/pages/queue/ProcessQueue.tsx` with its keyboard-shortcut hook `useProcessShortcuts.ts` and styles `process-queue.css`, wired into the queue screen (`QueueScreen.tsx`) and router (`router.tsx`); it reuses the existing typed `window.appApi` queue surface (`queue.next`/queue actions) so the renderer never touches the DB directly. Covered by Vitest (`ProcessQueue.test.tsx`) and a Playwright spec (`tests/electron/process-queue.spec.ts`) verifying ten mixed elements process end-to-end.
- 2026-05-30 - T030 Queue actions - done. Each due item in `/queue` now supports open/postpone/raise/lower/done/dismiss/delete without leaving the list. The transactional `QueueActionService` lives in `packages/local-db` (`queue-action-service.ts`) with unit tests and composes the existing scheduler/element/queue repositories; actions append `operation_log` entries (`reschedule_element`, `update_element`, `soft_delete_element`) and survive app restart. The action surface is exposed through the typed `window.appApi` (channels + contract + ipc + db-service + preload + appApi); the renderer (`apps/web/src/pages/queue/` + new `QueueSnackbar.tsx` for undo/feedback) never touches the DB directly. Covered by Vitest (`queue-action-service.test.ts`, `QueueScreen.test.tsx`, `contract.test.ts`) and the Playwright spec (`tests/electron/queue.spec.ts`).
- 2026-05-30 - T029 Due queue - done. A new `/queue` route lists due sources, extracts, and cards sorted by priority then due date, with filters for element type, concept, and status. The due-queue query lives in `packages/local-db` (`queue-query.ts`) with unit tests and is exposed through the typed `window.appApi` surface (channels + contract + ipc + db-service + preload + appApi); the renderer (`apps/web/src/pages/queue/` + `apps/web/src/components/queue/`) never touches the DB directly. Covered by Vitest (`queue-query.test.ts`, `contract.test.ts`) and a Playwright spec (`tests/electron/queue.spec.ts`).
- 2026-05-30 - T028 Topic/extract scheduler - done. Sources, topics, and extracts now get an attention-based next-review schedule that is distinct from FSRS card review (it answers "should the user process this again, and when?" rather than "can the user recall this?"). The scheduler weighs priority, distillation stage, last-processed date, user action, whether the element produced useful children, stagnation, and repeated postponement; logic lives in `packages/core` with unit tests and is composed by the `SchedulerService` in `packages/local-db` behind the typed `window.appApi` surface. Mutations append `operation_log` (`reschedule_element`) entries and survive app restart.
- 2026-05-30 - T027 Priorities (A/B/C/D) - done. Priority is stored numerically on every element and surfaced as the A/B/C/D labels (A = high value, B = useful, C = maybe, D = low/background); every source, extract, and card can be raised or lowered. Mapping/clamp logic lives in `packages/core` (`priority.ts`) with unit tests, persistence runs through `ElementRepository` in `packages/local-db` behind the typed `window.appApi` surface (channels + contract + ipc + db-service + preload + appApi), and the inspector exposes raise/lower controls. The renderer never touches the DB directly; mutations append `operation_log` entries and survive app restart (Playwright `tests/electron/priority.spec.ts`).
- 2026-06-07 - Source block processing system - done. Processed spans were promoted from a visual-only document mark into durable per-source-block processing outcomes (`unread`, `read`, `extracted`, `ignored`, `processed_without_output`, `needs_later`, `stale_after_edit`) stored in SQLite and exposed through typed block-processing APIs. Extraction derives extracted block state from live output lineage, source document saves/OCR acceptance reconcile stale block rows, source progress feeds reader filters, source-yield analytics, scheduler pressure, and source mark-done gating, and `processed_span` remains only a visual projection or legacy annotation. Covered by repository/service/editor/renderer/IPC tests plus `tests/electron/processed-spans.spec.ts`.
- 2026-05-30 - T026 Mark processed on source text - done. Processed spans on a source can now be marked, collapsed/dimmed, and restored so the user can hide processed text without deleting the archived source. This earlier visual affordance is now superseded by the durable source block processing system recorded on 2026-06-07.
- 2026-05-29 - T025 Extract splitting (sub-extracts) - done. An extract can now be split into child sub-extracts that preserve full source lineage (source element ID, source block IDs, offsets, inherited source metadata/priority) back to the original source. Split logic lives in `packages/local-db` (`extraction-service`) with unit tests; the renderer drives it through the typed `window.appApi` surface (`apps/web/src/reader/ExtractView.tsx`) and never touches the DB directly. Mutations append `operation_log` entries and the flow survives app restart (Playwright `tests/electron/sub-extract.spec.ts`).
- 2026-05-29 - T024 Extract review mode - done. Extracts now open as readable mini-topics with trim/rewrite/split/convert/postpone/done/delete actions, and an extract can advance its distillation stage raw_extract → clean_extract → atomic_statement. The stage-transition and review logic live in `packages/local-db` (`extract-service`) with unit tests; the renderer drives everything through the typed `window.appApi` surface (channels + contract + ipc + db-service + appApi) and never touches the DB directly. UI is `apps/web/src/reader/ExtractView.tsx`. Mutations append `operation_log` entries, lineage is preserved, and the flow survives app restart (Playwright `tests/electron/extract-review.spec.ts`).
- 2026-05-29 - T023 Element hierarchy view - done. Source and extract pages now render a lineage tree in the inspector: source pages show their children (extracts/sub-extracts/cards) and extract pages show their parent chain plus children, with click-through navigation working in both directions. The tree is backed by a `packages/local-db` `lineage-query` exposed through the typed `window.appApi` surface (channels + contract + ipc + db-service + appApi); the renderer never touches the DB directly. Covered by Vitest (`lineage-query`, `LineageTree`) and a Playwright spec (`tests/electron/lineage.spec.ts`); lineage is preserved.
- 2026-05-29 - T022 Source locations - done. Each extract now persists its source element ID, source block IDs, start/end offsets, and a human-readable label, and the user can jump from an extract back to the exact paragraph in the originating source. Source-location logic and the jump-to-source flow run through the typed `window.appApi` surface; lineage is preserved and survives app restart.
- 2026-05-29 - T021 Extraction (keystone) - done. Extracting selected source text now creates a child `extract` element with its own document body, a source reference back to the originating source, a parent link, a persisted source location (source element ID, block IDs, start/end offsets, human-readable label), inherited priority, and a scheduled review state; the parent text is visually marked as an extracted span in the reader. Extraction is transactional, appends `operation_log` entries, preserves source lineage, and survives app restart; the renderer drives it only through the typed `window.appApi` surface (no direct DB access).
- 2026-05-29 - T020 Highlights - done. Highlights now persist as document marks (annotations), distinct from extracts: applying "Highlight" from the selection toolbar stores a highlight mark on the underlying document and renders it in the reader, and highlights can be removed without affecting extracts. Mark logic lives in `packages/editor` (`marks/`) with unit tests; the renderer hook is `apps/web/src/pages/source/useHighlights.ts`, persistence flows through the typed `window.appApi` document surface (channels + contract + ipc + db-service + document-repository), and the renderer never touches the DB directly. Survives app restart (Playwright `tests/electron/highlights.spec.ts`).
- 2026-05-29 - T019 Text-selection toolbar - done. Selecting text in the source reader now surfaces an inline toolbar (Extract, Cloze, Highlight, Copy, Cancel) anchored to the selection without breaking editor selection. Selection-to-source-location logic lives in `packages/editor` (`selection-location.ts`) with unit tests; the toolbar UI/positioning lives in `apps/web/src/reader/`, wired into `SourceReader.tsx`. Covered by Playwright `tests/electron/selection-toolbar.spec.ts`.
- 2026-05-29 - T018 Source reading mode - done. A clean source reader (`apps/web` `SourceReader.tsx` + `reader.css`) shows title, metadata, body, the read-point marker, and extracted-span markers, with keyboard actions for processing a long article. Reader decoration logic lives in `packages/editor` (`reader-decorations.ts`) with unit tests; persistence/queries flow through the typed `window.appApi` surface (contract + db-service), renderer never touches the DB directly. Survives app restart (Playwright `tests/electron/source-reader.spec.ts`).
- 2026-05-29 - T017 Read-points - done. Sources/topics now carry a `read_point` (block ID + offset) that can be set, jumped to, and auto-updated when an extract is created; reopening a source resumes near the last read-point. Read-point logic lives in `packages/editor` (`read-point.ts`) with the renderer hook in `apps/web` (`useReadPoint.ts`); persistence flows through the typed `window.appApi` surface (channels + contract + ipc + db-service), renderer never touches the DB directly. Survives app restart (Playwright `tests/electron/read-points.spec.ts`).
- 2026-05-29 - T016 Stable block IDs - done. Every block node now carries a stable ID preserved across imports and saves (the basis for extraction, read-points, and sync). Block-ID generation/normalization lives in `packages/editor` (`block-id.ts`, `blocks.ts`) and the constrained Tiptap schema; IDs survive the ProseMirror JSON round-trip through `packages/core` (`prosemirror.ts`) and the typed `window.appApi` document surface (db-service + contract), and persist across app restart (Playwright `tests/electron/block-ids.spec.ts`). Renderer never touches the DB directly.
- 2026-05-29 - T015 Tiptap document editor - done. Source bodies render/edit through a constrained Tiptap/ProseMirror schema (headings, paragraphs, bold, italic, links, blockquotes, lists, code, hr) in `packages/editor` (`schema.ts`, `serialize.ts`, `SourceEditor.tsx`); edits serialize to ProseMirror JSON and save/reload via the typed `window.appApi` document surface. Renderer never touches the DB directly.
- 2026-05-29 - T014 Source provenance fields - done. Manual imports now capture canonical URL, original URL, accessed date, and snapshot provenance fields (no remote fetching). URL normalization/canonicalization lives in `packages/core` (`url.ts`); the schema, typed `window.appApi` import contract, and the "New source" modal persist these fields, and the inspector surfaces them. Renderer never touches the DB directly.
- 2026-05-29 - T013 Manual text import - done. The "New source" modal now accepts a body that is stored both as plain text and as ProseMirror JSON; a pasted article appears as a source in the inbox. Plain-text-to-ProseMirror conversion lives in `packages/core`; the typed `window.appApi` source-import surface persists both representations, and renderer never touches the DB directly.
- 2026-05-29 - T012 Inbox - done. Sources can be created in the inbox, listed, viewed, kept, prioritized, accepted into active learning, or deleted — all via the typed `window.appApi` surface (no renderer DB access). Mutations run in single transactions and append `operation_log` entries; deletes are soft.
- 2026-05-29 - T011 Local settings in SQLite - done. User/domain settings (daily review budget, default desired retention, default topic interval, default source priority, keyboard layout, theme) now persist in the SQLite `settings` table and are read through the typed `window.appApi` surface; scheduler code consumes them via the typed API rather than touching the DB directly.
- 2026-05-29 - T010 Universal element inspector - done. The right inspector panel now renders any selected element's type, status, stage, priority, due date, parent, children, source, tags, and review metadata, fetched through a new typed `window.appApi` inspector query (backed by `packages/local-db` `inspector-query`) — the renderer never touches the DB directly. Shell selection state lives in a dedicated `selection` context; Vitest covers the inspector query and a Playwright spec exercises the inspector end-to-end.
- 2026-05-29 - T009 Desktop dev seed & fixtures - done. A `pnpm seed` desktop dev command resets the dev SQLite DB and builds a realistic demo collection (a source with document blocks, an extract with a source location, a sub-extract, a Q&A card, a cloze card, review state/logs, concepts/tags, asset metadata, and `operation_log` entries). Shared factories/fixtures now live in `packages/testing` and are reused by both Vitest and Playwright; `packages/local-db` gains a `test-db` helper for in-memory/seeded test databases.
- 2026-05-29 - T008 Repository classes in packages/local-db - done. `packages/local-db` now holds `ElementRepository`, `DocumentRepository`, `SourceRepository`, `ReviewRepository`, `QueueRepository`, `SearchRepository`, `AssetRepository`, `SettingsRepository`, and `OperationLogRepository` behind the Electron/IPC boundary. Meaningful mutations run in single transactions across multiple tables and append `operation_log` entries; deletes are soft (`deleted_at`); the renderer reaches repositories only via typed `window.appApi` (no React component touches SQL). Per-repo smoke tests cover referential integrity + persistence.
- 2026-05-29 - T007 Electron desktop shell + native SQLite persistence - done. `apps/desktop` provides a secure Electron window (contextIsolation, no nodeIntegration, no remote module, sandbox where practical) and a narrow typed `window.appApi` preload bridge (`app.health()`, `db.getStatus()`, `settings.get/update()`) with Zod-validated IPC payloads. The app data directory is initialized (app.sqlite + -wal/-shm, assets/, backups/), SQLite opens via better-sqlite3 with foreign_keys=ON / journal_mode=WAL / busy_timeout=5000, Drizzle migrations run safely on startup, dev loads the Vite dev server and production loads the built renderer, and data persists across app restart. The renderer has no raw Node/filesystem/SQLite access and never sees a generic db.query(sql).
- 2026-05-29 - T006 Native SQLite Drizzle schema - done. `packages/db` now holds the Drizzle SQLite-dialect schema and generated migrations for all M1 tables (elements, documents, document_blocks, document_marks, sources, source_locations, element_relations, read_points, cards, review_states, review_logs, concepts, tags, element_tags, tasks, assets, operation_log, settings); types align with @interleave/core, drizzle-kit generate/migrate plus a dev-reset can create/reset a dev DB, and the schema round-trips against an in-memory better-sqlite3 DB in tests. FTS tables arrive with search later.
- 2026-05-29 - Architecture pivot to Electron + native SQLite. The project moves from a PGlite/browser-first PWA to a local-first **Electron desktop app** on a **native SQLite** database (better-sqlite3 + Drizzle, SQLite dialect), with a filesystem **asset vault** for PDFs/snapshots/media/exports/backups and an `operation_log` from day one. The React + Vite app becomes a pure **renderer** that talks to a narrow typed `window.appApi` preload bridge; Electron (main/preload/IPC) owns all trusted local capabilities and the renderer never touches SQLite or arbitrary filesystem APIs. New monorepo additions: `apps/desktop` and `packages/local-db`; `packages/db` keeps the schema/migrations (now SQLite dialect). Native **pnpm** (`pnpm typecheck`/`pnpm test`/`pnpm lint`) is the canonical desktop toolchain; the Docker/compose/Makefile setup is kept but re-scoped to the future server phase (`api`/`worker`/`db`/`minio`). Definition of Done now requires features to survive **app restart**. Roadmap content revised in place (T001–T011, T047–T050) and pivot notes added to gold-standard sync/extension/semantic-search/desktop tasks; task numbering unchanged. Cloud sync (T051+) is designed around the SQLite op-log + Postgres (not Electric/PGlite/PowerSync now); Tauri (T097) is deprioritized to a possible future alternative shell only; a PWA/browser version is deprioritized.
- 2026-05-29 - T005 Domain language in packages/core - done. Documented TS types for the Element family (Element, ElementType, ElementStatus, DistillationStage, Priority), ReviewState/ReviewLog, Source, Document, ElementRelation, ElementLocation, plus the new desktop types Asset, AssetLocation, OperationLogEntry, and LocalVaultPath, exported from @interleave/core for app and tests.
- 2026-05-29 - T004 App shell skeleton - done. Keyboard-first shell with left sidebar, top command bar, central work area, right inspector, and bottom status bar; every main route renders through the same shell and is keyboard-navigable.
- 2026-05-29 - T003 Scaffold the React app - done. Vite + React 19 + TS + TanStack Router + Tailwind v4 in apps/web with routes /, /inbox, /queue, /source/$id, /review, /search, /settings, wired to the design tokens.
- 2026-05-29 - T002 Tooling + Docker + CI gates - done. Strict TypeScript, Biome, Vitest, Playwright smoke E2E, Dockerfiles + docker-compose + Makefile, and GitHub Actions CI wired so CI rejects type errors, lint errors, unit failures, and a smoke E2E failure.
- 2026-05-29 - T001 Create the monorepo - done. pnpm + Turborepo workspace with apps/{web,api} and packages/{core,db,scheduler,editor,ui,testing}; root typecheck/lint/test scripts pass.
</content>
