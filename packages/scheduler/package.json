{
  "name": "@interleave/scheduler",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "dev": "echo \"@interleave/scheduler: no dev step\"",
    "build": "echo \"@interleave/scheduler: no build step\"",
    "test": "vitest run",
    "lint": "echo \"@interleave/scheduler: linted from repo root via biome\"",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@interleave/core": "workspace:*",
    "ts-fsrs": "5.4.1"
  }
}
